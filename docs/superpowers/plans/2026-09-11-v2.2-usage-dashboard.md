# v2.2.0 用量看板（三级披露）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-09-11-v2.2-usage-dashboard-design.md`（R0-R10）交付用量看板三级披露（L1 迷你条/L2 黑板/L3 主列大看板）、宿主增量缓存与全链路性能治理（P1-P4）、模型分布（F05）、趋势与任意回看、导出图片、MAM 音效链。

**Architecture:** 宿主半（`src/host/*.js`）一次聚合产出折叠量（byDay/byHour/byRoute/工具/用户估算），按「末事件 seq 指纹」缓存，快照下发当日五口径+模型 Top6+近 14 日+24 小时桶；L3 主列面板与按需范围路由消费同一聚合核。客户端半（`src/client/*.tsx`）经 `main` keyed 面板渲染大看板，`ctx.layout.selectPanel` 切换；黑板/迷你条互斥顶替；canvas 导出图片。

**Tech Stack:** 既有栈——宿主纯 JS（node:fs/zstd 会话读取经 harness 服务）、客户端 React 19 + TS、esbuild 打包、vitest 测试（.test.mjs/.test.ts 同仓）、dsh 插件协议（slot: `main`/`sidebar.panellist`/`shell.overlay`；`ctx.layout.selectPanel`，rc.2 已验证存在）。

## Global Constraints

- 需求唯一事实源：spec `2026-09-11-v2.2-usage-dashboard-design.md`（R0-R10）；本计划与其冲突时以 spec 为准并上报。
- 版本：`package.json` 与 `dsh.plugin.json` 同步 `2.2.0`；`engines.dsh` 对齐 `>=0.1.2-rc.1`。
- 不变面（spec「验证与风险」）：五口径口径语义、警报触发语义（阈值跨越/日键去重）、L1 用量行内容与排版、黑板 TTL/关闭四路、宠物导入与管理体系。
- 纯 token 口径：不出现任何价格/金额字段（F04 存档）。
- 铁律（spec 6.6 修订后）：迷你条唯一可点元素=「详情 »」；拖拽无命令；单击每目标单行为；宠物本体无操作按钮。
- 测试命令：`npm test`（vitest run 全量）、`npm run typecheck`、`npm run build`、`npm run validate`。宿主纯函数测试放 `test/*.test.mjs`（import 自 `../src/host/*.js`），客户端逻辑测试放 `test/*.test.ts`。
- 每任务收尾四绿（test/typecheck/build/validate）+ 一次 commit；提交信息带任务号。
- 现有断言不可无故删除：`test/dashboard.test.mjs` 的 29 个 summarize 字节契约用例必须原样通过（D-5 增量式约束延续）。
- 轮询契约：可见 1.5s；不可见降频 5s（P4）；`/state` 响应新增 `unchanged` 短路形态（P3），老客户端（不带 `since`）行为完全不变。
- 事实源命名约定：本计划自审后已与 spec 修订版对齐——`byDayRoute` 交叉维度在 Task 1 落地；spec R1 的「用户输入估算今日单日裁剪」已修订为「全日 map 随折叠量缓存（区间聚合消费）」。
- E2E 检验产物只放 `docs/superpowers/plans/artifacts/v2.2-e2e/`（统一检验文件夹）；该文件夹**合并 main 前删除**，CHANGELOG/README 不得引用其中文件。

---

### Task 1: 宿主数据核——foldUsage 扩展 byRoute/byHour/requestCount（R2）

**Files:**
- Modify: `src/host/dashboard.js`（`foldUsage` 函数体，约 L58-89）
- Test: `test/dashboard.test.mjs`（追加用例，不改既有用例）

**Interfaces:**
- Consumes: 既有 `USAGE_KEYS`、`zeroUsage()`、`dateKeyOf(ms)`。
- Produces（后续 Task 2/4/6 依赖，签名固定）:
  - `foldUsage(evts) → { byDay, grand, byRoute, byDayRoute, requestCount, byHour }`
  - `byRoute: { [routeKey: string]: {inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, requestCount} }`，`routeKey = provider + '/' + model`；source 缺失或 provider/model 非非空字符串 → `routeKey = '未知'`（会话全量账）
  - `byDayRoute: { [dayKey]: { [routeKey]: 同byRoute桶 } }`（**日×路由交叉账**——当日模型 Top6 与区间模型聚合都从这里切片，不用会话全量 byRoute 跨日污染）
  - `byDay/byHour` 桶对象额外携带 `requestCount`（`zeroUsage()` 桶是普通对象，加字段即可；趋势的 requestCount 由这里透传）
  - `requestCount: number`（结算样本数：+1 计一次，替换 -1）
  - `byHour: { [hourKey: string]: usage桶 }`，`hourKey` 形如 `2026-09-11T14`（本地时区整点）
  - 新导出：`hourKeyOf(ms) → string`；`routeSource(ev) → {provider, model}`（读 `ev.data.message?.source`，防御式）

- [ ] **Step 1: 写失败测试**（追加到 `test/dashboard.test.mjs` 的 `describe('dashboard', ...)` 内）

```js
  // ---- v2.2 Task 1：byRoute / requestCount / byHour ----
  const mkMsg = (turn, step, time, usage, source) => ({
    type: 'assistant/message', time, data: { turn, step, usage, message: source ? { source } : undefined },
  })
  const U = (i, o, c) => ({ inputTokens: i, outputTokens: o, cacheReadTokens: c })

  it('foldUsage routes usage by provider/model and counts settled samples', () => {
    const evts = [
      mkMsg(0, 0, 1000, U(100, 10, 0), { provider: 'p1', model: 'm1' }),
      mkMsg(0, 1, 2000, U(50, 5, 20), { provider: 'p2', model: 'm2' }),
    ]
    const f = foldUsage(evts)
    expect(f.requestCount).toBe(2)
    expect(f.byRoute['p1/m1'].inputTokens).toBe(100)
    expect(f.byRoute['p2/m2'].cacheReadTokens).toBe(20)
    expect(f.byRoute['p1/m1'].requestCount).toBe(1)
  })

  it('foldUsage replacement moves usage between routes and decrements requestCount', () => {
    const evts = [
      mkMsg(0, 0, 1000, U(100, 10, 0), { provider: 'p1', model: 'm1' }),
      mkMsg(0, 0, 3000, U(70, 7, 0), { provider: 'p2', model: 'm2' }), // 同 (turn,step) 替换
    ]
    const f = foldUsage(evts)
    expect(f.requestCount).toBe(1)
    expect(f.byRoute['p1/m1']).toBeUndefined()           // 旧路由账全部冲回
    expect(f.byRoute['p2/m2'].inputTokens).toBe(70)
    expect(f.byRoute['p2/m2'].requestCount).toBe(1)
  })

  it('foldUsage missing source goes to 未知 bucket', () => {
    const f = foldUsage([mkMsg(0, 0, 1000, U(10, 1, 0), undefined)])
    expect(f.byRoute['未知'].inputTokens).toBe(10)
  })

  it('foldUsage byHour buckets by calendar hour key', () => {
    const t = new Date(2026, 8, 11, 14, 30, 0).getTime()
    const f = foldUsage([mkMsg(0, 0, t, U(10, 1, 0), { provider: 'p', model: 'm' })])
    expect(f.byHour['2026-09-11T14'].inputTokens).toBe(10)
    expect(hourKeyOf(t)).toBe('2026-09-11T14')
  })

  it('foldUsage carries requestCount on day/hour buckets and byDayRoute cross-dimension', () => {
    const t = new Date(2026, 8, 11, 14, 30, 0).getTime()
    const f = foldUsage([
      mkMsg(0, 0, t, U(10, 1, 0), { provider: 'p', model: 'm' }),
      mkMsg(0, 1, t + 1000, U(20, 2, 0), { provider: 'q', model: 'n' }),
    ])
    expect(f.byDay['2026-09-11'].requestCount).toBe(2)
    expect(f.byDayRoute['2026-09-11']['p/m'].inputTokens).toBe(10)
    expect(f.byDayRoute['2026-09-11']['q/n'].inputTokens).toBe(20)
    // 同 (turn,step) 替换：byDayRoute 同步冲回旧路由
    const g = foldUsage([
      mkMsg(0, 0, t, U(10, 1, 0), { provider: 'p', model: 'm' }),
      mkMsg(0, 0, t + 1000, U(5, 1, 0), { provider: 'q', model: 'n' }),
    ])
    expect(g.byDayRoute['2026-09-11']['p/m']).toBeUndefined()
    expect(g.byDayRoute['2026-09-11']['q/n'].inputTokens).toBe(5)
    expect(g.byDay['2026-09-11'].requestCount).toBe(1)
  })
```

同时在文件头 import 行补 `hourKeyOf`：

```js
import { foldUsage, dateKeyOf, hourKeyOf } from '../src/host/dashboard.js'
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/dashboard.test.mjs -t 'foldUsage routes'`
Expected: FAIL（`f.byRoute` 为 undefined / `hourKeyOf` 未导出）

- [ ] **Step 3: 实现**（替换 `src/host/dashboard.js` 的 `foldUsage` 整函数，并在其上方加两个导出）

```js
/** 事件消息源路由（v2.2 R2）：assistant/message.data.message.source.{provider,model}；
 *  缺失/非字符串 → null（记账归「未知」桶）。 */
export function routeSource(ev) {
  const src = ev && ev.data && ev.data.message && ev.data.message.source
  if (!src || typeof src !== 'object') return null
  const p = typeof src.provider === 'string' ? src.provider.trim() : ''
  const m = typeof src.model === 'string' ? src.model.trim() : ''
  return p && m ? { provider: p, model: m } : null
}

/** 整点桶键（本地时区）：YYYY-MM-DDTHH（与 dateKeyOf 同款 pad 拼接）。 */
export function hourKeyOf(ms) {
  const d = new Date(ms)
  return dateKeyOf(ms) + 'T' + String(d.getHours()).padStart(2, '0')
}

const UNKNOWN_ROUTE = '未知'
const routeBuckets = () => ({ inputTokens: 0, outputTokens: 0, cacheReadTokens: 0, cacheWriteTokens: 0, requestCount: 0 })

export function foldUsage(evts) {
  const byDay = {}
  const byHour = {}
  const byRoute = {}
  const byDayRoute = {}
  const grand = zeroUsage()
  let requestCount = 0
  let lastKey = null
  let lastBuckets = null
  let lastDay = null
  let lastHour = null
  let lastRoute = null
  const apply = (b, day, hour, route, sign) => {
    const d = byDay[day] || (byDay[day] = zeroUsage())
    const h = byHour[hour] || (byHour[hour] = zeroUsage())
    const r = byRoute[route] || (byRoute[route] = routeBuckets())
    const dr = byDayRoute[day] || (byDayRoute[day] = {})
    const rd = dr[route] || (dr[route] = routeBuckets())
    for (const k of USAGE_KEYS) {
      const v = (b[k] || 0) * sign
      grand[k] += v
      d[k] += v
      h[k] += v
      r[k] += v
      rd[k] += v
    }
    r.requestCount += sign
    rd.requestCount += sign
    d.requestCount = (d.requestCount || 0) + sign
    h.requestCount = (h.requestCount || 0) + sign
    requestCount += sign
  }
  for (const ev of Array.isArray(evts) ? evts : []) {
    const d = ev && ev.data
    if (!d) continue
    if (ev.type === 'llm/retry-started') {
      lastKey = null; lastBuckets = null; lastDay = null; lastHour = null; lastRoute = null
      continue
    }
    if (ev.type !== 'assistant/message' || !d.usage || typeof d.usage !== 'object') continue
    if (typeof ev.time !== 'number' || !Number.isFinite(ev.time)) continue // 无时间戳的样本无法归日，跳过（防 NaN 桶）
    const u = d.usage
    const b = {}
    for (const k of USAGE_KEYS) b[k] = typeof u[k] === 'number' && Number.isFinite(u[k]) && u[k] > 0 ? u[k] : 0
    const key = d.turn + ':' + d.step
    const day = dateKeyOf(ev.time)
    const hour = hourKeyOf(ev.time)
    const src = routeSource(ev)
    const route = src ? src.provider + '/' + src.model : UNKNOWN_ROUTE
    if (key === lastKey && lastBuckets && lastDay) apply(lastBuckets, lastDay, lastHour, lastRoute, -1)
    apply(b, day, hour, route, 1)
    lastKey = key; lastBuckets = b; lastDay = day; lastHour = hour; lastRoute = route
  }
  return { byDay, byHour, byRoute, byDayRoute, grand, requestCount }
}
```

- [ ] **Step 4: 跑全量测试确认通过（含 29 个 summarize 契约用例）**

Run: `npx vitest run test/dashboard.test.mjs`
Expected: PASS 全部

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/dashboard.js test/dashboard.test.mjs
git commit -m "feat(host): Task1 foldUsage 扩展 byRoute/byHour/requestCount（R2 模型分布数据核）"
```

---

### Task 2: 宿主纯函数——buildTrend（14 日+24 整点桶）与 summarizeRange（区间聚合）（R3/R9）

**Files:**
- Modify: `src/host/dashboard.js`（文件末尾追加纯函数区）
- Test: `test/dashboard.test.mjs`（追加）

**Interfaces:**
- Consumes: Task 1 的 `foldUsage` 产物形状、`hourKeyOf`、`hitRate(usage)`（既有）、`dateKeyOf`。
- Produces:
  - `buildTrend(folds, nowMs) → { days: TrendDay[14], hours: TrendHour[24] }`，`folds: foldUsage返回值[]`（每会话一份）
  - `TrendDay = { key: 'YYYY-MM-DD', dayTotal, requestTotal, cacheRead, outputTokens, hitPct, requestCount }`（dayTotal=四桶和；requestTotal=input+cacheRead；hitPct=0..1；days 升序、末端=今日，无数据日补零桶）
  - `TrendHour = { key: 'YYYY-MM-DDTHH', dayTotal, requestTotal, requestCount }`（当日 0-23 时，未来小时为零桶；**不含进行中的小时也照记**——整点锚定体现在 5h 视图取「最近 5 个完整整点桶」由客户端切片，见 Task 12）
  - `summarizeRange(folds, toolsByDayList, userEstByDayList, fromKey, toKey) → { from, to, days: TrendDay[], totals: { requestTotal, cacheRead, outputTokens, userEst, hitPct, requestCount }, models: RouteAgg[], tools: ToolAgg[] }`；`RouteAgg = { route, provider, model, requestTotal, cacheRead, outputTokens }`（按 requestTotal 降序，全量不截断——截断由调用方做）；`ToolAgg = { name, count, durMs }`（按 count 降序）
  - `foldToolsByDay(evts) → { byDay: { [dayKey]: { [toolName]: { count, durMs } } } }`（tool/call 计数；tool/result 带 durationMs 时累加耗时——字段名以 `ev.data.name` / `ev.data.durationMs` 防御式读取）

- [ ] **Step 1: 写失败测试**（追加到 `test/dashboard.test.mjs`）

```js
  // ---- v2.2 Task 2：buildTrend / summarizeRange / foldToolsByDay ----
  const fold1 = () => foldUsage([
    mkMsg(0, 0, new Date(2026, 8, 11, 10, 0).getTime(), U(100, 40, 60), { provider: 'p', model: 'm1' }),
  ])

  it('buildTrend returns 14 ascending day buckets with zero-fill and derived fields', () => {
    const now = new Date(2026, 8, 11, 15, 0).getTime()
    const t = buildTrend([fold1()], now)
    expect(t.days).toHaveLength(14)
    expect(t.days[13].key).toBe('2026-09-11')
    expect(t.days[13].dayTotal).toBe(200)
    expect(t.days[13].requestTotal).toBe(160)
    expect(t.days[13].hitPct).toBeCloseTo(0.375, 5) // 60/160
    expect(t.days[13].requestCount).toBe(1)
    expect(t.days[0].key).toBe('2026-08-29')
    expect(t.days[0].dayTotal).toBe(0)
  })

  it('buildTrend returns 24 hour buckets for today only', () => {
    const now = new Date(2026, 8, 11, 15, 0).getTime()
    const t = buildTrend([fold1()], now)
    expect(t.hours).toHaveLength(24)
    expect(t.hours[10].requestTotal).toBe(160)
    expect(t.hours[0].key).toBe('2026-09-11T00')
    expect(t.hours[23].key).toBe('2026-09-11T23')
  })

  it('summarizeRange aggregates days/models/tools/userEst within [from, to]', () => {
    const now = new Date(2026, 8, 11, 15, 0).getTime()
    const f = fold1()
    const tools = { byDay: { '2026-09-11': { bash: { count: 3, durMs: 1200 } } } }
    const userEst = { '2026-09-11': 55 }
    const r = summarizeRange([f], [tools], [userEst], '2026-09-10', '2026-09-11')
    expect(r.days.map((d) => d.key)).toEqual(['2026-09-10', '2026-09-11'])
    expect(r.totals.requestTotal).toBe(160)
    expect(r.totals.userEst).toBe(55)
    expect(r.models[0]).toMatchObject({ route: 'p/m1', provider: 'p', model: 'm1', requestTotal: 160 })
    expect(r.tools[0]).toEqual({ name: 'bash', count: 3, durMs: 1200 })
  })

  it('foldToolsByDay counts tool calls and durations defensively', () => {
    const evts = [
      { type: 'tool/call', time: new Date(2026, 8, 11, 9).getTime(), data: { name: 'bash' } },
      { type: 'tool/result', time: new Date(2026, 8, 11, 9).getTime() + 500, data: { name: 'bash', durationMs: 500 } },
      { type: 'tool/call', time: new Date(2026, 8, 11, 9).getTime() + 600, data: {} }, // 无名 → 忽略
    ]
    const t = foldToolsByDay(evts)
    expect(t.byDay['2026-09-11'].bash).toEqual({ count: 2, durMs: 500 })
  })
```

import 行补 `buildTrend, summarizeRange, foldToolsByDay`。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/dashboard.test.mjs -t 'buildTrend'`
Expected: FAIL（未导出）

- [ ] **Step 3: 实现**（追加到 `src/host/dashboard.js` 末尾）

```js
// ---------- v2.2 R3：趋势窗口（14 日 + 当日 24 整点桶）与区间聚合 ----------

const dayTotalOf = (u) => u.inputTokens + u.outputTokens + u.cacheReadTokens + u.cacheWriteTokens
const reqTotalOf = (u) => u.inputTokens + u.cacheReadTokens

const shiftDayKey = (key, delta) => {
  const [y, m, d] = key.split('-').map(Number)
  const dt = new Date(y, m - 1, d + delta)
  return dateKeyOf(dt.getTime())
}

/** folds: foldUsage() 产物数组（每会话一份）。days=近 14 日升序零填充；hours=今日 0-23 整点桶。
 *  requestCount 由 Task 1 的 byDay/byHour 桶透传。 */
export function buildTrend(folds, nowMs) {
  const todayKey = dateKeyOf(nowMs)
  const dayKeys = []
  for (let i = 13; i >= 0; i--) dayKeys.push(shiftDayKey(todayKey, -i))
  const dayAcc = Object.fromEntries(dayKeys.map((k) => [k, zeroUsage()]))
  const hourAcc = {}
  for (let h = 0; h < 24; h++) hourAcc[todayKey + 'T' + String(h).padStart(2, '0')] = zeroUsage()
  for (const f of Array.isArray(folds) ? folds : []) {
    if (!f) continue
    for (const [k, u] of Object.entries(f.byDay || {})) {
      if (!dayAcc[k]) continue
      for (const bk of USAGE_KEYS) dayAcc[k][bk] += u[bk] || 0
      dayAcc[k].requestCount = (dayAcc[k].requestCount || 0) + (u.requestCount || 0)
    }
    for (const [k, u] of Object.entries(f.byHour || {})) {
      if (!hourAcc[k]) continue
      for (const bk of USAGE_KEYS) hourAcc[k][bk] += u[bk] || 0
      hourAcc[k].requestCount = (hourAcc[k].requestCount || 0) + (u.requestCount || 0)
    }
  }
  const day = (k, b) => ({ key: k, dayTotal: dayTotalOf(b), requestTotal: reqTotalOf(b), cacheRead: b.cacheReadTokens, outputTokens: b.outputTokens, hitPct: hitRate(b), requestCount: b.requestCount || 0 })
  return {
    days: dayKeys.map((k) => day(k, dayAcc[k])),
    hours: Object.entries(hourAcc).map(([k, u]) => ({ key: k, dayTotal: dayTotalOf(u), requestTotal: reqTotalOf(u), requestCount: u.requestCount || 0 })),
  }
}

/** 区间聚合（/dashboard/range 数据核）。folds/toolsByDayList/userEstByDayList 与会话一一对应。
 *  模型聚合走 byDayRoute 日切片（Task 1 交叉维度）——绝不用会话全量 byRoute（跨日旧账会污染区间）。 */
export function summarizeRange(folds, toolsByDayList, userEstByDayList, fromKey, toKey) {
  const dayKeys = []
  for (let k = fromKey; k <= toKey && dayKeys.length <= 31; k = shiftDayKey(k, 1)) dayKeys.push(k)
  const dayAcc = Object.fromEntries(dayKeys.map((k) => [k, zeroUsage()]))
  const routeAcc = {}
  for (const f of Array.isArray(folds) ? folds : []) {
    if (!f) continue
    for (const [k, u] of Object.entries(f.byDay || {})) {
      if (!dayAcc[k]) continue
      for (const bk of USAGE_KEYS) dayAcc[k][bk] += u[bk] || 0
      dayAcc[k].requestCount = (dayAcc[k].requestCount || 0) + (u.requestCount || 0)
    }
    for (const k of dayKeys) {
      const dayRoute = f.byDayRoute && f.byDayRoute[k]
      if (!dayRoute) continue
      for (const [route, r] of Object.entries(dayRoute)) {
        const slash = route.indexOf('/')
        const a = routeAcc[route] || (routeAcc[route] = {
          route,
          provider: slash > 0 ? route.slice(0, slash) : '',
          model: slash > 0 ? route.slice(slash + 1) : '',
          requestTotal: 0, cacheRead: 0, outputTokens: 0,
        })
        a.requestTotal += r.inputTokens + r.cacheReadTokens
        a.cacheRead += r.cacheReadTokens
        a.outputTokens += r.outputTokens
      }
    }
  }
  const totals = { requestTotal: 0, cacheRead: 0, outputTokens: 0, userEst: 0, hitPct: 0, requestCount: 0 }
  for (const est of Array.isArray(userEstByDayList) ? userEstByDayList : []) {
    if (!est) continue
    for (const k of dayKeys) totals.userEst += est[k] || 0
  }
  const days = dayKeys.map((k) => {
    const b = dayAcc[k]
    return { key: k, dayTotal: dayTotalOf(b), requestTotal: reqTotalOf(b), cacheRead: b.cacheReadTokens, outputTokens: b.outputTokens, hitPct: hitRate(b), requestCount: b.requestCount || 0 }
  })
  totals.requestTotal = days.reduce((s, d) => s + d.requestTotal, 0)
  totals.cacheRead = days.reduce((s, d) => s + d.cacheRead, 0)
  totals.outputTokens = days.reduce((s, d) => s + d.outputTokens, 0)
  totals.requestCount = days.reduce((s, d) => s + d.requestCount, 0)
  totals.hitPct = totals.requestTotal > 0 ? totals.cacheRead / totals.requestTotal : 0
  const models = Object.values(routeAcc).sort((a, b) => b.requestTotal - a.requestTotal)
  const toolsAcc = {}
  for (const t of Array.isArray(toolsByDayList) ? toolsByDayList : []) {
    if (!t || !t.byDay) continue
    for (const k of dayKeys) {
      const day = t.byDay[k]
      if (!day) continue
      for (const [name, v] of Object.entries(day)) {
        const a = toolsAcc[name] || (toolsAcc[name] = { name, count: 0, durMs: 0 })
        a.count += v.count || 0
        a.durMs += v.durMs || 0
      }
    }
  }
  const tools = Object.values(toolsAcc).sort((a, b) => b.count - a.count)
  return { from: fromKey, to: toKey, days, totals, models, tools }
}

/** 工具调用按日折叠（/dashboard/range 的工具区数据核；防御式读 name/durationMs）。 */
export function foldToolsByDay(evts) {
  const byDay = {}
  for (const ev of Array.isArray(evts) ? evts : []) {
    const d = ev && ev.data
    if (!d || (ev.type !== 'tool/call' && ev.type !== 'tool/result')) continue
    if (typeof ev.time !== 'number' || !Number.isFinite(ev.time)) continue
    const name = typeof d.name === 'string' && d.name.trim() ? d.name.trim() : null
    if (!name) continue
    const day = dateKeyOf(ev.time)
    const b = byDay[day] || (byDay[day] = {})
    const a = b[name] || (b[name] = { count: 0, durMs: 0 })
    if (ev.type === 'tool/call') a.count += 1
    if (ev.type === 'tool/result' && typeof d.durationMs === 'number' && Number.isFinite(d.durationMs) && d.durationMs > 0) a.durMs += d.durationMs
  }
  return { byDay }
}
```

（Task 1 测试已覆盖 byDayRoute 冲回语义；本任务测试中 `summarizeRange` 的 models 断言不变——它现在从 byDayRoute 切片得到，行为一致但长会话跨日不再污染。）

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/dashboard.test.mjs`
Expected: PASS

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/dashboard.js test/dashboard.test.mjs
git commit -m "feat(host): Task2 buildTrend/summarizeRange/foldToolsByDay 纯函数（R3 趋势与区间聚合核）"
```

---

### Task 3: 宿主——evaluateAlerts 结构化（en 文案 follow-up）（R10）

**Files:**
- Modify: `src/host/dashboard.js`（`evaluateAlerts`，L103 起）
- Test: `test/dashboard.test.mjs`（追加；既有 zh 断言不动）

**Interfaces:**
- Produces: 警报项追加 `reached: number` 字段（触发时的总量值：day-warn/day-hit=dayTotal；milestone=k*unit）；`text` zh 字符串原样保留（29 契约用例不受影响）。客户端 Task 9 按 `kind + reached` 用 i18n 拼装。

- [ ] **Step 1: 写失败测试**

```js
  it('evaluateAlerts emits structured reached for i18n assembly', () => {
    const out = evaluateAlerts({ dayTotal: 0, grandTotal: 0 }, { dayTotal: 900, grandTotal: 0 }, { dayLimitTokens: 1000, milestoneUnit: 0 }, '2026-09-11')
    expect(out[0]).toMatchObject({ kind: 'day-warn', reached: 900 })
    const ms = evaluateAlerts({ dayTotal: 0, grandTotal: 0 }, { dayTotal: 0, grandTotal: 2_500_000 }, { dayLimitTokens: 0, milestoneUnit: 1_000_000 }, '2026-09-11')
    expect(ms[0]).toMatchObject({ kind: 'milestone', reached: 2_000_000 })
  })
```

- [ ] **Step 2: 确认失败** — Run: `npx vitest run test/dashboard.test.mjs -t 'structured reached'`，Expected FAIL。

- [ ] **Step 3: 实现**——三个 `out.push({...})` 各补 `reached`：

```js
      out.push({ id: 'day-warn' + day, kind: 'day-warn', reached: next.dayTotal, text: '今日 token 已用 80% · ' + formatTokens(next.dayTotal) })
      out.push({ id: 'day-hit' + day, kind: 'day-hit', reached: next.dayTotal, text: '今日 token 已达阈值 · ' + formatTokens(next.dayTotal) })
      out.push({ id: 'milestone:' + k, kind: 'milestone', reached: k * unit, text: '里程碑：累计 ' + formatTokens(k * unit) + ' token' })
```

- [ ] **Step 4: 全量通过** — `npx vitest run test/dashboard.test.mjs` PASS。

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/dashboard.js test/dashboard.test.mjs
git commit -m "feat(host): Task3 evaluateAlerts 结构化 reached 字段（en 文案 follow-up 数据面）"
```

---

### Task 4: 宿主引擎——增量缓存（P1）+ 快照扩展（usage.models/trend）+ 双扫描透传（R1/R2/R3）

**Files:**
- Modify: `src/host/state.js`（`buildDashboard` 重构为消费折叠条目；`createStateEngine.compute` 缓存化；导出新纯函数）
- Test: `test/state-dashboard.test.mjs`（追加）、`test/state.test.mjs`（追加缓存行为用例）

**Interfaces:**
- Consumes: Task 1 `foldUsage`（新返回形状）、Task 2 `buildTrend`；既有 `scanSession`、`scanInfo`、`estimateUserTokensByDay`、`summarize`、`summarizeStructured`、`evaluateAlerts`、`derivePaceTier`、`events5mCount`。
- Produces:
  - `aggregateFold(entries, cfg, now, alertPrev) → DashboardAgg`（新导出纯函数；`entries: FoldEntry[]`，`FoldEntry = { id, title, scan, folded: {usage, userEstByDay, toolsByDay} }`——scan 为 `scanInfo(evts)` 结果）——**既有 `buildDashboard(sessionsData, cfg, now, alertPrev)` 签名与返回保持不变**（内部改为：把 sessionsData 折叠成 FoldEntry 再调 aggregateFold），既有 state-dashboard 测试原样通过
  - `DashboardAgg.usage.models: RouteAgg[]`（当日 Top6，按 requestTotal 降序，元素含 `route/provider/model/requestTotal/cacheRead/outputTokens`）、`usage.tools: {name, count, durMs}[]`（当日 Top5 按次数）与 `usage.trend: { days: TrendDay[14], hours: TrendHour[24] }`
  - `usage.counts: { approval: number, running: number, done: number }`（引擎侧从 projects Map 填——MiniBar 计数口径 follow-up；aggregateFold 内不产 counts，引擎组装时并入）
  - 引擎 `compute()`：每 agentId 维护 `cache = { fp: string|null, scan, folded, recentTimes: number[] }`；`fp = evts.length + ':' + (末事件 seq ?? -1)`；fp 未变 → 复用 scan+folded；时间派生量（pace/approvals/summary 行）每轮从缓存重算
  - trend 节流：引擎侧 `trendHourKey` 记录，`hourKeyOf(now)` 变化或任一 fp 变化才重算 `buildTrend`，否则复用

- [ ] **Step 1: 写失败测试**（`test/state-dashboard.test.mjs` 追加）

```js
  // ---- v2.2 Task 4：models/trend 快照字段（经 buildDashboard 兼容入口） ----
  it('buildDashboard emits usage.models top6 and trend 14d/24h', () => {
    const t = new Date(2026, 8, 11, 15, 0).getTime()
    const evts = [
      { type: 'assistant/message', time: t, data: { turn: 0, step: 0, usage: { inputTokens: 100, outputTokens: 40, cacheReadTokens: 60 }, message: { source: { provider: 'p', model: 'm1' } } } },
    ]
    const dash = buildDashboard([{ id: 'a', title: 'A', events: evts }], {}, t, { dayTotal: 0, grandTotal: 0 })
    expect(dash.usage.models[0]).toMatchObject({ route: 'p/m1', requestTotal: 160 })
    expect(dash.usage.trend.days).toHaveLength(14)
    expect(dash.usage.trend.days[13].key).toBe('2026-09-11')
    expect(dash.usage.trend.hours).toHaveLength(24)
  })
```

（`test/state.test.mjs` 追加缓存行为用例——构造 `createStateEngine` 假 deps，同一 session 两轮 compute，第二轮替换 `getSession` 返回**更长**的事件数组，断言 dayUsage 反映新事件；再跑一轮相同数组，断言 `diag` 无重扫计数（引擎暴露 `stats.rescans` 供测试）：

```js
  it('engine caches per-session folds by fingerprint and rescans only on change', () => {
    let evts = [{ type: 'turn/start', seq: 1, time: 1000, data: { turn: 0 } }]
    const deps = {
      roots: () => [{ id: 'a' }],
      getSession: () => ({ snapshotEvents: () => evts }),
      getTitle: () => 'A',
      now: () => 2000,
    }
    const eng = createStateEngine(deps)
    eng.compute(); evts = evts.slice(); eng.compute()
    expect(eng.stats.rescans).toBe(1) // 第二轮 fp 相同 → 0 次新增重扫（首轮算 1）
    evts = [...evts, { type: 'turn/end', seq: 2, time: 1500, data: { turn: 0, reason: { kind: 'stop' } } }]
    eng.compute()
    expect(eng.stats.rescans).toBe(2) // fp 变化 → 重扫
  })
```

- [ ] **Step 2: 确认失败** — `npx vitest run test/state-dashboard.test.mjs test/state.test.mjs`，Expected FAIL（models/trend 未产出、stats 不存在）。

- [ ] **Step 3: 实现**——`src/host/state.js` 三处改造（完整替换逻辑如下）：

(a) import 行补 `buildTrend, foldToolsByDay`（来自 dashboard.js）。

(b) `buildDashboard` 重构为薄兼容层 + 新纯函数（放其原位置）：

```js
export function buildDashboard(sessionsData, cfg, now, alertPrev) {
  const entries = (Array.isArray(sessionsData) ? sessionsData : []).map((s) => {
    const evts = s && Array.isArray(s.events) ? s.events : []
    return {
      id: s && s.id !== undefined && s.id !== null ? s.id : '',
      title: (s && s.title) || (s && s.id) || '',
      scan: scanInfo(evts, 0),
      folded: foldEntry(evts),
    }
  })
  return aggregateFold(entries, cfg, now, alertPrev)
}

/** 单会话折叠（缓存值的最小单位）：usage 三维账 + 用户输入估算 + 工具按日。 */
export function foldEntry(evts) {
  return {
    usage: foldUsage(evts),
    userEstByDay: estimateUserTokensByDay(evts),
    toolsByDay: foldToolsByDay(evts),
  }
}
```

（注：`scanInfo(evts, 0)` 的 dayStart 影响的只是 lines 的「今日」判断——`scanInfo` 第二参为 dayStartMs；兼容层传 0 仅为纯度，pace/summary 不消费 lines 的当日性；若 `scanInfo` 对 dayStart 敏感的断言存在，则改为 `scanInfo(evts, new Date(now).setHours(0,0,0,0))` 并把 now 传入——以既有测试实际行为为准，二选一后在 commit 信息注明。）

(c) 新纯函数 `aggregateFold`（核心聚合，**逐字段复刻既有 buildDashboard 语义**——把现 buildDashboard 中 `for (const s of sessions)` 循环体改为读 `entry.scan.metrics`/`entry.folded.usage`，其余 pace/alerts/summary 段原样保留），差异点只有三处新增：

```js
  // ① models Top6（R2）：当日模型账从 byDayRoute[dayKey] 切片（Task 1 交叉维度，跨日不污染）
  const routeDay = {}
  for (const s of sessions) {
    const dayRoute = s.folded.usage.byDayRoute && s.folded.usage.byDayRoute[dayKey]
    if (!dayRoute) continue
    for (const [route, r] of Object.entries(dayRoute)) {
      const slash = route.indexOf('/')
      const a = routeDay[route] || (routeDay[route] = { route, provider: slash > 0 ? route.slice(0, slash) : '', model: slash > 0 ? route.slice(slash + 1) : '', requestTotal: 0, cacheRead: 0, outputTokens: 0 })
      a.requestTotal += r.inputTokens + r.cacheReadTokens
      a.cacheRead += r.cacheReadTokens
      a.outputTokens += r.outputTokens
    }
  }
  // ①b tools 当日 Top5（spec R5 工具区；L3 的 5h/7d 视图消费，缓存后零成本）
  const toolDay = {}
  for (const s of sessions) {
    const day = s.folded.toolsByDay && s.folded.toolsByDay[dayKey]
    if (!day) continue
    for (const [name, v] of Object.entries(day)) {
      const a = toolDay[name] || (toolDay[name] = { name, count: 0, durMs: 0 })
      a.count += v.count || 0
      a.durMs += v.durMs || 0
    }
  }
```

```js
  // ② trend（R3）
  trend: buildTrend(sessions.map((s) => s.folded.usage), nowMs)
  // ③ usage.models（当日 Top6）与 usage.tools（当日 Top5）
  models: Object.values(routeDay).sort((a, b) => b.requestTotal - a.requestTotal).slice(0, 6),
  tools: Object.values(toolDay).sort((a, b) => b.count - a.count).slice(0, 5),
```

返回对象在既有字段基础上追加 `usage.models`、`usage.tools`、`usage.trend`（usage 对象内）。

(d) 引擎缓存化——`createStateEngine` 内：

```js
  const foldCache = new Map() // agentId → { fp, scan, folded, recent: number[] }
  const stats = { rescans: 0 }
  let trendCache = { hourKey: null, value: null }
```

`compute()` 的会话循环改为：

```js
        const session = deps.getSession(a.id)
        if (session) {
          const evts = sessionEvents(session)
          const last = evts.length > 0 ? evts[evts.length - 1] : null
          const fp = evts.length + ':' + (last && typeof last.seq === 'number' ? last.seq : -1)
          let c = foldCache.get(a.id)
          if (!c || c.fp !== fp) {
            stats.rescans += 1
            const recent = []
            for (const e of evts) if (e && typeof e.time === 'number' && ACTIVITY_TYPES.has(e.type) && e.time > now - 300000) recent.push(e.time)
            c = { fp, scan: scanSession(session, deps.getTitle, dayStart.getTime(), evts), folded: foldEntry(evts), recent }
            foldCache.set(a.id, c)
          } else {
            // 5 分钟活跃窗滑动：只裁剪，不重扫
            while (c.recent.length > 0 && c.recent[0] <= now - 300000) c.recent.shift()
          }
          info = c.scan
          entry = { id: a.id, title: info.title || a.id, scan: c.scan, folded: c.folded, recentCount: c.recent.length }
        }
```

`aggregateFold` 的 `agg5m` 改为 `agg5m += entry.recentCount`（引擎路径）；`buildDashboard` 兼容层路径自行 `events5mCount`。trend 节流在引擎组装 dashState 后：

```js
    const hk = hourKeyOf(now)
    if (trendCache.hourKey !== hk || anyFpChanged) trendCache = { hourKey: hk, value: dashState.usage.trend }
    else dashState.usage.trend = trendCache.value
```

（`anyFpChanged` = 本轮 stats.rescans 有增量；engine 对外追加 `stats` getter；`derive`/`list`/卡片路径与 ageLabel 全部改为读 `c.scan` 派生，**不再每轮调 scanSession**——这是 P1 的核心。）`approvals` 的 `waitMin` 来自 scan.pendingList 时间戳每轮重算（时间派生量，在 aggregateFold 内已如此）。

- [ ] **Step 4: 全量测试通过**（state-dashboard 既有用例 + 新用例；`npx vitest run test/`）

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/state.js test/state-dashboard.test.mjs test/state.test.mjs
git commit -m "feat(host): Task4 引擎指纹增量缓存(P1)+双扫描透传+usage.models/tools/trend 快照(R2/R3)"
```

---

### Task 5: 宿主——P2 宠物目录 mtime 缓存 + Config 3 新键（R1/R6/R8）

**Files:**
- Modify: `src/host/index.js`（snapshotExtra 用缓存；Config 追加）
- Test: `test/host-index.test.mjs`（追加缓存行为用例）

**Interfaces:**
- Produces:
  - Config 新键：`dashboardSidebarEntry: z.boolean().default(false)`、`exportQuote: z.string().default('')`、`exportPose: z.string().default('random')`（客户端 Task 10/13/14 消费同名键）
  - `listPetsCached()`/`loadManifestCached(dir, opts)`（index.js 内部函数；mtime 指纹）

- [ ] **Step 1: 写失败测试**

```js
  it('listPetsCached rescans only when petsRoot mtime changes', async () => {
    const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'foxbell-p2-'))
    const calls = { n: 0 }
    const origStat = fs.statSync
    // 通过 monkeypatch listPets 不可行（ESM 直引）——改为断言两次调用返回同引用（缓存命中）
    const a = listPetsCachedForTest(dir)
    const b = listPetsCachedForTest(dir)
    expect(a).toBe(b) // 同一数组引用 = 未重扫
    fs.rmSync(dir, { recursive: true, force: true })
  })
```

（实现侧从 index.js `export const __testables = { listPetsCached }` 形式导出 `listPetsCachedForTest`；测试 import 之。）

- [ ] **Step 2: 确认失败** — `npx vitest run test/host-index.test.mjs -t 'listPetsCached'` FAIL。

- [ ] **Step 3: 实现**——index.js：

```js
  // ---- v2.2 P2：宠物目录/清单 mtime 指纹缓存（spec R1）----
  const petsDirCache = { root: null, mtimeMs: -1, pets: [] }
  const listPetsCached = (root) => {
    if (petsDirCache.root !== root) { petsDirCache.root = root; petsDirCache.mtimeMs = -1 }
    let mtime = -1
    try { mtime = fs.statSync(root).mtimeMs } catch { return petsDirCache.mtimeMs === -1 ? [] : petsDirCache.pets }
    if (mtime !== petsDirCache.mtimeMs) {
      petsDirCache.mtimeMs = mtime
      petsDirCache.pets = listPets(root)
    }
    return petsDirCache.pets
  }
  const manifestCache = new Map() // dir → { mtimeMs, manifest }
  const loadManifestCached = (dir, opts) => {
    let mtime = -1
    try { mtime = fs.statSync(path.join(dir, 'pet.json')).mtimeMs } catch { /* 缺失走原 loadManifest 报错路径 */ }
    const c = manifestCache.get(dir)
    if (c && c.mtimeMs === mtime) return c.manifest
    const m = loadManifest(dir, opts)
    manifestCache.set(dir, { mtimeMs: mtime, manifest: m })
    return m
  }
```

`snapshotExtra` 内 `loadManifest(...)` 换 `loadManifestCached(...)`、`listPets(PETS_ROOT)` 两处（快照与启动 log 行）换 `listPetsCached(PETS_ROOT)`。宠物导入/删除路由（zip 落盘、宠物删除）在写操作后调 `manifestCache.delete(dir)` + 主动 `fs.statSync(PETS_ROOT)` 触发失效（目录 mtime 本身会变，删除缓存行仅为即时性保险）。Config 追加三键（放 ttsEnabled 之后，注释 `// v2.2 用量看板 3 项（R6 侧栏入口/R8 导出评语与姿态；客户端消费）`）。

- [ ] **Step 4: 全量测试** — `npx vitest run test/host-index.test.mjs` PASS。

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/index.js test/host-index.test.mjs
git commit -m "perf(host): Task5 宠物目录/清单 mtime 缓存(P2)+Config 新增 dashboardSidebarEntry/exportQuote/exportPose"
```

---

### Task 6: 宿主路由——/state ?since 短路（P3）+ /dashboard/range（R3）+ /sounds 静态（R7 前置）

**Files:**
- Modify: `src/host/routes.js`（/state handler 扩展；追加两条路由）
- Modify: `src/host/index.js`（snapshotExtra 支持 unchanged 形态；env 增 soundsDir）
- Test: `test/routes.test.mjs`（追加）

**Interfaces:**
- Consumes: Task 4 引擎（`engine.stats`、foldCache 需暴露只读聚合入口——index.js 侧 `stateEngine.rangeSummary(fromKey, toKey)`（Task 4 已有引擎缓存，此方法遍历 foldCache 调 `summarizeRange`））；Task 2 `summarizeRange`。
- Produces:
  - `GET /state?since=<seq>`：`engine.contentRev()` 与 since 相等且非首轮 → `{ seq, unchanged: true, ages: string[] }`（ages 与全量 projects 顺序一致）；不等 → 全量快照（含 `seq: contentRev`）
  - `engine.contentRev()`：本轮 compute 后的变更令牌（实现：`[list() 各行 status/unread/title/lines].join('|') + '#' + queue.length + '#' + dashboard.dayTotal + '#' + dashboard.grandTotal + '#' + pace.tier + '#' + alerts ids join` 的简单字符串；age 不参与）
  - `GET /dashboard/range?from=YYYY-MM-DD&to=YYYY-MM-DD`：`{ from, to, days, totals, models, tools }`（Task 2 形状；跨度 >31 天 → PetError 参数错；非法日期同）
  - `GET /sounds/<file>`（kind:'prefix'）：serve `assets/sounds/<file>`（file 白名单 `^[a-z0-9-]+\.wav$`，防穿越）

- [ ] **Step 1: 写失败测试**（routes.test.mjs 追加，沿用该文件既有 fake env 手法）

```js
  it('/state with unchanged since returns tiny payload with ages', async () => {
    const env = makeEnv() // 既有构造器：含 stateEngine/snapshotExtra 桩
    // 桩：contentRev 恒 'R1'，snapshotExtra 返回 { seq: 'R1', projects: [{ id: 'a', age: '3s' }] }
    const res = await get(env, '/dyn-pet-foxbell/state?since=R1')
    expect(res.unchanged).toBe(true)
    expect(res.ages).toEqual(['3s'])
    expect(Object.keys(res).sort()).toEqual(['ages', 'seq', 'unchanged'])
  })

  it('/dashboard/range returns summarized range and rejects >31d', async () => {
    const env = makeEnv({ rangeSummary: (f, t) => ({ from: f, to: t, days: [], totals: {}, models: [], tools: [] }) })
    const ok = await get(env, '/dyn-pet-foxbell/dashboard/range?from=2026-09-01&to=2026-09-11')
    expect(ok.from).toBe('2026-09-01')
    const bad = await getStatus(env, '/dyn-pet-foxbell/dashboard/range?from=2026-08-01&to=2026-09-11')
    expect(bad).toBe(400)
  })

  it('/sounds serves whitelisted wav only', async () => {
    const ok = await getStatus(env2, '/dyn-pet-foxbell/sounds/alert-1.wav')
    expect(ok).toBe(200)
    const bad = await getStatus(env2, '/dyn-pet-foxbell/sounds/..%2Fpet.json')
    expect(bad).toBe(400)
  })
```

（`makeEnv`/`get`/`getStatus` 为该文件既有测试助手名，按实际文件调整命名；env2 指带真实 assets 目录的 env。）

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

/state handler（既有 handler 内、`json(res, env.snapshotExtra(hint))` 之前插入）：

```js
        let since = null
        try { since = new URL(req.url || '/', 'http://x').searchParams.get('since') } catch { /* ignore */ }
        const rev = env.stateEngine.contentRev ? env.stateEngine.contentRev() : null
        if (since !== null && rev !== null && since === rev) {
          json(res, { seq: rev, unchanged: true, ages: env.stateEngine.list().map((p) => p.age || '') })
          return
        }
        const body = env.snapshotExtra(hint)
        if (rev !== null) body.seq = rev
```

（注意：全量快照的 `seq` 字段从 completions 序号改为 contentRev——客户端 8 之前用 seq 判 completions 增量？查证：store.ts 只用 `snap.seq` 做有效性检查（`typeof snap.seq !== 'number'`）——contentRev 是 string 会破坏该检查。**改为**：全量快照保留既有数值 `seq`（engine.nextSeq()），另加顶层 `rev: string`；unchanged 形态 `{ rev, unchanged: true, ages, seq: <nextSeq> }`。Task 8 客户端用 `rev` 做 since 参数。）

/dashboard/range（registerRoutes env 增 `stateEngine.rangeSummary`，由 index.js 注入）：

```js
  reg({
    kind: 'exact',
    path: `${ROUTE_PREFIX}/dashboard/range`,
    handler(req, res) {
      try {
        const u = new URL(req.url || '/', 'http://x')
        const from = u.searchParams.get('from') || ''
        const to = u.searchParams.get('to') || ''
        if (!/^\d{4}-\d{2}-\d{2}$/.test(from) || !/^\d{4}-\d{2}-\d{2}$/.test(to) || from > to) {
          throw new PetError('bad-request', 'from/to 需为 YYYY-MM-DD 且 from<=to').with('from', from).with('to', to)
        }
        const days = Math.round((new Date(to + 'T00:00:00').getTime() - new Date(from + 'T00:00:00').getTime()) / 86400000) + 1
        if (days > 31) throw new PetError('bad-request', '跨度最长 31 天').with('days', String(days))
        json(res, env.stateEngine.rangeSummary(from, to))
      } catch (e) { errJson(res, e) }
    },
  })
```

/sounds（env 增 `pkgDir`，既有）：

```js
  reg({
    kind: 'prefix',
    path: `${ROUTE_PREFIX}/sounds/`,
    handler(req, res) {
      try {
        const rel = decodeURIComponent((req.url || '').split(`${ROUTE_PREFIX}/sounds/`)[1] || '')
        if (!/^[a-z0-9-]+\.wav$/.test(rel)) throw new PetError('bad-request', '非法音效文件名').with('file', rel)
        const file = path.join(env.pkgDir, 'assets', 'sounds', rel)
        if (!fs.existsSync(file)) throw new PetError('not-found', '音效不存在').with('file', rel)
        res.writeHead(200, { 'content-type': 'audio/wav', 'cache-control': 'public, max-age=86400' })
        fs.createReadStream(file).pipe(res)
      } catch (e) { errJson(res, e) }
    },
  })
```

引擎侧（state.js，Task 4 文件的延续）：

```js
  const contentRev = () => {
    const rows = list().map((p) => `${p.status}:${p.unread?1:0}:${p.title}:${(p.lines||[]).join('/')}`).join('|')
    const d = dashState || {}
    return [rows, queue.length, d.usage ? d.usage.day.requestTotal : 0, d.usage ? d.usage.grandTotal : 0,
      d.pace ? d.pace.tier : '', (d.alerts || []).map((a) => a.id).join(',')].join('#')
  }
  const rangeSummary = (fromKey, toKey) => {
    const folds = [], tools = [], ests = []
    for (const c of foldCache.values()) { folds.push(c.folded.usage); tools.push(c.folded.toolsByDay); ests.push(c.folded.userEstByDay) }
    return summarizeRange(folds, tools, ests, fromKey, toKey)
  }
```

return 对象追加 `contentRev, rangeSummary, stats: { get rescans() { return stats.rescans } }`。

- [ ] **Step 4: 全量测试** — `npx vitest run test/routes.test.mjs test/state.test.mjs` PASS。

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/host/routes.js src/host/index.js src/host/state.js test/routes.test.mjs
git commit -m "feat(host): Task6 /state since短路(P3)+/dashboard/range(R3)+/sounds静态路由(R7前置)"
```

---

### Task 7: 版本与守卫——2.2.0 / engines.dsh / 默认音效资产生成（R7 前置/R10）

**Files:**
- Modify: `package.json`（version 2.2.0）、`dsh.plugin.json`（version + engines）
- Modify: `scripts/validate.mjs`（版本断言与豁免注释同步）
- Create: `assets/sounds/alert-1.wav`、`alert-2.wav`、`alert-3.wav`（脚本生成，非采样——许可洁净）
- Create: `scripts/gen-alert-sounds.mjs`（一次性生成脚本，留仓可复现）

**Interfaces:**
- Produces: 三个 0.35s 44.1kHz 单声道 PCM16 WAV（两音上行 chime，基频 660/880/990 变体，指数衰减包络）；`/sounds/` 路由（Task 6）可服务。spec R7「音源 plan 阶段核实」结论：**不取 MAM 二进制**（上游许可未核实，避免侵权风险），以脚本合成同风格短音效替代——此裁定回填 spec R7 一句话（同 commit 改 spec）。

- [ ] **Step 1: 写生成脚本** `scripts/gen-alert-sounds.mjs`

```js
// 一次性：生成 assets/sounds/alert-{1,2,3}.wav（两音上行 chime，PCM16 44.1kHz 单声道）。
// 用法：node scripts/gen-alert-sounds.mjs
import { writeFileSync, mkdirSync } from 'node:fs'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'

const SR = 44100
const NOTE = (f, t0, dur) => ({ f, t0, dur })
const RECIPES = [
  [NOTE(659.25, 0.00, 0.16), NOTE(987.77, 0.14, 0.20)], // E5→B5
  [NOTE(587.33, 0.00, 0.16), NOTE(880.00, 0.14, 0.20)], // D5→A5
  [NOTE(523.25, 0.00, 0.14), NOTE(783.99, 0.12, 0.16), NOTE(1046.50, 0.26, 0.16)], // C5→G5→C6
]
const render = (notes) => {
  const total = Math.max(...notes.map((n) => n.t0 + n.dur))
  const buf = new Float32Array(Math.ceil(total * SR))
  for (const { f, t0, dur } of notes) {
    const s0 = Math.floor(t0 * SR), n = Math.floor(dur * SR)
    for (let i = 0; i < n; i++) {
      const t = i / SR
      const env = Math.exp(-4.5 * t / dur) * (1 - Math.exp(-0.0008 * SR * t))
      buf[s0 + i] += 0.42 * env * Math.sin(2 * Math.PI * f * t)
    }
  }
  return buf
}
const wav = (pcm) => {
  const bytes = Buffer.alloc(44 + pcm.length * 2)
  bytes.write('RIFF', 0); bytes.writeUInt32LE(36 + pcm.length * 2, 4); bytes.write('WAVEfmt ', 8)
  bytes.writeUInt32LE(16, 16); bytes.writeUInt16LE(1, 20); bytes.writeUInt16LE(1, 22)
  bytes.writeUInt32LE(SR, 24); bytes.writeUInt32LE(SR * 2, 28); bytes.writeUInt16LE(2, 32); bytes.writeUInt16LE(16, 34)
  bytes.write('data', 36); bytes.writeUInt32LE(pcm.length * 2, 40)
  for (let i = 0; i < pcm.length; i++) bytes.writeInt16LE(Math.max(-1, Math.min(1, pcm[i])) * 32767 | 0, 44 + i * 2)
  return bytes
}
const outDir = join(dirname(fileURLToPath(import.meta.url)), '..', 'assets', 'sounds')
mkdirSync(outDir, { recursive: true })
RECIPES.forEach((r, i) => writeFileSync(join(outDir, `alert-${i + 1}.wav`), wav(render(r))))
console.log(`generated ${RECIPES.length} sounds in ${outDir}`)
```

- [ ] **Step 2: 运行生成并验证**

```bash
node scripts/gen-alert-sounds.mjs && ls -la assets/sounds/ && file assets/sounds/alert-1.wav
```
Expected: 三个 wav，`file` 输出 `WAVE audio`，各 ~30-35KB。

- [ ] **Step 3: 版本与 engines**

`package.json` `"version": "2.2.0"`；`dsh.plugin.json` `"version": "2.2.0"`、`"engines": { "dsh": ">=0.1.2-rc.1" }`。`scripts/validate.mjs` 中版本相关断言/注释同步（搜 `2.1.0` 全部出现处，按该文件语义更新；若存在 dsh.plugin.json 版本一致性检查则确认通过）。

- [ ] **Step 4: 四绿验证**

```bash
npm test && npm run typecheck && npm run build && npm run validate
```
Expected: 全绿（validate 若因版本冻结断言失败，按其注释指引更新期望值——这与 v2.1 Task 10 同款操作）。

- [ ] **Step 5: spec 回填 + 提交**

spec R7 音源句改为：「音源裁定（plan 阶段）：MAM 二进制许可未核实，改用仓内脚本合成同风格 chime（`scripts/gen-alert-sounds.mjs`），许可洁净」。

```bash
git add package.json dsh.plugin.json scripts/validate.mjs scripts/gen-alert-sounds.mjs assets/sounds docs/superpowers/specs/2026-09-11-v2.2-usage-dashboard-design.md
git commit -m "chore: Task7 版本2.2.0+engines对齐+合成默认音效3枚(R7音源裁定回填spec)"
```

---

### Task 8: 客户端——api 类型扩展 + store since 短路（P3）+ 不可见降频（P4）（R1）

**Files:**
- Modify: `src/client/api.ts`（类型）、`src/client/store.ts`（pollOnce/interval）
- Test: `test/client-logic.test.ts`（追加）

**Interfaces:**
- Consumes: Task 6 的 `/state?since` 响应形态（`{ rev, unchanged: true, ages, seq }`）与全量快照新顶层字段 `rev: string`。
- Produces:
  - api.ts：`TrendDay/TrendHour/RouteUsage/RangeSummary` 类型；`UsageSnapshot` 增 `models: RouteUsage[]`、`trend: { days: TrendDay[]; hours: TrendHour[] }`；`DashboardAlert` 增 `reached?: number`；`StateSnapshot` 增 `rev?: string`；新 `apiGetRange(from, to): Promise<RangeSummary>`
  - store.ts：`POLL_INTERVAL_HIDDEN_MS = 5000`；pollOnce 带 `&since=`、unchanged 时仅合并 `ages`；`visibilitychange` 切换 interval

- [ ] **Step 1: 写失败测试**（client-logic.test.ts 追加）

```ts
import { mergeUnchanged } from "../src/client/store";

describe("v2.2 P3 unchanged merge", () => {
  it("merges ages into current snapshot and keeps rest", () => {
    const snap = { seq: 5, rev: "R1", projects: [{ id: "a", age: "1s" }, { id: "b", age: "2s" }] } as any;
    const out = mergeUnchanged(snap, { rev: "R1", unchanged: true, ages: ["9s", "8s"], seq: 5 });
    expect(out.projects[0].age).toBe("9s");
    expect(out.projects[1].age).toBe("8s");
    expect(out.rev).toBe("R1");
  });
});
```

- [ ] **Step 2: 确认失败** — `npx vitest run test/client-logic.test.ts -t 'unchanged'` FAIL。

- [ ] **Step 3: 实现**

api.ts 类型追加（放在 DashboardSnapshot 区块）：

```ts
export interface RouteUsage { route: string; provider: string; model: string; requestTotal: number; cacheRead: number; outputTokens: number }
export interface TrendDay { key: string; dayTotal: number; requestTotal: number; cacheRead: number; outputTokens: number; hitPct: number; requestCount: number }
export interface TrendHour { key: string; dayTotal: number; requestTotal: number; requestCount: number }
export interface RangeSummary { from: string; to: string; days: TrendDay[]; totals: { requestTotal: number; cacheRead: number; outputTokens: number; userEst: number; hitPct: number; requestCount: number }; models: RouteUsage[]; tools: { name: string; count: number; durMs: number }[] }

export async function apiGetRange(from: string, to: string): Promise<RangeSummary> {
  return apiGet<RangeSummary>(`/dashboard/range?from=${from}&to=${to}`);
}
```

`UsageSnapshot` 增 `models?: RouteUsage[]`、`tools?: { name: string; count: number; durMs: number }[]`（均可空防御——容错旧宿主）、`trend?: { days: TrendDay[]; hours: TrendHour[] }`；`StateSnapshot` 增 `rev?: string`；`DashboardAlert` 增 `reached?: number`。

store.ts：

```ts
export const POLL_INTERVAL_HIDDEN_MS = 5000;

/** P3：unchanged 响应仅合并 ages（行序与全量 projects 一致），其余沿用旧快照。导出供测试。 */
export function mergeUnchanged(cur: StateSnapshot, res: { rev: string; unchanged: true; ages: string[]; seq: number }): StateSnapshot {
  const projects = (cur.projects || []).map((p, i) => ({ ...p, age: (res.ages && res.ages[i]) || p.age }));
  return { ...cur, seq: res.seq, rev: res.rev, projects };
}

let lastRev: string | null = null;

export async function pollOnce(): Promise<void> {
  let snap: StateSnapshot;
  try {
    const hint = cfgStore.getSnapshot().activePetId;
    const since = lastRev !== null ? `&since=${encodeURIComponent(lastRev)}` : "";
    snap = await apiGet<StateSnapshot>(`/state?pet=${encodeURIComponent(hint)}${since}`);
  } catch { return; }
  if (!snap || typeof snap.seq !== "number") return;
  if ((snap as { unchanged?: boolean }).unchanged === true) {
    state.snapshot = mergeUnchanged(state.snapshot!, snap as never);
    appStore.emit();
    return;
  }
  lastRev = typeof snap.rev === "string" ? snap.rev : null;
  state.snapshot = snap;
  // ……以下为既有热替换逻辑原样保留
```

start/stop 改为可变间隔：

```ts
const schedule = () => {
  if (pollTimer !== null) { clearTimeout(pollTimer); pollTimer = null; }
  const hidden = typeof document !== "undefined" && document.hidden;
  void pollOnce();
  pollTimer = setTimeout(function tick() {
    void pollOnce().finally(() => {
      const h = typeof document !== "undefined" && document.hidden;
      pollTimer = setTimeout(tick, h ? POLL_INTERVAL_HIDDEN_MS : POLL_INTERVAL_MS);
    });
  }, typeof document !== "undefined" && document.hidden ? POLL_INTERVAL_HIDDEN_MS : POLL_INTERVAL_MS);
};
// start() 调 schedule()；stop() 清 timeout；模块加载处（index.tsx 已有 start 调用）不变。
// visibilitychange 立刻触发一次 schedule 以快速回到 1.5s（在 index.tsx 注册，防测试环境无 document）。
```

- [ ] **Step 4: 全量测试**（client-logic + store-hotswap）PASS。

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/api.ts src/client/store.ts test/client-logic.test.ts
git commit -m "feat(client): Task8 类型扩展+since短路客户端(P3)+不可见5s降频(P4)"
```

---

### Task 9: 客户端——usage 组退役 + 默认音效链 + 警报 i18n（R7/R10 follow-up）

**Files:**
- Modify: `src/client/voices.ts`（GROUPS 去 'usage'）、`src/client/Pet.tsx`（警报段改造）、`src/client/i18n.ts`（警报与看板新键 + t() 参数插值）、`src/client/store.ts`（默认音效播放器）
- Test: `test/client-logic.test.ts`（追加）

**Interfaces:**
- Consumes: Task 3 `alerts[].reached`；Task 7 `/sounds/alert-N.wav`。
- Produces:
  - `t(key, params?)` 插值（`{v}` 占位）
  - `playDefaultAlertSound()`（store.ts 导出；Audio 池轮换 3 枚，失败静默）
  - 警报链（Pet.tsx）：`runtime.hasVoice === true（四组配齐）→ voicePlayer.pick('general') 命中即播` / 否则 `playDefaultAlertSound()`；TTS 段**删除**（spec 默认裁定②：TTS 不参与看板音效链；ttsEnabled 键保留给台词朗读用途，本任务不动其消费点）

- [ ] **Step 1: 写失败测试**

```ts
import { alertText } from "../src/client/i18n";

describe("v2.2 alert i18n", () => {
  it("builds zh/en alert text from kind+reached", () => {
    expect(alertText("zh", "day-warn", 1234567)).toContain("80%");
    expect(alertText("en", "day-hit", 100)).toContain("threshold");
    expect(alertText("zh", "milestone", 2000000)).toContain("200万");
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

i18n.ts——t() 加参数插值（保留无参调用兼容）：

```ts
export function t(key: string, params?: Record<string, string>): string {
  let s = lookup(key); // 既有查表逻辑
  if (params) for (const [k, v] of Object.entries(params)) s = s.split(`{${k}}`).join(v);
  return s;
}
```

字典追加（zh/en 两表）：

```ts
zh: { "dash.alert.dayWarn": "今日 token 已用 80% · {v}", "dash.alert.dayHit": "今日 token 已达阈值 · {v}", "dash.alert.milestone": "里程碑：累计 {v} token" }
en: { "dash.alert.dayWarn": "Daily tokens 80% used · {v}", "dash.alert.dayHit": "Daily token limit reached · {v}", "dash.alert.milestone": "Milestone: {v} tokens total" }

export function alertText(lang: "zh" | "en", kind: string, reached: number): string {
  const key = kind === "day-warn" ? "dash.alert.dayWarn" : kind === "day-hit" ? "dash.alert.dayHit" : "dash.alert.milestone";
  return t(key, { v: fmtTokens(reached) });
}
```

（alertText 消费 `a.reached`；`reached` 缺失时回退 `a.text`——防御旧宿主。）

store.ts 追加：

```ts
// v2.2 R7：看板默认音效（合成 chime ×3 轮换；失败静默——Audio autoplay 受限时不抛）
const ALERT_SOUNDS = [1, 2, 3].map((n) => `${ROUTE_PREFIX}/sounds/alert-${n}.wav`);
let alertIdx = 0;
export function playDefaultAlertSound(): void {
  try {
    const a = new Audio(ALERT_SOUNDS[alertIdx++ % ALERT_SOUNDS.length]);
    a.volume = 0.6;
    a.play().catch(() => {});
  } catch { /* ignore */ }
}
```

voices.ts：`const GROUPS: VoiceGroup[] = ["general", "approval", "done", "error"];`（删 'usage'；VoiceGroup 类型同步收缩；文件头注释更新 v2.2 裁决引 spec R7）。

Pet.tsx 警报段（L316 三优先级块整段替换）：

```tsx
        playTransient("jumping", 1600);
        if (cfgRef.current.muted) continue;
        // v2.2 R7 音效链：四组配齐 → general 组宠物语音；否则内置合成 chime（spec：语音组 > 默认音效；TTS 不参与）
        if (runtimeRef.current.hasVoice) {
          const v = voicePlayer.pick("general");
          if (v) {
            voicePlayer.play(v, {
              muted: cfgRef.current.muted,
              onSubtitle: a.kind === "milestone"
                ? (name, ms) => { if (!cfgRef.current.muted && cfgRef.current.talkative && runtimeRef.current.hasSubtitle) showBubble(alertText(curLang(), a.kind, a.reached ?? 0) || a.text, ms); }
                : undefined,
            });
            continue;
          }
        }
        playDefaultAlertSound();
```

举牌/气泡文案行同步：`setSign(alertText(curLang(), a.kind, a.reached ?? 0))`（`curLang()` 为 i18n 既有语言 getter 的实际名——按 i18n.ts 实际导出调整，如 `lang()`）。import 行补 `alertText`、`playDefaultAlertSound`。

- [ ] **Step 4: 全量测试 + typecheck（VoiceGroup 收缩会暴露 'usage' 残留引用——逐一改到新链）**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/voices.ts src/client/Pet.tsx src/client/i18n.ts src/client/store.ts test/client-logic.test.ts
git commit -m "feat(client): Task9 MAM四组音效链+默认chime+警报i18n结构化拼装(usage组退役)"
```

---

### Task 10: 客户端——MiniBar 钻取按钮 + 计数口径（R6/R10 follow-up）

**Files:**
- Modify: `src/client/MiniBar.tsx`、`src/client/styles.ts`、`src/client/Pet.tsx`（接线）
- Test: `test/client-logic.test.ts`（追加纯逻辑：countsFromDash 回退）

**Interfaces:**
- Consumes: Task 8 `dash.usage.counts?`（宿主 Task 4 引擎组装——本任务消费，缺省回退本地 cards 计数）；Task 9 之后的 Pet.tsx 结构。
- Produces: `MiniBar` 新 prop `onDetail?: () => void`（提供时渲染尾部按钮行）；`.dyn-pet-mini-detail` CSS（pointer-events:auto）；`countsFromDash(dash, cards)` 纯函数（测试钉住回退语义）。

- [ ] **Step 1: 写失败测试**

```ts
import { countsFromDash } from "../src/client/MiniBar";

describe("v2.2 MiniBar counts", () => {
  it("prefers host counts and falls back to local cards", () => {
    const cards = [{ status: "approval" }, { status: "running" }, { status: "running" }, { status: "done" }] as never[];
    expect(countsFromDash(null, cards)).toEqual({ approval: 1, running: 2, done: 1 });
    expect(countsFromDash({ counts: { approval: 3, running: 0, done: 2 } } as never, cards)).toEqual({ approval: 3, running: 0, done: 2 });
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

MiniBar.tsx：抽出计数逻辑为导出纯函数 `countsFromDash(dash, cards)`（宿主 counts 存在且三键为数字 → 用之；否则本地 cards 计数——现逻辑函数化）；props 增 `onDetail?: () => void`；JSX 尾部（countsText 行后、sessionReq 行后）追加：

```tsx
      {props.onDetail ? (
        <div className="dyn-pet-mini-detail" onClick={(e) => { e.stopPropagation(); props.onDetail!(); }}>
          {t("dash.detail")} »
        </div>
      ) : null}
```

styles.ts 追加：

```ts
export const MINI_DETAIL_CSS = `
.dyn-pet-mini-detail { pointer-events: auto; cursor: pointer; margin-top: 2px; align-self: flex-end; font-size: 11px; color: #8a6d3b; opacity: .85; }
.dyn-pet-mini-detail:hover { opacity: 1; text-decoration: underline; }
`;
```

（挂进既有 mini 样式串；容器 `.dyn-pet-mini` 保持 `pointer-events:none` 不动——仅此子元素开 auto，区域外点击照旧穿透，铁律修订的最小实现。）i18n 增 `dash.detail`：zh「详情」/ en「Details」。

Pet.tsx：MiniBar 挂载点两处（hover/manual）传 `onDetail={() => openBoard("manual")}`（openBoard 为既有黑板打开函数实际名——Pet.tsx 内黑板 state 设置器；钻取目标=L2 黑板，spec R6 主链）。

- [ ] **Step 4: 全量测试 + typecheck。**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/MiniBar.tsx src/client/styles.ts src/client/Pet.tsx src/client/i18n.ts test/client-logic.test.ts
git commit -m "feat(client): Task10 迷你条唯一钻取按钮+宿主全量计数口径(R6/R10)"
```

---

### Task 11: 客户端——黑板加料 + 锚定跟随 + 迷你条互斥（R4/R10）

**Files:**
- Create: `src/client/TrendChart.tsx`（共用 SVG 趋势组件）
- Modify: `src/client/Board.tsx`、`src/client/boardrows.ts`、`src/client/Pet.tsx`（布局仲裁）、`src/client/styles.ts`、`src/client/i18n.ts`
- Test: `test/client-logic.test.ts`（追加 sparkline 数据切片纯函数）

**Interfaces:**
- Consumes: Task 8 `dash.usage.trend.days`、`dash.usage.models`。
- Produces:
  - `TrendChart.tsx`：`TrendChart({ points: {label: string; value: number}[], width, height, accent?: boolean })`（SVG：渐变折线+面积+数据点+峰值文案由调用方拼；**无交互态**——tooltip 是 L3 增强版另用 `TrendChartInteractive` 同文件导出，hover/焦点 tooltip，Board 用基础版）
  - `lastN(days: TrendDay[], n: number)`（纯函数：末 n 日切片含 label 'M/D' 短日期）
  - Board 新 prop `onOpenPanel?: () => void`（入口行）；黑板行序：既有行 → sparkline 行 → 模型 Top3 行 → 入口行
  - Pet.tsx：`boardOpen` 真值期间不渲染 MiniBar（互斥顶替）；黑板定位从 fixed 右下改锚定宠物（`posRef` 当前 x → 左侧优先 `left = petX - boardW - 12`，越界翻右侧；y 对齐宠物顶部），三档 scale 沿用 boardLayer transform

- [ ] **Step 1: 写失败测试**

```ts
import { lastN } from "../src/client/TrendChart";

describe("v2.2 TrendChart data", () => {
  it("lastN slices tail with short labels and zero-fill to n", () => {
    const days = Array.from({ length: 14 }, (_, i) => ({ key: `2026-08-${29 + i}`, dayTotal: i }));
    const out = lastN(days as never, 7);
    expect(out).toHaveLength(7);
    expect(out[6]).toMatchObject({ label: "9/11", value: 13 });
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

TrendChart.tsx（完整新文件）：

```tsx
// TrendChart.tsx — v2.2 共用趋势图（spec R5 复刻 Codex++ 形态：三横网格线+蓝紫渐变折线/面积+数据点）。
// 基础版无交互（Board sparkline 行）；Interactive 版带 hover tooltip（L3 大看板专用）。
const LINE_GRAD = ["#3BA7FF", "#8D6BFF"];

export function lastN(days: { key: string; dayTotal: number; hitPct?: number }[], n: number): { label: string; value: number; key: string; hitPct?: number }[] {
  const src = Array.isArray(days) ? days : [];
  const tail = src.slice(Math.max(0, src.length - n));
  const out = tail.map((d) => {
    const [, m, day] = d.key.split("-");
    return { key: d.key, label: `${Number(m)}/${Number(day)}`, value: d.dayTotal || 0, hitPct: d.hitPct };
  });
  while (out.length < n) out.unshift({ key: "", label: "", value: 0 });
  return out;
}

function pathOf(pts: { x: number; y: number }[], smooth: boolean): string {
  if (pts.length === 0) return "";
  if (!smooth || pts.length < 3) return "M " + pts.map((p) => `${p.x.toFixed(1)} ${p.y.toFixed(1)}`).join(" L ");
  let d = `M ${pts[0].x.toFixed(1)} ${pts[0].y.toFixed(1)}`;
  for (let i = 1; i < pts.length; i++) {
    const p0 = pts[i - 1], p1 = pts[i];
    const mx = (p0.x + p1.x) / 2;
    d += ` C ${mx.toFixed(1)} ${p0.y.toFixed(1)} ${mx.toFixed(1)} ${p1.y.toFixed(1)} ${p1.x.toFixed(1)} ${p1.y.toFixed(1)}`;
  }
  return d;
}

export function TrendChart(props: { points: { label: string; value: number }[]; width: number; height: number; showLabels?: boolean }): ReactElement | null {
  const { points, width, height } = props;
  if (!points || points.length < 2) return null;
  const padL = 6, padR = 6, padT = 8, padB = props.showLabels ? 16 : 6;
  const max = Math.max(1, ...points.map((p) => p.value));
  const innerW = width - padL - padR, innerH = height - padT - padB;
  const pts = points.map((p, i) => ({
    x: padL + (innerW * i) / (points.length - 1),
    y: padT + innerH * (1 - p.value / max),
  }));
  const line = pathOf(pts, true);
  const baseline = (padT + innerH).toFixed(1);
  const area = `${line} L ${pts[pts.length - 1].x.toFixed(1)} ${baseline} L ${pts[0].x.toFixed(1)} ${baseline} Z`;
  const gid = "dyn-pet-trend-g";
  return (
    <svg viewBox={`0 0 ${width} ${height}`} style={{ width: "100%", height: "auto", display: "block" }} role="img">
      <defs>
        <linearGradient id={`${gid}-l`} x1="0" y1="0" x2={width} y2="0" gradientUnits="userSpaceOnUse">
          <stop offset="0" stopColor={LINE_GRAD[0]} /><stop offset="1" stopColor={LINE_GRAD[1]} />
        </linearGradient>
        <linearGradient id={`${gid}-a`} x1="0" y1={padT} x2="0" y2={padT + innerH} gradientUnits="userSpaceOnUse">
          <stop offset="0" stopColor={LINE_GRAD[0]} stopOpacity="0.22" /><stop offset="1" stopColor={LINE_GRAD[1]} stopOpacity="0.02" />
        </linearGradient>
      </defs>
      {[0, 0.5, 1].map((f) => (
        <path key={f} d={`M ${padL} ${(padT + innerH * f).toFixed(1)} H ${width - padR}`} fill="none" stroke="currentColor" strokeOpacity="0.12" strokeWidth="1" />
      ))}
      <path d={area} fill={`url(#${gid}-a)`} />
      <path d={line} fill="none" stroke={`url(#${gid}-l)`} strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" />
      {pts.map((p, i) => (
        <circle key={i} cx={p.x.toFixed(1)} cy={p.y.toFixed(1)} r={i === pts.length - 1 ? 3.6 : 2.6} fill="var(--dsw-bg, #fff)" stroke={`url(#${gid}-l)`} strokeWidth="1.6" />
      ))}
      {props.showLabels ? points.map((p, i) => (
        <text key={i} x={pts[i].x.toFixed(1)} y={height - 3} textAnchor={i === 0 ? "start" : i === points.length - 1 ? "end" : "middle"} fontSize="9" fill="currentColor" opacity="0.6">{p.label}</text>
      )) : null}
    </svg>
  );
}
```

（Interactive 版：同结构 + 每点 `<circle>` 带 `onPointerEnter` 设 `hoverIdx` state，绝对定位 tooltip div 显示 `label/value/hitPct`——L3 Task 12 内联实现即可，不预建。）Board 基础版不需要 tooltip，本文件到此。

Board.tsx：props 增 `onOpenPanel?: () => void`；`boardRows(s)` 之后追加三段（用 `props.dash.usage.trend?.days`/`models` 防御式读取，缺数据跳过对应行）：

```tsx
      {u && u.trend && u.trend.days ? (
        <div className="dyn-pet-board-row dyn-pet-board-spark">
          <span className="dyn-pet-board-spark-label">{t("dash.trend7")}</span>
          <TrendChart points={lastN(u.trend.days, 7)} width={270} height={44} />
        </div>
      ) : null}
      {u && Array.isArray(u.models) && u.models.length > 0 ? (
        <div className="dyn-pet-board-row dyn-pet-mini-dim">{t("dash.models") + " " + u.models.slice(0, 3).map((m) => `${shortModel(m.model || m.route)} ${fmtTokens(m.requestTotal)}`).join(" · ") + (u.models.length > 3 ? " " + t("dash.moreN", { n: String(u.models.length - 3) }) : "")}</div>
      ) : null}
      {props.onOpenPanel ? (
        <div className="dyn-pet-board-open" onClick={(e) => { e.stopPropagation(); props.onOpenPanel!(); }}>{t("dash.openPanel")} →</div>
      ) : null}
```

`shortModel`（MiniBar 同款截断，抽到 format.ts 导出：>12 字符取前 10 + '…'）。i18n 键：`dash.trend7`（「7日」/"7d"）、`dash.models`（「模型」/"Models"）、`dash.moreN`（「等{n}」/"+{n}"）、`dash.openPanel`（「查看完整看板」/"Open full dashboard"）。styles.ts 追加 `.dyn-pet-board-spark`（行内 flex，label 灰字 11px）、`.dyn-pet-board-open`（右对齐、pointer-events:auto、下划线 hover）。

Pet.tsx：① MiniBar 渲染条件加 `&& !boardOpen`（互斥）；② boardLayer 样式改：`position: fixed; top: petY; left: petX - BOARD_W - 12（<8 则右侧 petX + petW + 12）; transform: scale(scale); transform-origin: top left`（petX/petW 取自 posRef/精灵渲染尺寸实际变量名；黑板宽 310 常量已在 Board 样式）。黑板出现即顶替：互斥由 ① 保证。

- [ ] **Step 4: 全量测试 + typecheck。**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/TrendChart.tsx src/client/Board.tsx src/client/boardrows.ts src/client/Pet.tsx src/client/styles.ts src/client/i18n.ts src/client/format.ts test/client-logic.test.ts
git commit -m "feat(client): Task11 黑板sparkline/模型行/入口行+锚定宠物侧+迷你条互斥顶替(R4/R10)"
```

---

### Task 12: 客户端——DashboardPanel.tsx（L3 大看板主体）（R5/R9）

**Files:**
- Create: `src/client/DashboardPanel.tsx`
- Modify: `src/client/styles.ts`（面板样式区）、`src/client/i18n.ts`、`src/client/format.ts`（`fmtPct`）
- Test: `test/client-logic.test.ts`（追加视图窗口纯函数）

**Interfaces:**
- Consumes: `appStore`（快照 dashboard：14 日+24 桶+models+五口径）、Task 8 `apiGetRange`、Task 11 `TrendChart/lastN`、`fmtTokens`。
- Produces:
  - `viewWindow(tab, now): { kind: 'hours' | 'days'; n: number; from?: string; to?: string }`（'5h'→{hours,5}；'7d'→{days,7}；'30d'→{days,30}）
  - `DashboardPanel({ onOpenPanelRequest?: () => void })`——组件内部自取 appStore（useSyncExternalStore 模式与 Pet.tsx 一致的实际用法）；30d/自定义走 `apiGetRange` + 60s 内存缓存（Map key `from:to`）
  - 五口径网格行序（spec R5）：用户输入(估，含子代理)/产出/请求输入(全文累计)/缓存命中/命中率 + 请求次数 + 数据截止时间

- [ ] **Step 1: 写失败测试**

```ts
import { viewWindow } from "../src/client/DashboardPanel";

describe("v2.2 view windows", () => {
  it("maps tabs to hour/day windows", () => {
    expect(viewWindow("5h")).toEqual({ kind: "hours", n: 5 });
    expect(viewWindow("7d")).toEqual({ kind: "days", n: 7 });
    expect(viewWindow("30d")).toEqual({ kind: "days", n: 30 });
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**——`DashboardPanel.tsx`（完整；样式类名 `dyn-pet-dash-*`）：

```tsx
// DashboardPanel.tsx — v2.2 L3 大看板（spec R5：复刻 Codex++ 排版，挂 main keyed 面板）。
// 数据：默认视图全走 /state 快照（14日+24小时桶+models）；30d/自定义走 /dashboard/range（60s 内存缓存）。
// 复刻要素：hero 大数字 / 五口径网格 / 蓝紫渐变趋势图(带 tooltip) / 模型分布进度条行 / 2×2 工具格 / 日期回看。
// 不复刻：价格（F04 存档）、推理单列（F03 存档）——spec §2.1。
import { useEffect, useMemo, useState } from "react";
import { appStore } from "./store";
import { apiGetRange, type RangeSummary, type TrendDay, type TrendHour } from "./api";
import { TrendChart, lastN } from "./TrendChart";
import { t } from "./i18n";
import { fmtTokens, fmtPct, shortModel } from "./format";

export type DashTab = "5h" | "7d" | "30d" | "custom";
export function viewWindow(tab: DashTab): { kind: "hours" | "days"; n: number } {
  if (tab === "5h") return { kind: "hours", n: 5 };
  if (tab === "7d") return { kind: "days", n: 7 };
  return { kind: "days", n: 30 };
}

const rangeCache = new Map<string, { at: number; data: RangeSummary }>();
async function fetchRange(from: string, to: string): Promise<RangeSummary> {
  const key = `${from}:${to}`;
  const c = rangeCache.get(key);
  if (c && Date.now() - c.at < 60000) return c.data;
  const data = await apiGetRange(from, to);
  rangeCache.set(key, { at: Date.now(), data });
  return data;
}
const dayKey = (d: Date) => `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;

export function DashboardPanel(): ReactElement {
  const snap = useSyncExternalStore(appStore.subscribe, appStore.getSnapshot);
  const [tab, setTab] = useState<DashTab>("7d");
  const [custom, setCustom] = useState({ from: dayKey(new Date(Date.now() - 6 * 86400000)), to: dayKey(new Date()) });
  const [range, setRange] = useState<RangeSummary | null>(null);
  const [busy, setBusy] = useState(false);
  const [rangeHint, setRangeHint] = useState("");
  const [hover, setHover] = useState<{ x: number; y: number; text: string } | null>(null);

  const dash = snap?.dashboard ?? null;
  const trend = dash?.usage?.trend ?? null;

  useEffect(() => {
    if (tab !== "30d" && tab !== "custom") return;
    let live = true; setBusy(true);
    // 31 天前端钳制（spec R9「超限前端截断提示」；宿主 400 之外的第二道闸）
    const toMs = new Date(custom.to + "T00:00:00").getTime();
    const maxFrom = dayKey(new Date(toMs - 30 * 86400000));
    if (custom.from < maxFrom) { setCustom((c) => ({ ...c, from: maxFrom })); setRangeHint(t("dash.rangeClamp")); }
    else setRangeHint("");
    const from = tab === "30d" ? dayKey(new Date(Date.now() - 29 * 86400000)) : custom.from;
    const to = tab === "custom" ? custom.to : dayKey(new Date());
    fetchRange(from, to)
      .then((r) => { if (live) setRange(r); })
      .catch(() => { if (live) setRange(null); })
      .finally(() => { if (live) setBusy(false); });
    return () => { live = false; };
  }, [tab, custom.from, custom.to]);

  // —— 窗口数据（纯推导）——
  const points = useMemo(() => {
    if (tab === "5h" && trend) {
      const nowH = new Date().getHours();
      // 整点锚定（spec R3）：最近 5 个「完整」整点桶 = 排除进行中小时
      const idxs: number[] = [];
      for (let k = 4; k >= 1; k--) idxs.push(((nowH - k) % 24 + 24) % 24);
      return idxs.map((i) => ({ label: `${String(i).padStart(2, "0")}:00`, value: (trend.hours[i] as TrendHour | undefined)?.dayTotal ?? 0, key: trend.hours[i]?.key ?? "" }));
    }
    if (tab === "7d" && trend) return lastN(trend.days, 7);
    if (range) return range.days.map((d: TrendDay) => ({ label: `${Number(d.key.slice(5, 7))}/${Number(d.key.slice(8, 10))}`, value: d.dayTotal, key: d.key }));
    return [];
  }, [tab, trend, range]);

  // —— hero 与五口径（7d/30d/custom 用区间 totals；5h 用当日快照口径）——
  const hero = useMemo(() => points.reduce((s, p) => s + p.value, 0), [points]);
  const usage = dash?.usage ?? null;
  const totals = tab === "custom" || tab === "30d" ? range?.totals ?? null : null;

  const grid: [string, string][] = [
    [t("dash.g.userEst"), "~" + fmtTokens(totals ? totals.userEst : usage?.userEst ?? 0) + t("dash.estimateSuffix") + " · " + t("dash.withSubagents")],
    [t("dash.g.output"), fmtTokens(totals ? totals.outputTokens : usage?.day.outputTokens ?? 0)],
    [t("dash.g.requestTotal"), fmtTokens(totals ? totals.requestTotal : usage?.requestTotal ?? 0)],
    [t("dash.g.cacheRead"), fmtTokens(totals ? totals.cacheRead : usage?.day.cacheReadTokens ?? 0)],
    [t("dash.g.hitPct"), fmtPct(totals ? totals.hitPct : usage?.cacheHitRate ?? 0)],
    [t("dash.g.requests"), String(totals ? totals.requestCount : trend?.days[13]?.requestCount ?? 0)],
    [t("dash.g.asOf"), new Date().toLocaleTimeString()],
  ];
  const models = tab === "30d" || tab === "custom" ? range?.models ?? [] : (usage?.models ?? []);
  const maxModel = Math.max(1, ...models.map((m) => m.requestTotal));

  return (
    <div className="dyn-pet-dash">
      <div className="dyn-pet-dash-head">
        <h2>{t("dash.panelTitle")}</h2>
        <div className="dyn-pet-dash-tabs">
          {(["5h", "7d", "30d", "custom"] as DashTab[]).map((x) => (
            <button key={x} className={"dyn-pet-dash-tab" + (tab === x ? " is-active" : "")} onClick={() => setTab(x)}>{t(`dash.tab.${x}`)}</button>
          ))}
          {tab === "custom" ? (
            <span className="dyn-pet-dash-dates">
              <input type="date" value={custom.from} max={custom.to} onChange={(e) => setCustom((c) => ({ ...c, from: e.target.value }))} />
              <span>–</span>
              <input type="date" value={custom.to} min={custom.from} max={dayKey(new Date())} onChange={(e) => setCustom((c) => ({ ...c, to: e.target.value }))} />
              {rangeHint ? <em className="dyn-pet-dash-rangehint">{rangeHint}</em> : null}
            </span>
          ) : null}
        </div>
      </div>

      <div className="dyn-pet-dash-hero">
        <div className="dyn-pet-dash-hero-label">{t("dash.heroTotal", { range: t(`dash.tab.${tab}`) })}</div>
        <div className="dyn-pet-dash-hero-num">{fmtTokens(hero)}</div>
        {tab !== "30d" && tab !== "custom" && trend ? <div className="dyn-pet-dash-weekhit">{t("dash.weekHit", { a: fmtPct(weekHit(trend, 0)), b: fmtPct(weekHit(trend, 1)), d: hitDeltaText(trend) })}</div> : null}
      </div>

      <div className="dyn-pet-dash-grid">{grid.map(([k, v]) => (<div key={k} className="dyn-pet-dash-cell"><span>{k}</span><strong>{v}</strong></div>))}</div>

      <div className="dyn-pet-dash-card">
        <div className="dyn-pet-dash-card-head"><span>{t("dash.trendTitle")}</span><span className="dyn-pet-dash-meta">{busy ? t("dash.loading") : t("dash.peak", { v: fmtTokens(Math.max(0, ...points.map((p) => p.value))) })}</span></div>
        <div className="dyn-pet-dash-trendwrap">
          <TrendChart points={points} width={560} height={120} showLabels />
          {points.map((p, i) => null /* 数据点 tooltip 在 TrendChart 上叠一层透明命中区 */)}
          <TooltipLayer points={points} onHover={setHover} hover={hover} />
        </div>
      </div>

      <div className="dyn-pet-dash-card">
        <div className="dyn-pet-dash-card-head"><span>{t("dash.models")}</span></div>
        {models.length === 0 ? <div className="dyn-pet-dash-empty">{t("dash.noData")}</div> : models.slice(0, 6).map((m) => (
          <div key={m.route} className="dyn-pet-dash-model">
            <span className="dyn-pet-dash-model-name" title={m.route}>{shortModel(m.model || m.route)}</span>
            <span className="dyn-pet-dash-model-val">{fmtTokens(m.requestTotal)}</span>
            <span className="dyn-pet-dash-model-bar"><i style={{ width: `${Math.max(4, (m.requestTotal / maxModel) * 100).toFixed(1)}%` }} /></span>
          </div>
        ))}
      </div>

      <div className="dyn-pet-dash-card">
        <div className="dyn-pet-dash-card-head"><span>{t("dash.tools")}</span></div>
        <ToolsGrid range={range} trend={trend} />
      </div>

      <div className="dyn-pet-dash-foot">{t("dash.caliberNote")}</div>
    </div>
  );
}
```

（`weekHit(trend, weekIdx)`/`hitDeltaText(trend)`/`ToolsGrid`/`TooltipLayer` 为同文件内小组件/纯函数：weekHit 按周一切界聚合本周(0)/上周(1)命中率（14 日快照内可算）；**ToolsGrid 数据源随视图**——5h/7d 用快照 `usage.tools`（Task 4 当日 Top5），30d/custom 用 `range.tools` Top5；2×2 指标格=调用总数/平均耗时/Top 工具次数/Top 工具总耗时。`TooltipLayer`：绝对定位 div，pointer 移入第 i 点显示 `label · fmtTokens(value) · 命中率%`（数据点带 hitPct 时；`lastN` 输出补 `hitPct` 透传字段，小时桶视图无 hitPct 则省略该段）。）i18n 追加上述全部 `dash.*` 键（zh/en，含 `dash.rangeClamp`「已截断为最近 31 天」/"Clamped to last 31 days"）；format.ts 加 `fmtPct(x)= (x*100).toFixed(1)+'%'`。样式区：`.dyn-pet-dash{padding:16px;overflow:auto;height:100%}`、hero 28px 加粗、grid 两列、card 圆角 10px 边框次级背景（对齐 Codex++ §2.1 排版参数）、model-bar 通栏 5px 圆角、tabs 胶囊按钮。**面板内滚动正常滚自身（6.6 规则 7 面板语义）**。

- [ ] **Step 4: 全量测试 + typecheck。**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/DashboardPanel.tsx src/client/styles.ts src/client/i18n.ts src/client/format.ts test/client-logic.test.ts
git commit -m "feat(client): Task12 L3大看板主体——选项卡/hero/五口径网格/趋势图/模型分布/工具区(R5/R9)"
```

---

### Task 13: 客户端——面板注册与钻取链闭合（R6）

**Files:**
- Modify: `src/client/index.tsx`（main keyed 注册 + sidebar.panellist 配置门 + layout 接线）、`src/client/PetMenu.tsx`（新菜单项）、`src/client/Pet.tsx`（黑板入口行回调）、`src/client/store.ts`（panelSwitcher）
- Test: `test/client-logic.test.ts`（追加 panelSwitcher 纯逻辑）

**Interfaces:**
- Consumes: rc.2 已验证的 `ctx.layout.selectPanel(panelId: MainPanelId | null): void`（客户端 runner api-catalog L86-90）；`sidebar.panellist` register options `{ name, id, order, label }`；`main` keyed register `{ name: 'main', key: 'foxbell-dashboard' }`；Task 12 `DashboardPanel`。
- Produces:
  - `PANEL_ID = 'foxbell-dashboard'`（store.ts 导出常量）
  - `setPanelSwitcher(fn)`/`openDashboardPanel()`（store.ts；fn 由 index.tsx 注入 `ctx.layout.selectPanel(PANEL_ID)`，缺失时 openDashboardPanel 返回 false 且调用方静默）
  - PetMenu 新项「用量看板」：`onOpenDashboard(): void`（关菜单 + openDashboardPanel()；失败则 toast 无——静默降级为无操作并 console.warn 一次）
  - sidebar.panellist 注册受 `cfgStore.getSnapshot().dashboardSidebarEntry` 门控（true 才 register；配置变化经既有 cfg 订阅重挂——沿用 sidebar.footer.action 的注册模式）

- [ ] **Step 1: 写失败测试**

```ts
import { setPanelSwitcher, openDashboardPanel } from "../src/client/store";

describe("v2.2 panel switcher", () => {
  it("returns false without switcher, true and calls after set", () => {
    expect(openDashboardPanel()).toBe(false);
    let called = "";
    setPanelSwitcher((id) => { called = id; });
    expect(openDashboardPanel()).toBe(true);
    expect(called).toBe("foxbell-dashboard");
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

store.ts：

```ts
export const PANEL_ID = "foxbell-dashboard";
let panelSwitcher: ((id: string) => void) | null = null;
export function setPanelSwitcher(fn: ((id: string) => void) | null): void { panelSwitcher = fn; }
export function openDashboardPanel(): boolean {
  if (!panelSwitcher) { console.warn("[foxbell-pet] panel switcher unavailable"); return false; }
  panelSwitcher(PANEL_ID);
  return true;
}
```

index.tsx（slots 就绪分支内，shell.overlay 注册后追加）：

```tsx
  const layout = (ctx.get("layout") ?? (ctx as unknown as { layout?: { selectPanel(id: string): void } }).layout) as
    | { selectPanel(id: string): void } | undefined;
  if (layout && typeof layout.selectPanel === "function") setPanelSwitcher((id) => layout.selectPanel(id));

  slots.inject("main", () =>
    slots.register({ name: "main", key: PANEL_ID }, () => React.createElement(DashboardPanel))
  );

  const syncSidebarEntry = () => {
    const on = !!cfgStore.getSnapshot().dashboardSidebarEntry;
    if (on && !sidebarEntryOff) {
      sidebarEntryOff = slots.inject("sidebar.panellist", () =>
        slots.register({ name: "sidebar.panellist", id: PANEL_ID, order: 200, label: t("dash.panelTitle") }, () =>
          React.createElement("div", { className: "dyn-pet-sideicon", title: t("dash.panelTitle") }, "📊")));
    } else if (!on && sidebarEntryOff) { sidebarEntryOff(); sidebarEntryOff = null; }
  };
  let sidebarEntryOff: (() => void) | null = null;
  syncSidebarEntry();
  const offCfg = cfgStore.subscribe(syncSidebarEntry);
  ctx.effect(() => { offCfg(); if (sidebarEntryOff) sidebarEntryOff(); });
```

（`ctx.effect` 为本文件既有清理注册 API 名——按实际调整；`slots.inject` 返回反注册函数（v2.1 sidebar.footer.action 同款用法，按既有代码确认）。react import 已有 `React.createElement` 用法。）

PetMenu.tsx：props 增 `onOpenDashboard(): void`；主页项列表「📊 查看最近总结」之后插「📈 用量看板」项（图标+文案+ onClick=onOpenDashboard）。Pet.tsx：PetMenu 挂载传 `onOpenDashboard={() => { closeMenu(); openDashboardPanel(); }}`；Board `onOpenPanel={() => { closeBoard(); openDashboardPanel(); }}`。

- [ ] **Step 4: 全量测试 + typecheck。**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/index.tsx src/client/PetMenu.tsx src/client/Pet.tsx src/client/store.ts src/client/i18n.ts test/client-logic.test.ts
git commit -m "feat(client): Task13 main面板注册+侧栏入口门控+右键/黑板/迷你条钻取链闭合(R6)"
```

---

### Task 14: 客户端——导出图片/复制文本 + 评语池 + 设置卡 3 项（R8）

**Files:**
- Create: `src/client/quotes.ts`（评语池与匹配）、`src/client/exportimage.ts`（canvas 合成）
- Modify: `src/client/DashboardPanel.tsx`（头部两按钮）、`src/client/SettingsCard.tsx`、`src/client/settingsdraft.ts`、`src/client/config.ts`（3 新键）、`src/client/animations.ts`（导出 ANIM 行序供姿态）、`src/client/i18n.ts`
- Test: `test/client-logic.test.ts`（追加 pickQuote 纯逻辑）

**Interfaces:**
- Consumes: Task 5 宿主 Config 键（dashboardSidebarEntry/exportQuote/exportPose 同名客户端键）；Task 11/12 数据；`ANIM` 行表与 `appStore.getRuntime().spriteUrl/rows`（帧几何：帧宽=sheetW/cols——cols 按 ANIM 最长 `d` 数组；行高=sheetH/rows）。
- Produces:
  - `pickQuote(agg: { total: number; hitPct: number; trendUp: boolean | null; multiModel: boolean; loaf: boolean }, lang: 'zh'|'en', custom?: string): string`——custom 非空优先（占位符 `{range}/{tokens}/{hit}/{models}` 替换），否则规则池（优先级：命中率>0.9 → 总量档（>100万/适度）→ 趋势连升/降 → 多模型 → 摸鱼；池 10 条 zh/en）
  - `exportDashboardImage(opts: { points: {label,value}[]; hero: number; grid: [string,string][]; models: {name,val}[]; pose: string; quote: string; sprite: HTMLImageElement | null }): Promise<Blob>`（1200×675 canvas：标题栏/hero/mini 趋势折线（canvas 直绘）/网格/模型行/底部立绘（pose='random' → 按 ANIM 键随机行首帧）+气泡评语）
  - SettingsCard：复选「侧栏看板入口」+ 文本域「导出评语」+ 下拉「导出姿态」（random + ANIM 键清单，i18n `dash.pose.*`）

- [ ] **Step 1: 写失败测试**

```ts
import { pickQuote, fillQuote } from "../src/client/quotes";

describe("v2.2 quotes", () => {
  it("custom template wins with placeholders filled", () => {
    expect(fillQuote("今日 {tokens}，命中 {hit}", { tokens: "12.3万", hit: "90%" })).toBe("今日 12.3万，命中 90%");
  });
  it("rule pool picks by priority (hit>total>trend>multi>loaf)", () => {
    const q = pickQuote({ total: 500, hitPct: 0.95, trendUp: true, multiModel: true, loaf: false }, "zh");
    expect(q).toContain("缓存");
  });
});
```

- [ ] **Step 2: 确认失败** — FAIL。

- [ ] **Step 3: 实现**

quotes.ts（完整）：

```ts
// quotes.ts — v2.2 R8 导出评语池：纯规则匹配（无 LLM），zh/en 双池；自定义模板优先。
export interface QuoteAgg { total: number; hitPct: number; trendUp: boolean | null; multiModel: boolean; loaf: boolean }
export interface QuoteVars { range: string; tokens: string; hit: string; models: string }

const POOL: Record<"zh" | "en", { when: (a: QuoteAgg) => boolean; text: string }[]> = {
  zh: [
    { when: (a) => a.hitPct >= 0.9, text: "缓存吃得干干净净，省钱小能手！" },
    { when: (a) => a.hitPct > 0 && a.hitPct < 0.5, text: "缓存有点冷，上下文还在热身。" },
    { when: (a) => a.total >= 1_000_000, text: "百万俱乐部又添一日。" },
    { when: (a) => a.total < 100_000, text: "今天很节制，尾巴都翘起来了。" },
    { when: (a) => a.trendUp === true, text: "用量在爬坡，我在看着呢。" },
    { when: (a) => a.trendUp === false, text: "用量在收敛，干得漂亮。" },
    { when: (a) => a.multiModel, text: "多线作战，指挥若定。" },
    { when: (a) => a.loaf, text: "今天有点闲，摸鱼使我快乐。" },
    { when: () => true, text: "又是好好干活的一天。" },
  ],
  en: [
    { when: (a) => a.hitPct >= 0.9, text: "Cache hit like a pro!" },
    { when: (a) => a.hitPct > 0 && a.hitPct < 0.5, text: "Cache still warming up." },
    { when: (a) => a.total >= 1_000_000, text: "Another day in the million club." },
    { when: (a) => a.total < 100_000, text: "Lean day. Tail's up!" },
    { when: (a) => a.trendUp === true, text: "Usage is climbing, I'm watching." },
    { when: (a) => a.trendUp === false, text: "Usage is tapering. Nice." },
    { when: (a) => a.multiModel, text: "Multi-model ops, fully in control." },
    { when: (a) => a.loaf, text: "Quiet day. Loaf mode." },
    { when: () => true, text: "Another good day's work." },
  ],
};

export function fillQuote(tpl: string, v: QuoteVars): string {
  return tpl.split("{range}").join(v.range).split("{tokens}").join(v.tokens).split("{hitPct}").join(v.hit).split("{models}").join(v.models);
}

export function pickQuote(a: QuoteAgg, lang: "zh" | "en", custom?: string, vars?: QuoteVars): string {
  if (custom && custom.trim()) return fillQuote(custom.trim(), vars ?? { range: "", tokens: "", hit: "", models: "" });
  for (const r of POOL[lang]) if (r.when(a)) return r.text;
  return POOL[lang][POOL[lang].length - 1].text;
}
```

exportimage.ts（核心骨架，绘制顺序固定）：

```ts
// exportimage.ts — v2.2 R8 canvas 导出（1200×675 PNG）：标题/hero/趋势折线/口径网格/模型行/宠物立绘+评语。
export interface ExportInput {
  title: string; hero: string; grid: [string, string][]; models: { name: string; val: string }[];
  points: { label: string; value: number }[];
  quote: string; poseRow: number; frameCols: number;
  sprite: HTMLImageElement | null; frameW: number; frameH: number;
}
export async function exportDashboardImage(input: ExportInput): Promise<Blob> {
  const W = 1200, H = 675;
  const cv = document.createElement("canvas"); cv.width = W; cv.height = H;
  const c = cv.getContext("2d")!;
  c.fillStyle = "#fdf9f3"; c.fillRect(0, 0, W, H);            // 暖纸底
  c.fillStyle = "#2b2b2b"; c.font = "600 30px system-ui"; c.fillText(input.title, 48, 64);
  c.font = "700 64px system-ui"; c.fillText(input.hero, 48, 160);
  c.font = "400 15px system-ui"; c.fillStyle = "#7a6a58";
  drawTrend(c, input.points, 48, 200, W - 96, 150);           // 蓝紫渐变折线（createLinearGradient #3BA7FF→#8D6BFF）
  let y = 400;
  c.fillStyle = "#2b2b2b";
  for (const [k, v] of input.grid.slice(0, 4)) { c.font = "400 16px system-ui"; c.fillText(k, 48, y); c.font = "600 16px system-ui"; c.fillText(v, 300, y); y += 30; }
  y += 8;
  for (const m of input.models.slice(0, 3)) { c.font = "500 15px system-ui"; c.fillText(`${m.name}  ${m.val}`, 48, y); y += 26; }
  // 底部立绘 + 气泡评语
  if (input.sprite) {
    const scale = Math.min(200 / input.frameH, 1);
    const dw = input.frameW * scale, dh = input.frameH * scale;
    c.drawImage(input.sprite, 0, input.poseRow * input.frameH, input.frameW, input.frameH, W - dw - 64, H - dh - 48, dw, dh);
  }
  roundRect(c, W - 560, H - 160, 440, 88, 16); c.fillStyle = "#ffffff"; c.fill();
  c.fillStyle = "#4a3b2a"; c.font = "500 18px system-ui";
  wrapText(c, input.quote, W - 544, H - 128, 400, 24);
  return await new Promise<Blob>((res) => cv.toBlob((b) => res(b!), "image/png"));
}
```

（`drawTrend/roundRect/wrapText` 同文件工具函数：渐变 stroke + 面积填充 + 数据点；圆角路径；按宽折行。DashboardPanel 头部按钮：`复制文本` 拼 grid+models 多行文本 `navigator.clipboard.writeText`；`导出图片` 组装 ExportInput——sprite 经 `new Image()` 从 runtime.spriteUrl 加载（await onload，失败传 null）；poseRow=config.exportPose==='random' ? 随机 ANIM 行 : ANIM[pose]?.row ?? 0；frameCols=max(d.length)；frameW=sprite.naturalWidth/frameCols；quote=pickQuote(...)。）SettingsCard 三项接入 draft/save 既有模式（布尔→复选、字符串→文本域/下拉；NUM_KEYS 不含新键——纯布尔/字符串，无需 Number 转换）。config.ts 键与默认值与宿主 Config 逐字一致（false/''/'random'）。

- [ ] **Step 4: 全量测试 + typecheck。**

- [ ] **Step 5: 四绿 + 提交**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add src/client/quotes.ts src/client/exportimage.ts src/client/DashboardPanel.tsx src/client/SettingsCard.tsx src/client/settingsdraft.ts src/client/config.ts src/client/animations.ts src/client/i18n.ts test/client-logic.test.ts
git commit -m "feat(client): Task14 canvas导出图片(立绘随机/自选+评语池+自定义模板)+复制文本+设置卡3项(R8)"
```

---

### Task 15: E2E 真机验证（rc.2 + computer-use 自测）+ 文档收尾（spec「验证与风险」）

**Files:**
- Create: `docs/superpowers/plans/artifacts/v2.2-e2e/`（**统一检验文件夹**——本任务全部测试产物只放这里；该文件夹仅供分支上的评审使用，**合并 main 前删除、不进 main**）
- Modify: `README.md`、`README.en.md`、`CHANGELOG.md`（[2.2.0] 段）

**Interfaces:** 无代码接口；验收对 spec「验证与风险」全条目。

**执行方式要求**：本任务必须**真实机器自测**——起 dsh web、装最新构建、用你具备的浏览器/computer-use 工具自己点击操作并截图取证；不允许只跑 curl 断言 JSON 就算通过。

- [ ] **Step 1: 构建最新插件 + 起 dsh web（3080）**

```bash
# 1) 插件最新构建（分支工作树内）
npm run build && npm run validate
# 2) 宿主（沿用 v2.1 已验证流程；lib 产物缺失时按 v2.1 plan Task 0 的桩流程重建）
cd ~/Documents/DeepSeek/deepseek-harness && git status --porcelain | head   # 必须 clean
# 杀遗留 3080 进程 → 起 dsh web（profile 指向 ~/.dsh/profiles/web，
# 其中 dsh-foxbell-pet 以 local link 指向本分支工作树——v2.1 E2E 已接线，验证 link 目标是本分支）
# 3) 起服后 curl 验活 + 记录服务版本
curl -s "http://127.0.0.1:3080/<按既有路径>" | head   # 按 v2.1 E2E 实际路径
```

- [ ] **Step 2: 真机验收清单（浏览器/computer-use 逐项操作；每项截图 + 简短操作记录存 `artifacts/v2.2-e2e/E<n>-<slug>.png` + `report.md`）**

| # | 项目 | 通过标准（操作方式） |
|---|---|---|
| E1 | 服务+插件加载 | 打开 3080，宠物渲染，console 无 foxbell 报错（截图含控制台） |
| E2 | L1 迷你条钻取 | 悬停出迷你条（截图）；点「详情 »」外区域点击穿透正常；点「详情 »」开黑板且迷你条消失（前后两张截图） |
| E3 | 黑板加料+锚定 | sparkline/模型/入口三行渲染；拖宠物到屏幕另一侧，黑板锚定跟随（两张截图）；✕ 关闭后迷你条恢复 |
| E4 | L3 大看板 | 黑板「查看完整看板 →」切换到主列面板（截图五个分区完整）；选项卡 5h/7d/30d 各截一张（数据随窗口变化） |
| E5 | 自定义范围 | 故意选 >31 天跨度 → from 被钳到最近 31 天并出现截断提示（截图）；选上月出逐日数据 |
| E6 | 模型分布对账 | L3 模型行合计 ≈ 同区间 hero；与 `~/.dsh/storages/session_projcache/sessions/*.json` 的 provider 维度 totals 抽 1 会话人工比对，结论写 report.md |
| E7 | 右键菜单 | 右键宠物 → 「用量看板」直达 L3（截图菜单与结果） |
| E8 | 侧栏入口门控 | 默认无侧栏图标（截图）；设置卡勾选「侧栏看板入口」→ 图标出现、点击切换（截图）；取消勾选 → 移除 |
| E9 | 警报音效 | 设置卡临时调低 dayLimitTokens 触发 day-warn：宠物无完整语音组时播出合成 chime（DevTools 音频节点/Elements 里 audio 元素截图佐证）；muted 开启时不播 |
| E10 | 导出图片/复制文本 | 「导出图片」下载 PNG（打开文件截图：含立绘+评语+趋势线）；「复制文本」粘贴板内容含五口径行 |
| E11 | P3/P4 生效 | DevTools Network：连续 /state 多数为小体积 unchanged 响应（截图对比大小）；切后台标签页 ≥10s 后 interval 变 5s（Console 截图） |
| E12 | 降级回归 | 不触碰任何新入口时，L1/L2/状态卡/右键既有项行为与 v2.1 一致（对照截图） |

- [ ] **Step 3: 性能抽查**——DevTools Performance 录 30s 空闲（无会话活动）：记录 /state 平均耗时与轮询 CPU 占比进 report.md；有条件时与 main（v2.1.0）同场景对比各记一组数字。

- [ ] **Step 4: 文档**——CHANGELOG `[2.2.0]`（对齐 spec R0-R10 一句话/项）；README 两语「用量看板」章节（三级披露 + L3 截图 1 张，图可引用仓库内相对路径 `docs/.../v2.2-e2e/E4-*.png` 的拷贝放 `assets/` 或 README 直接文字描述——README 不依赖将被删除的检验文件夹，截图另拷）；issue #4 评论勾掉已清偿项（黑板锚定、en 警报、双扫描、MiniBar 计数、engines.dsh）。

- [ ] **Step 5: 终验四绿 + 提交（检验文件夹随分支提交、注明删除约定）**

```bash
npm test && npm run typecheck && npm run build && npm run validate
git add README.md README.en.md CHANGELOG.md docs/superpowers/plans/artifacts/v2.2-e2e
git commit -m "docs(e2e): Task15 v2.2.0 真机E2E 12项+性能抽查+README/CHANGELOG（检验文件夹合并前删除）"
```

> **检验文件夹处置约定**：`artifacts/v2.2-e2e/` 只服务于分支评审。评审通过后、PR 合并前，执行方以最后一个 commit `git rm -r docs/superpowers/plans/artifacts/v2.2-e2e` 删除（CHANGELOG/README 不引用其中文件），确保 main 不收测试产物。

---

## Self-Review 记录（2026-09-12 交叉自检后终版）

1. **Spec 覆盖**：R0（三级披露=Task 10/11/12/13）、R1 P1-P4（Task 4/5/6/8）、R2（Task 1/4，byDayRoute 切片）、R3（Task 2/6/12，整点锚定+节流）、R4（Task 11）、R5（Task 12/13，工具区全视图覆盖：5h/7d 快照 tools + 30d/custom range.tools）、R6（Task 10/11/13）、R7（Task 7/9）、R8（Task 14，占位符与 spec 同名 `{range}/{tokens}/{hitPct}/{models}`）、R9（Task 12，31 天前端钳制+提示）、R10（Task 3/4/6/7/10/11）——全覆盖；「思考文本兜底」不做清单无任务（正确）。
2. **交叉自检修复记录**（2026-09-12）：① spec R1「用户输入估算今日单日裁剪」与 R3 区间聚合矛盾 → spec 改全日 map；② byDayRoute 交叉维度从 Task 4 注记前移进 Task 1 实现+测试（Task 4 当日模型、Task 2 区间模型都改读切片，长会话跨日不污染）；③ byDay/byHour 桶补 requestCount 透传（趋势字段闭环）；④ Task 2 清理教学式修正块为最终代码；⑤ F40 周对比行条件放宽到快照支持的全部视图（5h/7d）；⑥ 自定义范围补 31 天前端钳制+提示；⑦ 评语占位符 `{hit}`→`{hitPct}` 对齐 spec。
3. **类型一致性**：`RouteUsage/TrendDay/TrendHour/RangeSummary`（Task 8 定义，Task 12 消费）；`usage.tools`（Task 4 产、Task 8 类型、Task 12 消费）；`PANEL_ID`（Task 13）；`pickQuote/fillQuote`（Task 14）；`foldUsage` 六键返回形状（Task 1 定、Task 2/4 消费）。
