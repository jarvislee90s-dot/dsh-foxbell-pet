# v2.2.0 用量看板 执行报告（executor report）

- 日期：2026-09-12
- 分支：`feature/v2.2-usage-dashboard`（自 main @ 2095432 建，**停在该分支等待 review，未合并 main、未开 PR**）
- HEAD：`4be8592` 之后追加文档收尾 commit（见下）；最终四绿全绿
- 需求事实源：`docs/superpowers/specs/2026-09-11-v2.2-usage-dashboard-design.md`（R0-R10）
- 执行依据：`docs/superpowers/plans/2026-09-11-v2.2-usage-dashboard.md`（15 任务）；过程账本 `.superpowers/sdd/progress.md`

## ① 提交清单与 HEAD

| 任务 | 提交 | 说明 |
|---|---|---|
| Task 1 | f388517 | foldUsage 扩展 byRoute/byHour/requestCount + routeSource/hourKeyOf（R2） |
| Task 2 | 7f89c90 + 82312b2 | buildTrend/summarizeRange/foldToolsByDay；评审修复：工具计数回归仅 tool/call 口径（spec R5）+ 区间钳制 ≤31 天（R3/R9） |
| Task 3 | a0279ec | evaluateAlerts 结构化 reached（R10） |
| Task 4 | 73947df | 引擎指纹增量缓存 P1 + 双扫描透传 + usage.models/tools/trend/counts 快照（R1/R2/R3/R10） |
| Task 5 | 1db3e88 + 4b4b88f | P2 宠物目录/清单 mtime 缓存 + Config 3 新键；评审修复：逐宠物 manifest mtime 复验（清单编辑即时失效） |
| Task 6 | 5fc85af | /state ?since 短路 P3 + /dashboard/range + /sounds；E2E 跟进修复 f382741（/sounds 尾斜杠） |
| Task 7 | 1d8b40d | 版本 2.2.0 + engines.dsh ≥0.1.2-rc.1 + 合成音效 3 枚（R7 音源裁定回填 spec）+ lib 构建同步 |
| Task 8 | e7be49c + 1338c60 + 6160287 | api 类型 + since 短路客户端 + 不可见 5s 降频；评审修复：轮询调度代数守卫（防重入双链）；bundle-smoke document 桩补齐 + 构建同步（消除陈旧产物对冒烟门的掩蔽） |
| Task 9 | adac265 | usage 组退役 + MAM 四组音效链 + 默认 chime + 警报 i18n（R7/R10） |
| Task 10 | 62fa905 | 迷你条唯一钻取按钮「详情 »」+ 宿主全量计数口径（R6/R10） |
| Task 11 | ee0882d | TrendChart + 黑板 sparkline/模型 Top3/入口行 + 锚定宠物侧 + 迷你条互斥（R4/R10） |
| Task 12 | 14661a1 + 01ce740 | DashboardPanel L3 主体（R5/R9）；评审修复：31 天钳制同 pass 生效 + 提示常驻至下次编辑 |
| Task 13 | c787050 | main 面板注册 + sidebar.panellist 门控 + 右键/黑板钻取链闭合（R6）；E2E 跟进修复 ca8b68f（layout 可选注入，Critical） |
| Task 14 | 4be8592 | canvas 导出图片 + 复制文本 + 评语池 + 设置卡 3 项（R8） |
| 自检 | 63b21ca | L3 口径脚注对齐 spec R5 引文 + 数据截止时间标签 |
| Task 15 | （本次提交） | E2E 检验文件夹 + README/CHANGELOG + 本报告 |

**HEAD：见 `git log`（本次文档收尾 commit）。** 每任务独立 commit、信息带任务号；既有断言零删除（29 个 summarize 字节契约用例原样通过，`git diff main HEAD -- test/dashboard.test.mjs` 中 29 用例所在区块无 − 行）；实现/导出全程无任何价格金额字段。

## ② 四绿证据（最终态）

- `npm test` → **396 passed (396) / 15 files**（29 summarize 契约 + v2.1 全部既有用例 + v2.2 新增 67 用例）
- `npm run typecheck` → tsc --noEmit 零输出
- `npm run build` → lib/index.js + lib/client.js 重建，纯度自检通过（validate §9 复核）
- `npm run validate` → **VALIDATE OK**（11/11，含版本一致性、src↔lib 重建比对、错误码表三方一致）

每任务收尾均独立跑过四绿（各任务报告见 `.superpowers/sdd/reports/task-N-report.md`，随 .superpowers/ 本地留存不入库）。

## ③ E1-E12 真机逐项结果（截图与详情见 `artifacts/v2.2-e2e/report.md`）

环境：rc.2 真机（3080），profile local link 指向本分支工作树；Playwright 真实点击/右键/拖拽。

| # | 结果 | 要点 |
|---|---|---|
| E1 | PASS* | 首次加载实锤 **Critical 缺陷**：`ctx.get("layout")` 对未声明服务抛错 → 整客户端插件加载失败、宠物不挂载；改 `ctx.inject(["layout"])` 可选注入（ca8b68f）后复验通过：宠物渲染、console 无 foxbell 报错（仅无关插件 dsh-emoji 405 ×2） |
| E2 | PASS | 悬停出迷你条（五口径行 + 「详情 »」）；真实点击「详情 »」→ 黑板开 + 迷你条消失；`.dyn-pet-mini` 保持 pointer-events:none、仅按钮 auto |
| E3 | PASS | 黑板三行齐全（sparkline/模型行无数据正确跳过/入口行）；锚定精确（左 `petX−322`、左缘右翻转 `petX+204` 实测逐像素吻合）、拖拽跟随、TTL/✕ 关闭后迷你条恢复 |
| E4 | PASS | 「查看完整看板 →」→ L3 主列面板挂载（layout.selectPanel 生效）；hero/周命中/五口径网格/渐变趋势图（峰值、轴标签）/模型/工具/口径脚注齐全；5h=最近 5 个完整整点桶（01:00–05:00，进行中小时排除）；7d/30d 数据随窗口变化（30d 走 /dashboard/range） |
| E5 | PASS | 73 天自定义 → from 钳到 31 天（08-13…09-12）+「已截断为最近 31 天」提示，无 400 |
| E6 | PASS（数据核级） | 见 ④ |
| E7 | PASS | 右键菜单「📈 用量看板」真实点击 → 菜单关、L3 开 |
| E8 | PARTIAL-FAIL | 设置卡 3 项渲染/草稿/保存全通（update 抓包 + settings.yaml 落盘证实）；重启后 gate=true；但 sidebar.panellist 图标不渲染（无报错、注册形状与 slot-catalog 示例逐字一致），疑似子槽声明生命周期问题需上游排查。退化路径（右键/黑板链）可用 |
| E9 | PARTIAL | 3 枚 chime 经 /sounds 实播成功（0.34/0.34/0.42s）；警报链与 muted 短路为评审过的代码路径；完整阈值跨越触发需真实用量（环境限制，见下） |
| E10 | PASS | 复制文本（抓包）：五口径全部行 + 模型段；导出图片：真实下载 1200×675 PNG，含标题/hero/渐变趋势线/口径网格/宠物立绘 + 评语气泡（E10-export.png） |
| E11 | PASS/PARTIAL | P3：16s 实测 11 次轮询全部 unchanged、恒 122B、间隔 ~1507ms；服务端 unchanged 均值 0.62ms vs 全量 0.76ms、载荷 134B vs 14,513B（−99.1%）。P4：监听已注册 + 逻辑单测钉住；自动化浏览器无法真实切后台，5s 未做运行时测量 |
| E12 | PASS | 状态卡（报错样例 + 会话 id + 年龄标注 + 随宠物锚定）、右键全量 v2.1 菜单、黑板 TTL/✕、迷你条五口径行原样；滚轮/双击/隐藏为不变面零改动（v2.1 基线沿用） |

**E2E 过程修复的 2 个真机缺陷**：ca8b68f（Critical，见 E1）与 f382741（/sounds 尾斜杠路由 404，vitest 假派发较宽松未拦、已对齐真机 matcher 语义并 RED→GREEN）。

**环境限制**：本地源码版 harness 缺 flock 原生件（`native/system/.../system.node`，来自 GitHub release 产物），agent 会话无法启动 → 无真实 token 用量产生。影响：E6 的 L3 展示面（模型/工具/趋势空态）、E9 完整触发、非零数据视觉；附带真机验证了 v2.1 报错状态卡链路（E12 佐证截图 E12-error-card.png）。

## ④ 对账结论（模型分布 vs 官方 totals）

样本：`session-2d359772-cefe-4bff-9e37-e1ef0dcb1bdc`（801 条带 usage 的 assistant/message 事件，zstd 原始日志解出）→ 原样重放进出仓的 `src/host/dashboard.js#foldUsage`：

| 口径 | foldUsage grand | 官方 tokenUsage totals | diff |
|---|---|---|---|
| 请求输入（uncached） | 6,015,114 | 6,015,114 | **0** |
| 产出 | 736,436 | 736,436 | **0** |
| 缓存命中 | 305,636,224 | 305,636,224 | **0** |
| 缓存写入 | 0 | 0 | **0** |

- requestCount = 801；byRoute 各桶四口径之和 == grand、请求数之和 == 801（内部自洽）。
- 路由分布（按产出）：agentplan-cherry/DeepSeek-V4-Flash 500,564、deepseek-official/deepseek-v4-flash 199,322、opencode-go/deepseek-v4-flash 36,550（projcache 该版本 tokenUsage 行未分 provider 桶，路由维度以事件流为准重放比对）。
- 与 liveTokenUsage.settled 的 uncached 差 −1,807,360 为官方 settled 侧含 3 个估计样本（settledEstimates: 3）所致，与权威 tokenUsage.totals 完全一致。
- **结论：R1 缓存正确性与 R2 路由归因在真实数据上与 harness 官方统计完全对账通过**（复现步骤见 artifacts/v2.2-e2e/reconciliation.md）。

## ⑤ 自检修改清单（放下 plan、通读 spec R0-R10）

1. **L3 口径脚注**：spec R5 引文为「纯 token · 含子代理 · 本地聚合」，实现误复用 v2.1 设置卡文案（缺「含子代理·本地聚合」）→ 新增 `dash.caliber` 键并替换 L3 脚注（63b21ca）。
2. **「数据截止」→「数据截止时间」**：R5 五口径网格第 7 项标签逐字对齐（63b21ca）。
3. 记录性裁定（不改代码，已论证）：
   - R5「名词与 L1 一致」与 spec 自身明示标签「用户输入(估，含子代理)」的措辞张力——以 spec 显式枚举为准（L1 保留「你的输入」，两者后缀约定一致）；
   - R8「切换宠物回退随机姿态」在 plan 钉死的静态 ANIM 行清单下为空命题（姿态值跨宠物恒合法且有 `?? 0` 越界回退）；
   - spec R8 评语池「8-12 条」与 plan 代码 9 条的出入 → 实现为 10 条（补 total===0 规则，位于总量档内、先于 <10万 档）。

（注：实现期另有两处 plan 内部矛盾按预置规则裁定，详见各任务报告与 ledger——Task 1 测试/实现互斥（测试+契约为准，pruneIfEmpty）；Task 2 工具计数 plan 测试与 spec R5 冲突（spec 唯一权威，改测试为仅 tool/call 口径）；Task 14 测试占位符 `{hit}`→`{hitPct}`（spec R8 + plan 自检⑦）。）

## ⑥ 遗留问题与建议

**功能遗留**
1. **E8 侧栏图标不渲染**（PARTIAL-FAIL）：配置持久化、保存链路、gate 逻辑均验证通过，仅 sidebar.panellist 条目未在 rc.2 侧栏投影。建议下批排查 slot 子槽声明生命周期（或改用侧栏 footer.action 同款挂点），当前以右键菜单 + 黑板链两入口覆盖。
2. **黑板 15s TTL 内交互节奏**：E2E 中多次出现 TTL 先于人工操作关闭（行为正确，属 v2.1 契约），后续可考虑「鼠标悬停黑板时暂停 TTL」的体验打磨（挂 issue #4）。
3. **真实用量下的视觉验收**：待 harness 原生件修复的环境里，补拍非零数据下的 L3 全量视觉（模型分布条/非平坦趋势/评语多样性）与 E9 完整警报触发、E11 P4 真实切后台测量。

**工程债（final review/各任务评审 Minors，均不阻塞，完整清单见 ledger）**
4. `ctx.get("sessions")/("settingsScope")` 与 layout 同类的「未声明 get」隐患（当前服务在场故安全；建议统一 ctx.inject 可选注入）。
5. 复制失败仍闪「已复制」；导出异常无用户反馈 + 无重入护栏；`align-self: flex-end` 在非 flex 容器上无效（迷你条钻取按钮现为全宽左对齐，建议 `text-align:right`）；MiniBar 状态卡 day-hit `reached` 无测试钉。
6. 午夜跨越时缓存 scan 的 turns/errors 计数（喂总结行）在下一次事件前保持昨日口径（fp 缓存设计边界，已在 state.js 注释）；foldCache 无淘汰、root 被删边界伺服最后一份列表——量级均可忽略。
7. 迷你条 v2.1 继承的 `mini-wrap` 层可被 hit-test（非 v2.2 变更面，CSS 与 main 逐字节一致）；如需严格穿透可后续给 wrap 加 pointer-events:none。

**流程建议**
8. issue #4 的已清偿项勾选（黑板锚定、en 警报、双扫描、MiniBar 计数、engines.dsh）属对外操作，本次未执行，建议 review 后由维护者评论。
9. `artifacts/v2.2-e2e/` 检验文件夹随分支提交、合并 main 前删除（见文件夹内 report.md 约定）；README/CHANGELOG 未引用其中任何文件。
