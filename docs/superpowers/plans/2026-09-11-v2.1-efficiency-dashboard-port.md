# v2.1.0 效率看板移植 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 feature/effibility-dashboard-p1@886b303 上实机验证过的效率看板 v1.4.0 全集移植到 v2.0.0 架构的 main（@4750e61），发版 v2.1.0，并在 dsh-v0.1.5-rc.2 宿主上完成端到端验证（含 UI 截图与点击测试）。

**Architecture:** 宿主侧纯 JS（v2 惯例）：dashboard.js 纯函数原样入 `src/host/`，聚合接进 `state.js` 的 createStateEngine，快照经 `host/index.js` 的 snapshotExtra 下发。客户端侧 TSX：新增 MiniBar/Board/Sign 组件接入 `Pet.tsx`，设置卡在既有壳上并入草稿保存。文案走 `i18n.ts` 双语。

**Tech Stack:** 宿主 Cordis 服务（webServer/settings/sessions）、schemastery、客户端 React 18 + esbuild、vitest。

## Global Constraints（契约 `docs/superpowers/specs/2026-09-11-v2.1-efficiency-dashboard-port.md` 摘要，每个任务隐含遵守）

- 分支：从 `origin/main @ 4750e61` 切 `feature/v2.1-efficiency-dashboard`；移植源只读 = `feature/efficiency-dashboard-p1 @ 886b303`（`git show 886b303:<path>` 提取）。
- 第一个动作：`git cherry-pick 279d20b^..origin/feature/efficiency-dashboard-p1`（契约两份 + 本计划共三份 docs 提交，位于源分支尖）。
- 行为不变量 6 条（契约 §5）：五口径语义与文案（`请求输入/缓存命中/命中率/产出/你的输入~X(估)/含子代理`）、跨阈值+日键警报、档位默认值（12 事件/5min、3min、15/25/35/60min）、交互规则（单击每目标单行为/悬停纯读取/拖拽无命令/ESC 分层/滚轮穿透/右键仅本体/黑板优先互让）、纯 token 零金额白名单、宿主基线 dsh ≥ 0.1.2-rc.1（协议面 0.1.5-rc.2 逐项兼容已验证）。
- 12 项配置键名与默认值与 v1.4.0 逐字一致：`paceEnabled(true) paceIntenseEvents(12) paceLongrunMin(3) paceLoafStartMin(15) usageEnabled(true) dayLimitTokens(0) milestoneUnit(1000000) approvalFlickerMin(5) summaryEnabled(true) summaryEntrySec(15) boardTtlSec(15) ttsEnabled(false)`。
- 版本 v2.1.0；localStorage 键、路由前缀 `/dyn-pet-foxbell/`、轮询 1.5s 不变。
- 每任务收尾四绿：`npm run build && npm run validate && npm test && npm run typecheck`（v2 已有四脚本）。
- UI 文案 zh/en 双语（`src/client/i18n.ts`，键名前缀 `dash.`）。

---

### Task 1: 分支 + 契约入库 + dashboard.js 原样移植与测试转 vitest

**Files:**
- Create: `src/host/dashboard.js`（从源分支原样复制）
- Create: `test/dashboard.test.mjs`（node:test → vitest 转写）

**Interfaces:**
- Produces（后续任务的唯一依赖面，签名与 v1.4.0 一字不差）:
  `sessionEvents(session)`, `PACE_LABELS`, `DEFAULT_PACE`, `derivePaceTier(agg, now, opt?)`, `dateKeyOf(ms)`, `zeroUsage()`, `foldUsage(evts)`, `formatTokens(n)`, `evaluateAlerts(prev, next, cfg, dateKey?)`, `ageLabel(sec)`, `summarize(perSession, dayUsage, userEst)`, `blocksTextOf(blocks)`, `estimateTokens(text)`, `estimateUserTokensByDay(evts)`, `hitRate(usage)`

- [ ] **Step 1: 建分支 + 契约/计划文档入库**

```bash
cd /Users/jarvis/Documents/DeepSeek/DeepSeek-plugins/dsh-foxbell-pet
git fetch origin -q
git checkout -b feature/v2.1-efficiency-dashboard origin/main
git cherry-pick 279d20b^..origin/feature/efficiency-dashboard-p1   # 契约+本计划共三份 docs 提交（源分支尖，哈希无关引用）
```

- [ ] **Step 2: 复制 dashboard.js**

```bash
git show 886b303:src/dashboard.js > src/host/dashboard.js
```

- [ ] **Step 3: 测试转 vitest** —— `git show 886b303:test/dashboard.test.mjs` 取源，机械替换：`import { test } from 'node:test'` + `import assert from 'node:assert/strict'` → `import { describe, it, expect } from 'vitest'`；每个 `test('名称', () => {…})` → `it('名称', () => {…})`；`assert.equal(a,b)` → `expect(a).toBe(b)`；`assert.deepEqual(a,b)` → `expect(a).toEqual(b)`；`assert.ok(x)` → `expect(x).toBeTruthy()`；外层包 `describe('dashboard', () => {…})`；import 路径 `../src/dashboard.js` → `../src/host/dashboard.js`。**29 个用例名与断言值一个不动**（含 `请求输入 619.9万（缓存命中 531.5万 · 85.7%）`、`day-warn:2026-09-10` 日键、摸鱼阶梯 15/25/35/60 边界、`（缓存命中 0 · 0.0%）` 零值、`· 含子代理` 尾注）。

- [ ] **Step 4: 四绿验证**

Run: `npm run build && npm run validate && npm test && npm run typecheck`
Expected: 全绿，`dashboard` 套件 29 用例通过。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(v2.1): dashboard 纯函数原样移植 + 29 用例转 vitest（契约 cherry-pick）"
```

---

### Task 2: state.js 扩展——scanSession 增 metrics + 引擎内 dashboard 聚合器

**Files:**
- Modify: `src/host/state.js`
- Test: `test/dashboard.test.mjs` 追加（或新建 `test/state-dashboard.test.mjs`）

**Interfaces:**
- Consumes: Task 1 全部导出。
- Produces:
  - `scanSession(session, getTitle, dayStartMs)` 第三参可选；返回值追加 `metrics`：`{ turns, errors, toolCalls: Record<string,number>, toolDurMs: Record<string,number>, longestTurnMs, lastEventTime: number|null, pendingList: {id,at}[] }`（v1.4.0 `src/index.js` scanSession 的 metrics 段逐行移植：正向一遍收集，倒序 decidedIds 过滤未决审批）
  - `createStateEngine(deps)` 返回值追加 `dashboard(): DashboardAgg | null` 与内部每轮 compute 聚合；`DashboardAgg = { pace, usage: { day, requestTotal, cacheHitRate, userEst, session, grandTotal, models: {} }, alerts, approvals, summary }`（字段契约 = v1.4.0 `/state.dashboard`，见 `git show 886b303:src/index.js` 聚合段；alerts 经 evaluateAlerts 跨阈值判定并传 dayKey）
  - deps 增可选 `readConfig(): { paceEnabled, paceIntenseEvents, paceLongrunMin, paceLoafStartMin, usageEnabled, dayLimitTokens, milestoneUnit, approvalFlickerMin }`（缺省时全按默认门开）

- [ ] **Step 1: 失败测试**（vitest，造 4 个会话的假 events 数组直接喂聚合器——不走 cordis，聚合器须以纯函数 `buildDashboard(sessionsData, cfg, now)` 形式导出便于直测）：断言跨会话 pace 聚合（任一 turnOpen 即 active 带）、usage 今日桶累加、approvals 未决+等待分钟、summary 的 tokensText 名词、userEst 累加。

- [ ] **Step 2: 跑测确认失败** → `npm test` FAIL（函数不存在）。

- [ ] **Step 3: 实现** —— 从 `git show 886b303:src/index.js` 的 `scanSession` metrics 段与 computeProjects 聚合段移植（`events5mCount` 锚定 now 的修正一并带上），组装为 `src/host/state.js` 内导出的 `buildDashboard(...)` 纯函数 + `createStateEngine` 内的每轮调用与缓存。`sessionEvents` 改用本仓 `./dashboard.js` 的实现（v2 的 readEvents 仅 snapshotEvents——保持不动，metrics 扫描统一走 `sessionEvents()` 以复用双采集一次原则）。

- [ ] **Step 4: 测试通过 + 四绿 + Commit**

```bash
npm run build && npm run validate && npm test && npm run typecheck
git add -A && git commit -m "feat(v2.1): state 引擎扩展 metrics 与 dashboard 聚合器（纯函数 buildDashboard 可直测）"
```

---

### Task 3: host/index.js 接线——Config 12 字段 + snapshotExtra 下发 dashboard

**Files:**
- Modify: `src/host/index.js`（Config schema、readConfig 键集、snapshotExtra 组装）

**Interfaces:**
- Produces: `GET /state` 响应新增顶层 `dashboard` 字段（= 引擎 `dashboard()` 输出；引擎未就绪时为 `null`）；`StateSnapshot` 类型同步（Task 4）。

- [ ] **Step 1: Config 增 12 字段**（schemastery，默认值见 Global Constraints；键名逐字一致）。
- [ ] **Step 2: readConfig 键集**并入 12 键（v2 已有逐键回退模式，照抄现有行）。
- [ ] **Step 3: snapshotExtra** 追加 `dashboard: engine.dashboard()`。
- [ ] **Step 4: 路由测试**（vitest）：mock webServer 注册表直调 handler，断言响应含 dashboard.pace.tier='idle'（无会话）、usage 四桶零值、summary.tokensText 含「请求输入 0（缓存命中 0 · 0.0%）」。
- [ ] **Step 5: 四绿 + Commit** `feat(v2.1): /state 下发 dashboard 快照 + 12 项配置`

---

### Task 4: 客户端契约——StateSnapshot 类型 + config 12 键 + i18n 双语键

**Files:**
- Modify: `src/client/api.ts`（StateSnapshot 增 `dashboard: DashboardSnapshot | null`；导出 `DashboardSnapshot` 接口：pace/usage/alerts/approvals/summary 字段一一对应宿主）
- Modify: `src/client/config.ts`（PetConfig 增 12 键 + 默认值 + `NUM_KEYS`/`NUM_RANGE` 钳制表——从 `git show 886b303:src/client.js` 的 CFG_DEFAULT/NUM_RANGE 段移植）
- Modify: `src/client/i18n.ts`（新增 `dash.*` 双语键，完整清单：

```ts
// zh / en 成对
"dash.today": "今日" / "Today",
"dash.requestInput": "请求输入" / "Request input",
"dash.hit": "命中" / "Hit",
"dash.cacheHit": "缓存命中" / "Cache hit",
"dash.output": "产出" / "Output",
"dash.yourInput": "你的输入" / "Your input",
"dash.estimateSuffix": "(估)" / "(est.)",
"dash.withSubagents": "含子代理" / "incl. subagents",
"dash.sessionReq": "本会话" / "This session",
"dash.summaryTitle": "工作小结" / "Summary",
"dash.farewellTitle": "今日收工" / "Done for today",
"dash.summaryEntry": "📖 总结" / "📖 Summary",
"dash.menuUsage": "🏷 今日用量" / "🏷 Today's usage",
"dash.menuSummary": "📊 查看最近总结" / "📊 Recent summary",
"dash.menuSessions": "🗂 会话一览" / "🗂 Sessions",
"dash.unsaved": "有未保存更改" / "Unsaved changes",
"dash.saved": "已保存 ✓" / "Saved ✓",
"dash.invalidNums": (n) => `${n} 个数值无效` / `${n} invalid value(s)`,
"dash.save": "保存" / "Save", "dash.discard": "放弃" / "Discard",
"dash.caliberNote": "用量均为纯 token 口径，不折算金额" / "Usage is token-only, never converted to money",
"dash.noActive": "暂无进行中会话" / "No active sessions",
```

i18n 现有 t() 若不支持参数，加一个 `tf(key, n)` 变体；`invalidNums` 与现有字典风格一致即可。)

- [ ] **Step 1: 类型与配置先行 + typecheck 绿**
- [ ] **Step 2: i18n 完整性测试**（v2 有 i18n 完整性用例——zh/en 键集必须相等，照现有用例样式补 dash.* 断言）
- [ ] **Step 3: 四绿 + Commit** `feat(v2.1): 客户端 dashboard 契约类型 + 配置 12 键 + 双语文案`

---

### Task 5: Pet.tsx 接入——档位动画 + alerts 举牌语音 + 黑板触发 + 卡片年龄标注

**Files:**
- Create: `src/client/Sign.tsx`（举牌：props `{ text }`，样式从 `git show 886b303:src/client.js` 的 `.dyn-pet-sign` 移植）
- Modify: `src/client/Pet.tsx`
- Modify: `src/client/styles.css`（v2 若为 CSS 文件则并入；否则并入其样式载体）——新增 `.dyn-pet-sign/.dyn-pet-age` 及本计划后续组件样式

**Interfaces:**
- Consumes: `appStore.getSnapshot().dashboard`（store.ts 已轮询 /state，dashboard 字段随快照到达——确认 store 的 StateSnapshot 泛型已带 Task 4 类型）。

实现要点（全部为对 v1.4.0 已验证逻辑的 TSX 化，源锚点 `git show 886b303:src/client.js`）：
1. **档位**：`const dash = snap?.dashboard`；`tierAnim()`：`task==='running' && pace.tier==='longrun'` → `'waiting'`，插入 `refreshAnim` 链 `drag || transient || task || tierAnim() || look || idle`（源锚点 tierAnim 段）；`pace.tier` 以 `loaf` 开头时 `scheduleNextLook`/look 启动处直接重排（抑制环视）。**不变速**。
2. **alerts 消费**：`seenAlertsRef = useRef(new Set<string>())`；snap 差分 effect 内（现 completions 差分同处）遍历 `dash.alerts`，未见 id → setSign(text)+4.2s 清除+`playTransient('jumping',1600)`+语音三优先级：`playVoice('usage','jumping')`；voices.ts 的 VoiceGroup 联合类型加 `'usage'`（pick 落空自动静默已由 v2 闸门链保证）；无 usage 组且 `cfg.ttsEnabled` → `window.speechSynthesis.speak(new SpeechSynthesisUtterance(text))`，`lang='zh-CN'`。
3. **完成→📖入口**：completions 差分处若 `cfg.summaryEnabled` → `setEntry(true)` + `later(clear, summaryEntrySec*1000)`。
4. **年龄标注**：卡片 `lines` 首行渲染处追加 `p.age ? <span className="dyn-pet-age"> {p.age}</span> : null`；宿主侧 `projectsList` 输出已在 v1.4.0 带 `age`（Task 2 聚合时给 engine 现有 projectsList 的输出项补 `age: ageLabel(...)`——若 v2 state.js 的 projects 输出不带，在此任务一并补上）。

- [ ] **Step 1: 类型检查先行**（`npm run typecheck`）→ **Step 2: 四绿** → **Step 3: Commit** `feat(v2.1): 档位动画/警报举牌三优先级语音/总结入口/年龄标注接入 Pet`

---

### Task 6: MiniBar.tsx——五口径悬停条 + 节奏表盘

**Files:**
- Create: `src/client/MiniBar.tsx`
- Modify: `src/client/Pet.tsx`（挂载 + hover 0.5s + 手动模式 + 拖拽隐藏 + 黑板互让）
- Modify: 样式载体（`.dyn-pet-mini*` 全套，从源分支 CSS 段移植，宽 248px、行 `white-space: normal; word-break: break-word`）

**Interfaces:**
- Props: `{ dash: DashboardSnapshot | null; cards: ProjectCard[]; mode: 'hover'|'manual'; scale: PetScale }`；渲染行（文案经 t()，五口径名词逐字 = Task 4 键）：
  1. 表盘行：`TIER_PCT` 条 + `pace.label`（PACE_LABELS 值需 i18n 化：宿主下发的是中文 label——改为客户端按 `pace.tier` 查 `dash.tier.<tier>` 键，宿主 label 字段保留作兜底）
  2. `今日 请求输入 X · 命中 Y%`（X=fmt(requestTotal)，Y=(cacheHitRate*100).toFixed(1)）
  3. `缓存命中 X · 产出 Y`
  4. `你的输入 ~X(估) · 含子代理`
  5. 状态行（approval/running/done 计数，空则 `dash.noActive`）
  6. `本会话 请求 X · <title>`（usage.session）
- `fmt`：v1.4.0 的万/亿格式化函数移植到 `src/client/format.ts`（导出 `fmtTokens(n): string`，含单测）。
- scale 适配：根节点 `transform: scale(cfg.scale)` 或字号乘 scale（与 v2 卡片/菜单的缩放实现方式保持一致——读 `Pet.tsx` 现有 scale 应用点照做）。
- 交互（6.6 规则）：容器 `pointer-events: none`（纯读取）；hover 由 Pet 的 sprite `onPointerEnter/Leave` + 500ms 定时驱动；`stateRef.drag` 激活时强制隐藏；`board !== null` 时不渲染（黑板优先）。

- [ ] **Step 1: format.ts + 单测**（万/亿/尾零/9999 边界）→ **Step 2: MiniBar 组件 + typecheck** → **Step 3: Pet 挂载与交互 + 四绿** → **Step 4: Commit** `feat(v2.1): 五口径迷你条+节奏表盘（悬停/手动/拖拽隐藏/黑板互让/scale 适配）`

---

### Task 7: Board.tsx——小黑板 + 📖 入口 + 关宠 farewell + 菜单三项

**Files:**
- Create: `src/client/Board.tsx`
- Modify: `src/client/PetMenu.tsx`（三项：`dash.menuUsage`→setMiniMode('manual')；`dash.menuSummary`→setBoard({mode:'manual'})；`dash.menuSessions`→会话一览子页：列 cards（色点+title），点击走既有 `onProjectClick` 等价跳转并关菜单）
- Modify: `src/client/Pet.tsx`（board state `{mode:'manual'|'farewell'}|null`；入口点击→manual；petStore 隐隐 true→false 且 summaryEnabled→farewell+boardTtlSec 自动关；✕/点外部/ESC 三路等价关闭；ttl 定时统一管理）
- Modify: 样式载体（`.dyn-pet-board*` 310px、行 normal 换行；`.dyn-pet-entry`；黑板行文案直接渲染宿主 `summary.tokensText` + `会话 N · turn N · 报错 N` / `工具 Top3 toolsText` / `最长单 turn longestText`——宿主拼好的字符串原样显示，**五口径名词已在宿主侧 zh**；en 场景下黑板行由宿主 zh 字符串展示的问题：宿主 summarize 文案改为输出结构化字段+客户端拼装——`tokensText/toolsText/longestText` 拆为 `tokens{requestTotal,cacheRead,hitPct,output,userEst}` 结构化输出，客户端用 t() 拼；DashboardSnapshot.summary 类型同步。此为本任务内的一次性重构，断言相应更新）

- [ ] **Step 1: 宿主 summary 结构化**（Task 2/3 测试同步改）→ **Step 2: Board/入口/菜单 + 四绿** → **Step 3: Commit** `feat(v2.1): 小黑板+限时入口+关宠分发+菜单三项与会话一览（黑板行双语结构化）`

---

### Task 8: 标题闪烁 + ESC 分层 + 滚轮穿透

**Files:**
- Modify: `src/client/Pet.tsx`

三段效果（源锚点 `git show 886b303:src/client.js` 对应 effect，TSX 化）：
1. **标题闪烁**：`useEffect([], …)` 1.5s tick + `visibilitychange`；条件 `approvalFlickerMin>0 && dash.approvals.some(a=>a.waitMin>=min) && document.visibilityState!=='visible'`；1s 轮换 `🦊 审批等待中…`（i18n `dash.flickerTitle`）；恢复条件 = decided 或回页。
2. **ESC 分层**：PetMenu 若已有自身 ESC 关闭，改为统一 effect：`Escape` → manual 迷你条 → 黑板 → 菜单，一次剥一层。
3. **滚轮穿透**：宠物根 `onWheel` → `elementFromPoint`（临时摘 pointer-events）→ `scrollableAncestor`（overflowY 检查的父链爬升）→ `scrollTop += deltaY`；黑板/菜单内部滚动不经过该 handler。

- [ ] **Step 1: 实现 + typecheck** → **Step 2: 四绿 + Commit** `feat(v2.1): 标题闪烁/ESC 分层/滚轮穿透（spec 6.6 规则 7/8）`

---

### Task 9: SettingsCard 草稿化——在 v2 既有壳上并入统一保存

**Files:**
- Modify: `src/client/SettingsCard.tsx`（保留现有壳/header/chevron/双分区结构，改造为草稿模式）
- Modify: `src/client/i18n.ts`（缺的键已在 Task 4 就位）

改造规则（源锚点 `git show 886b303:src/client.js` 的 SettingsCard v1.4.0 版，映射进 v2 壳）：
1. `draft` state + `dirtyRef`；全部控件（既有 3 开关 + 4 动作下拉 + 缩放 + **新增 12 键**）写 draft；
2. 头部右侧状态字（`dash.unsaved` / `dash.saved` / `dash.invalidNums`），收起态可见；
3. `保存`：diff 键集合 → `cfgStore.set(patch)`，**数字键 `Number(draft[k])` 先转**（根因修复）+ NUM_RANGE 钳制 → `setSavedFlash(true)` 1.6s + `setOpen(false)`（保存自动收起）；
4. 无效数字（空/NaN）红框 `.dyn-pet-card-row.bad input` 且保存禁用；
5. 外部变更（右键菜单/宠物管理写同键）仅无未保存修改时 rebase draft；
6. 12 键的 UI 分组：`dash` 分区放在「配置」区内（基础开关之后、动作下拉之前），数字行为 `<input type="number">`、开关为 checkbox；标签 i18n（`dash.cfg.<key>` zh/en 各 12 条，标签文案沿用 v1.4.0 中文语义）。

- [ ] **Step 1: typecheck** → **Step 2: 四绿 + Commit** `feat(v2.1): 设置卡草稿+统一保存（数字转 Number 修复静默丢配）+12 键入卡`

---

### Task 10: v2.1.0 发版收尾

**Files:** `package.json`、`dsh.plugin.json`、`CHANGELOG.md`、`README.md`、`README.en.md`

- [ ] 版本三处 2.1.0；CHANGELOG 新段（效率看板移植条目 + 五口径 + 设置卡保存 + 兼容 0.1.5-rc.2 说明）；README 双语「效率看板」小节（功能清单 + 交互速查表，内容取 spec 6.6 三层表）；四绿；Commit `chore(release): v2.1.0 效率看板移植`。

---

### Task 11: 端到端验证（宿主 0.1.5-rc.2）——跑通 + 截图 + 点击测试

**前置（宿主升级，流程已验证）：**

```bash
cd /Users/jarvis/Documents/DeepSeek/deepseek-harness
git fetch origin --tags && git checkout dsh-v0.1.5-rc.2
pnpm install
# 从干净构建 workaround（上游 tsdown brace 入口缺陷仍未修）：
find packages apps -name lib -type d -not -path '*/node_modules/*' -prune -exec rm -rf {} +
find . -name '*.tsbuildinfo' -not -path '*/node_modules/*' -delete; rm -rf apps/web/dist
# 205 个借用包 + vendor + 根补齐 lib/types/{index,invariant,startup}.js 桩（内容 "export {}"）
# tsdown.config.ts 入口临时改显式数组 → pnpm run build → git checkout -- tsdown.config.ts
# 构建后：空 bundle(<60B) 且 lib/types 同名真身存在者改写为 `export * from "./types/<n>.js"`（default 导出补转发）
```

- [ ] **11a 服务跑通**：`pnpm dsh web --no-open` 起服务；`curl /state?token=…` 断言：`dashboard.pace.tier` 存在、五口径数值字段齐全、`projects[].age` 存在、v2 既有字段（activePet/pets/guard）回归无损；**插件在插件树加载日志无错**。
- [ ] **11b UI 截图**（浏览器自动化打开带 token 的 3080 页面，逐张截图存 `docs/superpowers/plans/artifacts/v2.1-e2e/`）：
  1. 宠物本体+状态卡（右下角默认态）
  2. 悬停宠物 0.5s 后的迷你条（五口径+表盘）
  3. 右键菜单全貌（含三项新入口）
  4. 会话一览子页
  5. 黑板展开态（有数据或零值态）
  6. 设置页 Foxbell 卡（收起态）与展开态（12 键+保存/放弃）
  7. 举牌/年龄标注（若有活动触发；无法稳定触发则注明跳过原因）
- [ ] **11c 点击测试**（逐项记录通过/失败）：
  1. 点状态卡 → 会话切换发生（`ctx.sessions.open` 路径）
  2. 点「今日用量」→ 迷你条手动出现；ESC → 关闭
  3. 点「查看最近总结」→ 黑板出现；✕ 与点外部分别验证关闭
  4. 设置卡：展开 → 改一个数字 + 一个开关 → 头部出现「有未保存更改」→ 保存 → 卡片自动收起 → 刷新页面值仍在（数字持久化=根因修复验证）
  5. 填非法数字（清空）→ 红框 + 保存禁用 + 「1 个数值无效」
  6. 拖拽宠物 → 物理正常且迷你条隐藏；宠物上滚滚轮 → 页面滚动穿透
  7. 双击 → 说话；单击 → 挥手（回归）
- [ ] **11d 回归**：外部宠物切换/导入流程抽查一项（v2.0.0 行为无破坏）；宠物隐藏→farewell 黑板出现后消失。
- [ ] **执行报告**：`docs/superpowers/plans/2026-09-11-v2.1-port-report.md`——任务×commit×四绿对照表、偏差清单、11a-d 逐项结果与截图引用、已知问题。Commit + push 分支（**不开 PR、不合 main——等 review**）。

---

## Self-Review 记录

- 契约覆盖：接口地图 10 行 ↔ Task 1-9 逐行对应（dashboard.js→T1、聚合→T2、/state→T3、客户端契约→T4、档位/举牌/入口/年龄→T5、迷你条→T6、黑板/菜单→T7、闪烁/ESC/滚轮→T8、设置卡→T9）；行为不变量 6 条落在 T1（29 用例）+T2（聚合测试）+T6/T7/T8（交互）；D1 版本→T10；D2 关 #5 删分支→review 合并后动作（不在本计划）；D3 双语→T4/T7/T9；端到端+截图+点击（用户特别要求）→T11。
- 占位符扫描：T11 前置的 workaround 为已验证流程的引用式步骤（桩/改写脚本在 v1.4.0 会话已执行过，逐条命令化）；无 TBD。
- 类型一致性：`DashboardSnapshot`（T4 定义）在 T5-T8 消费；`buildDashboard`（T2）为 T3 唯一快照来源；PACE tier 联合 `'intense'|'active'|'longrun'|'idle'|'loaf1'|'loaf2'|'loaf3'|'loaf4'` 贯穿宿主/客户端类型；`fmtTokens`（T6）供 T7 复用。
