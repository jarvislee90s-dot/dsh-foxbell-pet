# v2.1.0 效率看板移植 执行报告

- 日期：2026-09-11
- 分支：`feature/v2.1-efficiency-dashboard`（自 `origin/main @ 4750e61`，**已推送远端同名分支；未开 PR、未合 main、未动 `feature/efficiency-dashboard-p1`**——等 review）
- 移植源：`feature/efficiency-dashboard-p1 @ 886b303`（只读，`git show` 取代码）
- 实测宿主：`/Users/jarvis/Documents/DeepSeek/deepseek-harness @ tag dsh-v0.1.5-rc.2`（源码宿主，`pnpm dsh web`）
- 执行方式：subagent-driven-development——每任务独立实现者 + 独立评审者（spec 合规 + 代码质量双裁决），全任务后终审；进度台账 `.superpowers/sdd/progress.md`

## 1. 任务 × commit × 四绿对照表

四绿 = `npm run build && npm run validate && npm test && npm run typecheck` 全绿，每任务收尾时实跑通过后提交。

| 任务 | commit | 内容 | 四绿 | 评审 |
|---|---|---|---|---|
| docs 入库 | 2ecbfd6 / 98b2032 / a270457 | 契约×2 + 计划 cherry-pick（279d20b^..源分支尖） | n/a（纯 docs） | — |
| Task 1 | 9cde679 + a01b6bc + 55ef2e8 | main 基线修复（既存红）+ validate 豁免 + dashboard.js 逐字节移植 + 29 用例转 vitest | ✓ 282/282 | 双 ✓ |
| Task 2 | 926c237 | state.js scanSession metrics + 纯函数 buildDashboard + 引擎 dashboard()/readConfig | ✓ 297/297 | 双 ✓ |
| Task 3 | 8149326 | host/index.js Config 12 字段 + readConfig 接线 + snapshotExtra 下发 dashboard + 宿主路由测试 | ✓ 304/304 | 双 ✓ |
| Task 4 | 7b47b74 | DashboardSnapshot 类型 + 客户端 12 键/NUM_RANGE 钳制 + i18n dash.* 22 键双语 + 完整性断言 | ✓ 308/308 | 双 ✓ |
| Task 5 | 6c0888c | 档位动画（tierAnim 在 s.task 前）/ 警报举牌三优先级语音 / 📖 入口触发 / 卡片年龄标注（宿主 age） | ✓ 310/310 | 双 ✓ |
| Task 6 | 3e58d16 | format.ts fmtTokens（与宿主逐值恒等语料钉死）+ MiniBar 六行五口径 + 表盘 + hover/manual/拖拽隐藏 + 248px 样式 | ✓ 315/315 | 双 ✓ |
| Task 7 | 20d71a6 | Board.tsx 黑板（双语结构化行拼装）+ 📖 入口渲染 + farewell 分发 + 菜单三项 + 会话一览 + 宿主 summary 增量化结构化 | ✓ 321/321 | 双 ✓ |
| Task 8 | fed966f | 标题闪烁 + ESC 剥层（迷你条→黑板→菜单）+ 滚轮穿透 | ✓ 321/321 | 双 ✓ |
| Task 9 | 37feea0 | 设置卡草稿 + 统一保存（数字 Number 转换根因修复）+ 12 键入卡 + 红框/禁用/计数 | ✓ 326/326 | 双 ✓ |
| Task 10 | 61dfcfd | v2.1.0 发版：版本三处 + lock + lib 重建 + CHANGELOG + README 双语效率看板小节 | ✓ 326/326 | 双 ✓ |
| Task 11 | （本报告 + 01d4964 + 终审修复提交） | 端到端 11a-d（§4） | ✓（最终 326/326） | 终审 ✓ |

终审（whole-branch，4750e61..HEAD）：「Ready to merge: With fixes」→ 三项前置全部落地：① 状态卡右键守卫（见 §3.9）、② 本报告、③ 分支推送。累计 12 个任务评审 + 终审全部通过，无未修复的 Critical/Important。

## 2. 偏差及理由（计划/契约之外的实现裁定）

| # | 偏差 | 理由 | 证据 |
|---|---|---|---|
| D-1 | `src/host/dashboard.js` / `test/dashboard.test.mjs` 用 **.js/.mjs 逐字节原样复制**，而非契约接口地图所写 `dashboard.ts` TS 化改写 | 计划是执行依据且晚于契约定稿（architecture 一节明言「宿主侧纯 JS（v2 惯例）」）；逐字节复制使「逻辑原样移植」可机器验证（`git show 886b303:src/dashboard.js \| diff -` 为空） | Task 1 评审逐字节验证 |
| D-2 | Task 1 两个计划外提交：a01b6bc validate.mjs 对 dashboard.js 双兼容读取器的精确豁免；9cde679 `@types/react` ^18→^19 + bundle-smoke react-dom mock（**main 既有红**，4e4f2bf 起 typecheck/install 挂） | 逐字节约束与 validate 零容忍守卫硬冲突；基线红不修则四绿门槛无法成立。两处均最小化、独立提交、评审独立证实 | Task 1 评审 |
| D-3 | VoiceGroup：'usage' **未并入** validation.GROUPS，改为新增 `PLAY_GROUPS` 类型超集 | judgeVoiceTier 四组「全有或全无」——并入会让所有现役宠物 hasVoice 翻转 false、导入 UI 增必填组；三优先级链可观察行为不变 | Task 5 评审机制级证实 |
| D-4 | usage 语音组在 v2 实际不可达（manifest VOICE_GROUPS 只认四组，usage 组语音无法声明），警报语音实际走 TTS/静默 | 与 v1.4.0 实测环境可观察行为一致（当时也无 usage 语音素材）；语音组音频优先级链路已完整落位，宠物包生态支持后自动生效。CHANGELOG/README 已如实括注 | Task 5 评审 + CHANGELOG [2.1.0] |
| D-5 | 宿主 summary 结构化为**增量式**（summarize() 原样 + 新增 summarizeStructured 结构化字段，spread 合并下发） | 计划 Task 7 要求结构化重构并与客户端拼装，但 29 个移植用例逐字断言 summarize() 的 zh 文案（硬约束）。增量式同时满足两边；结构化与 zh 字符串双向一致性由双侧测试钉死 | Task 7 评审（29 用例 diff 为空） |
| D-6 | ESC 菜单层走 closeMenu()（连带剥 menuPage + 停预览）；闪烁文案挂载时 t() 捕获 | v2 对源 setMenu(null) 的收尾等价（还修了源在子页上的预览泄漏）；源即硬编码 zh + effect []，运行中切语言不热切 | Task 8 评审语义等价证实 |
| D-7 | 设置卡保存后草稿对齐快照（消除 v1.4.0 的 "12.4" 编辑残差误报） | 有注释的有意微改进；钳制取整后的残差会误亮「未保存」 | Task 9 评审 |
| D-8 | 设置卡 12 键组位置：基础开关之后、动作下拉之前（v1.4.0 在动作之后） | 计划 Task 9 第 6 条明文规定此顺序 | Task 9 评审 |

## 3. 自检修改清单（自检 a + b + 终审修复）

1. **黑板 scale 适配**（01d4964）：契约接口地图要求新组件「位置与字号随三档缩放」，Board 此前 310px 固定。在 Pet.tsx boardLayer 包装层加 `transform: scale(scale)`（origin bottom-right），入口按钮字号 `px(12)`。
2. **README zh/en 语音优先级括注**（01d4964）：补「当前内置与外部宠物清单均无 usage 语音组，阈值/里程碑警报实际走 TTS/静默」，与 CHANGELOG 口径一致（终审 Minor 建议）。
3. **validate.mjs L184 注释同步**（01d4964）：原文称 dsh.plugin.json「1.3.0 冻结」，与 Task 10 按计划升 2.1.0 矛盾，注释更新（断言逻辑不变）。
4. **状态卡右键守卫**（终审修复提交）：`.dyn-pet-proj` 补 `onContextMenu stopPropagation`——恢复 v1.4.0 的「右键仅本体」不变量（源 L867；v2.0.0 基线本就没有，本分支重写该 JSX 时补齐，终审 Important #1）。
5. 每处修改后四绿重跑通过（326/326）。

## 4. 端到端验证（宿主 dsh-v0.1.5-rc.2）

### 前置：宿主升级 + 干净构建 workaround

- `git checkout dsh-v0.1.5-rc.2` + `pnpm install`。
- 上游 tsdown brace 入口缺陷仍在，按计划前置流程执行：清理 lib/tsbuildinfo/web dist → tsc -b（host/client）→ lib/types/{index,invariant,startup}.js 桩补齐（564 个工作区包 + 9 个非包两层目录——后者为 tsdown workspace 发现口径所需，计划文本之外的新发现）→ tsdown.config.ts 入口临时改显式数组 → tsdown host/client 两面构建 → 配置还原（`git checkout --`）→ 空 bundle 改写为 `export * from "./types/<n>.js"`（host 面 12 处 + client 面 9 处；vendor/schemastery 需临时以 src/index.ts 为入口才能让 client 面跑完，随后还原）。
- `pnpm build:web` 前端构建成功；**宿主 tracked 源码零改动**（git status 仅 gitignore 范围内的 lib 产物与 .zcode/；tsdown 配置全部还原）。
- 插件经既有 `~/.dsh/profiles/web` link 指向本工作树加载。起服前结束了一个**遗留一日的旧 0.1.3 服务**（占用 3080）。

### 11a 服务跑通 —— PASS

- `pnpm dsh web --no-open` 起服，插件挂载日志无错：`[foxbell-pet] host mounted: sprite=2654210 builtinVoices=31 assetDir=…/dsh-foxbell-pet/assets pets=0`。
- `curl /dyn-pet-foxbell/state?token=…` 断言全部通过：`dashboard.pace.tier='idle'`（含 sinceMs/label）；usage 七字段齐全（day 四桶 0 / requestTotal / cacheHitRate / userEst / grandTotal / models {} / session null）；alerts=[]、approvals=[]；summary 双形态（zh tokensText `请求输入 0（缓存命中 0 · 0.0%）· 产出 0 · 你的输入 ~0(估) · 含子代理` + 结构化 tokens{...}）。
- v2 既有字段回归无损：seq/completions/runningSessions/projects/voices/activePet/pets/guard/assetDir/spriteBytes/diag 全在。
- `projects[].age`：实测环境无活动会话（projects=[]），live 断言不可行；由 test/host-index.test.mjs（真实 apply() + /state 处理器）与 test/state.test.mjs 的 age 用例覆盖。

### 11b UI 截图 —— 11 张入库（docs/superpowers/plans/artifacts/v2.1-e2e/）

| 文件 | 内容 |
|---|---|
| 01-pet-default.png | 宠物本体默认态 |
| 02-mini-bar-hover.png | 悬停 0.5s 迷你条：表盘（Idle）+ 五口径四行 + 状态行（No active sessions），en 双语实拍 |
| 03-context-menu.png + 03b-…-bottom.png | 右键菜单全貌（上/下两段）：含 🏷 今日用量 / 📊 Recent summary / 🗂 Sessions 三新入口 |
| 04-sessions-page.png | 会话一览子页（← Back + No sessions 空态） |
| 05-board.png | 黑板展开态（零值态）：Summary 头 + ✕ + 会话/turn/报错 + 五口径三分账 + 工具 Top3 + 最长 turn |
| 06-settings-card-collapsed.png / 06b-…-top.png / 06c-…-bottom.png | 设置页 Foxbell 卡收起态 + 展开态（12 键：Sound…TTS speech 开关、8 数字行、口径说明行、保存/放弃、宠物管理区） |
| 07-invalid-number.png | 非法数字态：头部「1 invalid value(s)」 |
| 08-switch-pet-regression.png | Switch pet 子页（✓ 当前宠物标记 + Switch Pet… 入口） |
| （举牌/年龄标注） | **跳过**：环境无活动会话→无 token 事件→无阈值/里程碑警报源，无法稳定触发；举牌组件渲染与年龄标注由测试覆盖 |

环境说明：IAB 截图管线有加倍/超时毛刺，全部成图均经视觉核验；>~450px 的 clip 会触发合成加倍，采用分段截图。

### 11c 点击测试 —— 逐项记录

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 点状态卡 → 会话切换 | **跳过**：实测环境无活动会话、无状态卡可点；跳转路径（onProjectClick/onSessionPick → sessions.open 等价）由 statuscards-contract 测试与 Task 7 评审接线核验覆盖 | — |
| 2 | 「今日用量」→ 手动迷你条；ESC 关闭 | **PASS** | 菜单点击后 mini DOM 出现；Escape 后消失 |
| 3 | 「查看最近总结」→ 黑板；✕ 关闭；点外部分别验证 | **PASS** | board 出现（manual 无 farewell 类）；✕ 后消失；CUA 真实点击空白处后消失 |
| 4 | 设置卡改值 → 保存 → 自动收起 → 刷新持久 | **PASS** | 改 Board stay 15→20 + 开 TTS → 头部「Unsaved changes」→ Save →「Saved ✓」+ 卡片自动收起（h 815→74）→ **整页刷新后值仍在（20/true，数字持久化=根因修复实机验证）** |
| 5 | 非法数字（清空）→ 红框 + 保存禁用 + 计数 | **PASS** | 头部「1 invalid value(s)」；行 .bad；输入框边框 rgb(239,68,68)；Save disabled；截图 07 |
| 6 | 拖拽 → 物理正常且迷你条隐藏；滚滚轮 → 页面滚动穿透 | **PASS** | 合成指针序列拖拽位移精确（+140/+60 → 矩形 +140/+60）；拖拽中采样迷你条已隐藏（含手动模式）；宠物上 wheel → 应用滚动容器 scrollTop 0→295.9 |
| 7 | 双击 → 说话；单击 → 挥手 | **PASS** | 双击后气泡「今天也要一起加油哦！」；单击无气泡（每目标单行为） |

说明：CUA 不支持右键、Playwright 可操作性检查在该 IAB 环境不可靠——右键菜单与部分菜单项点击采用页面内合成事件（记录为环境适配，非产品行为改动）；其余为 CUA 真实坐标点击/拖拽/滚轮。

### 11d 回归 —— PASS

- **farewell**：菜单「Hide pet」→ 宠物隐藏（root display none）+ farewell 黑板出现（`dyn-pet-board farewell`，「Done for today 🦊」+ 总结行）→ **boardTtlSec(=20，恰为 11c-4 保存值) 后自动消失** → 侧栏 🦊 开关恢复显示。
- **外部宠物抽查**：Switch pet 子页正常渲染（当前宠物 ✓ 标记、外部宠物空列表、Switch Pet… 导入/管理入口）；导入全流程需 zip 资产，抽查以子页结构 + 守卫/清单既有测试为准。
- 宿主仓库 tracked 源码零改动复查通过。

## 5. 已知问题（不阻塞合并；终审 triage 为 follow-up）

1. usage 语音组 v2 不可达（D-3/D-4）——待宠物包生态声明 usage 组后三优先级第一级自动生效。
2. 引擎每轮对同一事件数组走两遍（scanSession + buildDashboard 各一次 O(n) 扫描）——纯 CPU 议题，事件量大时的 metrics 复用优化（issue #4 技术债区）。
3. 举牌 ttl 定时器不取消（连续警报可能截断前一牌面）、标题闪烁 base 快照被宿主后续改标题覆盖、wheel pointerEvents 恢复无 try/finally、设置卡净零编辑阻塞外部 rebase、Saved ✓ 无条件闪现——均为 v1.4.0 源语义原样继承，评审记录在案。勘误（review Minor4）：`sign !== null` 对空串告警的渲染差异**并非**源语义继承，是对源真值判断（`sign ? …`）的微漂移，已在本 review 处置提交中改回真值判断；举牌 ttl 定时器同步补了卸载清理。
4. `dsh.plugin.json` 的 `engines.dsh: ">=0.0.1"` 低于文档基线 0.1.2-rc.1（v2.0.0 遗留），下次触碰该文件时对齐。
5. `models` 用量桶保持空 `{}`（一期范围外，issue #4 F05 预留）。
6. 测试环境遗留：`~/.dsh` 中 foxbell-pet 配置经测试后已恢复默认（15/false，Saved ✓ 实证）；dsh web 服务（rc.2）仍在 3080 运行可供复验，token 见 `/tmp/dsh-web.log`。

## 6. 结论

11 个任务全部完成并通过双评审 + 全分支终审；契约 6 条行为不变量、12 项配置逐字约束、29 个移植用例零改动均经机器/人工双重验证；端到端三项（跑通/截图/点击）完成，两项跳过项均有环境性理由与替代覆盖。分支已推送，等 review。
