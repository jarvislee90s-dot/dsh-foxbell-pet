# v2.2.1 可用性修复计划：原生件 / 悬浮条桥接 / 导出卡改版 / 自定义评语内联 / 唯一发声方

- 日期：2026-09-13
- 分支：`feature/v2.2.1-usability-fixes`（自 main 337b111）
- 状态：已完成（2026-09-13），停在分支等 review
- 提交：54a2390 计划 / 8527439 B hover 桥接 / af81f13 C+D 导出改版+评语内联 / 310403a E 唯一发声方 / e8f9fdb F 版本 2.2.1
- 关联：v2.2.0（PR #7 已合并）、issue #4

## 背景（用户报告）

1. 本地源码版 harness 新建/继续会话必报 `Cannot find module … native/system/packages/darwin-arm64/bin/system.node`（flock）——会话完全不可用。
2. 报错时宠物播报两条语音且同时重叠（多客户端并发各播一次）。
3. 导出图片四处不满意：①自定义评语不该放设置里，应在看板头部直达；②评语气泡压住宠物立绘；③导出图里模型名被截断（DeepSeek-V…）；④卡片要素与 Codex++ 导出图差距大——要竖排版、按其复刻（金额除外）、最下方是评论气泡+宠物，像正常聊天卡片。
4. 悬浮条鼠标离开宠物即消失，「详情 »」永远点不到。

## 工作流 A：装 flock 原生件（deepseek-harness，纯构建产物）

- `cd native/system && pnpm build:native`：cc + 本机 Node 头文件（~/.nvm v26.8.1 include/node，已确认存在）编译仓内 `packages/entry/src/flock.c` → 原子写入 `packages/darwin-arm64/bin/system.node`。离线无网络；产物路径被 `native/system/.gitignore:4 packages/*/bin/` 忽略，git 树保持干净。
- 验证：`file` = Mach-O arm64；重启 3080 dsh web；真机发一条极短消息（一次真实模型调用）→ 正常回复、无「本轮运行失败」卡、新会话目录出现 `session.jsonl.zstd`。
- 风险预案：`-Werror` 编译失败 → 停下报告，不改 harness 源码/构建脚本。

## 工作流 B：悬浮条 hover 桥接

根因：悬停进出事件挂在精灵方块（Pet.tsx sprite），悬浮条是兄弟节点且右侧留 12px 空隙；鼠标离开精灵（含穿隙）立即 `setMiniMode(null)` 卸载悬浮条。此前 E2E 用 Playwright 瞬移式 click 掩盖了该 bug（真人连续移动必现）。

- 精灵 `onPointerLeave`（hover 模式）改为启动 **200ms 宽限定时器** 而非直接卸载；
- `dyn-pet-mini-wrap` 加 `onPointerEnter` 取消宽限定时器（指针抵达悬浮条即保持）；精灵 `onPointerEnter` 同样先取消；
- 卸载清理定时器；manual 模式行为不变；
- 回归验证：Playwright `page.mouse.move(target, { steps: 20 })` 模拟真人连续移动，断言「悬停 → 分段移动到详情 → 点击 → 黑板打开」。

## 工作流 C：导出卡片改版（Codex++ 竖版浅色卡，720×1600）

参照：本机 `~/.config/Codex++/user_scripts/market-codex-daily-token-usage.js` 的导出卡（竖版浅色：标题+日期导航 / 累计 Token hero / label-value 指标行 / 趋势卡 / 按 Model 分布进度条 / 工具调用 2×2 / 底部统计）。金额元素一律不出现。

- 重写 `src/client/exportimage.ts`：背景 #f5f6f8 + 白圆角卡 → 头部（用量看板 + 日期范围徽标）→ hero `累计 Token`（完整千分位，不再 1.4万 缩写）+ 副行（turn · 请求次数 · 命中率）→ 指标行（请求输入(全文累计)/缓存命中/命中率/产出/你的输入(估·含子代理)/请求次数/最近更新）→ 趋势卡（峰值 + 蓝紫渐变折线/面积/数据点/x 轴标签）→ 按 Model 分布（≤6 行，**模型名完整不截断**，全宽圆角进度条下限 4%）→ 工具调用（调用 N · 总耗时 + 2×2 格 + Top5）→ 聊天式底部（**宠物立绘右下角完整全身可见，评语气泡在宠物左侧、间距 ≥24px，绝不重叠**）→ 页脚（纯 token · 含子代理 · 本地聚合 + DSH · Foxbell + 数据截止时间）。
- `ExportInput` 扩展：models ≤6 全名/值/占比、工具 2×2 指标 + Top5、rangeLabel、updatedAt、hitPct、turns/calls；`DashboardPanel` 抽 `toolsMetrics(tools)` 纯函数与 ToolsGrid 共用；既有纯函数保留。

## 工作流 D：自定义评语内联

- 看板头部：`复制文本`｜`导出图片`｜`按自定义评语导出`（新增）→ 内联输入行（占位符 `{range} {tokens} {hitPct} {models}`；localStorage 草稿 `dyn-foxbell-pet:export-quote`）+ `导出`；自定义非空走模板填充，留空走评语池；原「导出图片」行为不变。
- 移除设置卡「导出评语」与 `exportQuote` 配置键：client `config.ts` CFG_DEFAULT、`settingsdraft.ts` V22 键表、`SettingsCard` 行、`i18n`、宿主 `src/host/index.js` Config、sanitizeValue 字符串拦截、相关测试；`dashboardSidebarEntry`/`exportPose` 保留。

## 工作流 E：唯一发声方（多标签页语音重叠）

- 新模块 `src/client/voiceowner.ts`（Web Locks 选主）：`startVoiceOwnership()` 持 `foxbell-voice-owner` 锁至页面关闭；拿不到 2.5–3.5s 抖动重试；**可见优先**（发声方 hidden 让位、非发声方 visible 立即接手）；`navigator.locks` 缺席 → 退化为现状（不变静音）；`isVoiceOwner()` 查询。
- `index.tsx` apply 末尾启动；`Pet.tsx` 门控只管自动播报的"声"：`playVoice` 加 `{ auto }`（done/error/approval/running 传 auto=true，非发声方跳过 `voicePlayer.play`，动作/字幕各页照常）；警报链发声段前查 `isVoiceOwner()`；双击说话（用户手势）不门控。
- 真机验证：两个 Playwright 标签页 + `Audio.prototype.play` 探针，制造一次状态变化 → 仅一个页面出声。

## 工作流 F：版本与收尾

- `package.json`/`dsh.plugin.json` → 2.2.1；CHANGELOG `[2.2.1]`；四绿；停分支等 review（不合并、不开 PR）。

## 提交规划

| 工作流 | commit |
|---|---|
| B | fix(client): 悬浮条 hover 桥接（200ms 宽限） |
| C | feat(client): 导出卡片改版（Codex++ 竖版） |
| D | feat(client): 自定义评语内联 |
| E | feat(client): 唯一发声方 |
| F | chore: 2.2.1 + 文档 |
| 计划文档 | docs(计划) 首个 commit |

## 执行结果（2026-09-13）

- A：`pnpm build:native` 产出 `system.node`（Mach-O arm64 52KB）；真实会话跑通（模型回复 + 13.8K tok + 日志落盘）。
  - 额外发现并处置：第三方插件 dshmarket 于 01:26 自更新到 1.45.1，其 `lib/settings.js` 的 schemastery default 导入与 rc.2 不兼容，导致宿主启动整体失败（与本项目无关）。已隔离（`dshmarket.disabled-1.45.1` + profile package.json/package-map 移除条目，原 package.json 备份为 package.json.bak-dshmarket），恢复时改回即可。
- B：steps 分段移动回归通过（悬停→20 步移动→详情可点→黑板打开）；反向断言通过（离开 >200ms 关闭）。
- C：720×1600 竖版卡真机导出核对通过——模型全名 DeepSeek-V4-Flash、气泡不压宠物、真实数据（hero 1,269,973、命中率 72.0%、峰值 84.6万）。
- D：自定义评语内联真机验证通过——占位符全部替换（127万/72.0%），localStorage 草稿保留，导出成功。
- E：双标签页探针验证——状态变化时发声方 2 次播报、非发声方 0 次。
- F：版本 2.2.1 + CHANGELOG [2.2.1]；四绿（406/406 tests、tsc、build、VALIDATE OK）。
- 检验产物：docs/superpowers/plans/artifacts/v2.2.1-check/（A1/B/C1/D1/C2 截图）。

## 追加轮（用户对照 Codex++ 二次反馈：横版大看板"UI 字体大小配比/展示科目/模型名不省略"）

- commit 1f44814：
  - hero 改完整千分位大数字（40px/750，fmtInt 新增，弃 1,269,973→127万 缩写口径于大看板）；
  - 五口径指标从大盒格改紧凑 label/value 行（两列、细分隔线，Codex++ 同款），值改完整千分位；
  - TrendChart 平滑曲线从"中点直角贝塞尔"升级为 Catmull-Rom 样条（阶梯感消除）；
  - 横版模型行/复制文本不再 shortModel 截断（全名 DeepSeek-V4-Flash）；
  - 工具 2×2 紧凑化（值含 Top 工具名，toolsMetrics 增 topName/topDurName，topDur 语义修正为 durMs 最大者）；
  - 面板内容列 max-width 1020 居中，整体字号体系上调（13→14px 基础、标题 17px）。
  - 真机截图核对：C2-panel-restyle.png（hero 1,269,973 / 平滑曲线 9/11 峰 84.6万 / 全名模型行）。
