# 效率看板：头脑风暴与功能收敛设计（v2.1 方向）

- 日期：2026-09-09
- 状态：待用户审阅
- 关联：GitHub issue #3（效率看板）、`2026-09-06-v2-compat-and-external-pets-design.md`（v2.0.0，FR-1~29）
- 定位：本文档是 issue #3 的**功能收敛设计**——先无上限收集功能点，再筛选、归并成少量核心组件。代码级细节留给后续 plan 文档。
- 阶段关系：v2.1 方向，在 v2.0.0（兼容修复 + 外部宠物体系）完成后启动；不修改 v2.0.0 的范围。

---

## 1. 背景与定位（继承 issue #3，不可动摇）

- 本插件是**效率工具**：桌宠是 agent 工作状态的**低打扰表达层**，不做养成、不做经济、不做任何制造回访义务的游戏化系统。
- 每个提醒都必须**可行动**：提示了用户能据此做点什么（收尾会话、处理审批、换个思路）。
- 成本口径：**纯 token，不折算钱**（不维护价目表）。
- 宠物上不承载操作按钮（守「纯展示」定位），唯一的动作增强是**导航直达**。

## 2. 调研综述（四路来源，2026-09 实测）

| 调研路 | 代表对象 | 对本设计最有价值的发现 |
|---|---|---|
| LLM/Agent 用量监控 | ccusage、Claude-Code-Usage-Monitor（8.7k★）、菜单栏工具群、Langfuse、AgentOps、LiteLLM | 三级颜色阈值+「何时耗尽」预测是核心卖点；重置倒计时比百分比更有行动价值；反面教训：固定阈值不可调→告警疲劳 |
| 桌宠/助手软件 | VPet、vscode-pets、Clippy、workbuddy-buddy、openpets、awesome-codex-pet | 2024-2026 已出现成熟范式：7 状态机+渐进摸鱼动画+宠物即审批门+白名单隐私；Clippy 证明「不请自来的打断」必死 |
| 系统监控/氛围计算 | RunCat、Ambient Orb、Uptime Kuma、CCMenu、灵动岛 Live Activity、calm technology 八原则 | 主编码通道=「速度/步态+颜色+姿态」三通道冗余，文字只在展开层；打扰预算制：稳态零动画，跃迁才播一次 |
| 编码 agent 状态 UI + 告警设计 | Claude Code/Cline/Roo/Codex CLI、WakaTime、Grafana 告警、AWS Budgets、Apple HIG | 常驻可见的成本计数（不要求用户主动查）；告警分级/合并/静默/可行动是成熟学科 |

**贯穿性结论**：功能可以无穷多，但「宠物=效率看板」的形态共识非常集中——**常驻低打扰位 + 形态编码（不用文字）+ 达阈值才出声 + 每条提醒可行动**。这四条成为第 7 章实现约束的来源。

## 3. 功能点全集大表（81 条，含最终筛选状态）

> 状态列：**一期**（纳入 5 组件，见第 5-6 章）/ **二期**（数据积累后，作为组件参数）/ **纪律**（实现约束，非功能）/ **现状已有** / **存档**（不做，附理由）/ **v2**（v2.0.0 已规划）。
> issue #3 原始编号映射：A1→F13/F15、A2→F43、A3→F28、A4→F26、A5→F27、B1→F35、B2→F36、B3→F37、C1→F40、C2→F41、C3→F42。

### 3.1 用量与成本指标

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F01 | token 三分账 | 会话/日级输入·输出·缓存展示 | ccusage、Langfuse | 一期（②/④） |
| F02 | 缓存命中率与省钱效果 | 命中率 % | OpenRouter | 二期（②指标） |
| F03 | reasoning token 归桶 | 推理 token 单列 | Langfuse | 存档：与纯 token 口径重叠，边际价值低 |
| F04 | 成本估算（¥/$） | 价目表折钱 | ccusage、Helicone | 存档：用户否决，维护价目表太重型 |
| F05 | 按模型分布 | 各模型 token 占比 | Usage4Claude | 存档·后置观察：dsh 单模型场景为主 |
| F06 | 按项目/agent 归因 | 哪个项目烧的 | claude-code-otel | 一期（④按会话维度天然携带） |
| F07 | 账户余额水位 | API 余额可视化 | DeepSeek API | 存档：用户否决 |
| F08 | 生产力副产品指标 | 会话数/turn 数/工具调用数 | claude-code-otel | 一期（④指标集） |
| F09 | 最贵任务归因 | 哪类任务最烧 | Braintrust Topics | 二期（④指标） |
| F10 | sparkline 迷你趋势 | 历史曲线 | iStat、Rainmeter | 二期（②/④表现增强） |
| F11 | 5h 计费窗口进度 | 订阅窗口剩余 | ccusage、Codex CLI | 存档：按量计费模型不符 |
| F12 | 重置倒计时 | 额度何时恢复 | ClaudeUsageBar | 存档：同上 |

### 3.2 配额·阈值·预算

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F13 | 多级颜色阈值 | 阶梯变色+升级提醒 | ClaudeUsageBar、Helicone | 一期（②被动喊） |
| F14 | 阈值全可配置 | 默认值+设置卡可调 | 社区反面教训 | 一期（②参数） |
| F15 | 会话级阈值 | 单会话 token 上限 | LiteLLM | 一期（②） |
| F16 | 单次昂贵请求标记 | 单请求异常贵 | Portkey | 存档：涉价格口径，随 F04 否决 |
| F17 | 软/硬限制语义分离 | 提醒 vs 断供 | OpenAI | 存档：插件无断供能力 |
| F18 | 硬熔断/自动处置 | 超限自动降级 | Roo、Cursor、AWS | 存档：属 dsh 侧机制 |
| F19 | 双上限（次数+成本） | 触顶暂停 | Roo Code | 存档：同上 |

### 3.3 预测式告警

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F20 | 燃烧率+耗尽预测 | 按速度预测何时到阈值 | Claude-Code-Usage-Monitor | 存档：用户否决预测式 |
| F21 | 花费外推 | 照此速度今日将花 X | AWS Budgets | 存档：同上+涉价格 |

### 3.4 实时状态与进度

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F22 | 七状态机对齐 | workbuddy 全状态机 | workbuddy-buddy | 存档：现状四态+错误够用 |
| F23 | 当前动作展示 | 显示正在跑的工具 | OpenCode、openpets | 存档：现有取材链已覆盖（见 6.5） |
| F24 | 上下文余量 % | 余量颜色分级 | Cline、Claude Code | 存档·数据源待验证：dsh 若暴露 context 余量可复活 |
| F25 | 审批等待细分 | 等权限/等输入分开 | Claude Code、Devin | 存档：源码验证「等输入」与「完成」同信号（turn/end），区分需读内容，不做 |
| F26 | 审批滞留升级 | 红灯 N 分钟升级 | issue#3 A4 | 一期（③L2） |
| F27 | 长任务守望 | 单 turn>N 分钟看表 | issue#3 A5 | 一期（①长跑档） |
| F28 | 高强度姿态 | 事件激增→狂奔/擦汗 | issue#3 A3、RunCat | 一期（①高强度档） |
| F29 | 渐进摸鱼阶梯 | 无活动→咸鱼渐变 | workbuddy-buddy | 一期（①摸鱼档） |
| F30 | 失败循环信号 | 工具反复失败→焦虑 | Aider | 存档：错误态已有，循环判定过细 |
| F31 | 工具调用统计 | 次数+耗时 Top N | issue#3 数据源 | 一期（④指标） |
| F32 | 心跳缺失检测 | 无响应即断联 | healthchecks.io | 现状已有（断联态，v1.3.0） |
| F33 | 防抖判定 | 持续 N 分钟才算异常 | Prometheus | 存档：用户否决，监控不需要这么细 |
| F34 | 活动流步骤日志 | 点开看最近 N 步 | Devin、Manus | 存档：用户否决，dsh 自带会话记录 |

### 3.5 周期总结与趋势

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F35 | 小黑板总结 | 会话/turn/三分账/Top3/最长 turn/报错 | issue#3 B1 | 一期（④） |
| F36 | 总结触发时机 | 关宠/每小时/日终可配 | issue#3 B2 | 一期（④参数） |
| F37 | 里程碑一句话 | 整数关口一次性播报 | issue#3 B3 | 一期（②一次性关口规则） |
| F38 | 周报 | 周一早晨上周汇总 | WakaTime | 二期（④周期参数） |
| F39 | 定时摘要 | 低优先级攒整点说 | Apple Scheduled Summary | 存档：用户否决，复杂 |
| F40 | 缓存率趋势 | 本周 vs 上周 | issue#3 C1 | 二期（②/④指标） |
| F41 | 失败率异常 | 报错率突升 | issue#3 C2 | 二期（④指标） |
| F42 | 工具耗时漂移 | 某工具变慢 | issue#3 C3 | 二期（④指标） |
| F43 | 缓存率 vs 基线 | 低于项目近期基线 | issue#3 A2 | 二期（②指标） |
| F44 | 等待时间分类 | 等审批 vs 干活占比 | WakaTime | 二期（④指标） |
| F45 | 总结导出/分享 | 文本/图片导出 | OpenRouter | 二期（④输出参数） |

### 3.6 桌宠载体编码与形态

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F46 | 速度=强度 | 动画速度编码负载 | RunCat | 现状理念已有，①统一 |
| F47 | 三色语义+深红错误 | 绿黄红固定语义 | MAM | v2（FR-6） |
| F48 | 姿态第二通道 | 尾巴/耳朵/持物 | Nabaztag | 现状已有（动画体系） |
| F49 | 进行中缓动/终态定格 | 动画只给进行中 | CCMenu | 纪律 |
| F50 | 事件一次短动画 | 跃迁播一次即回稳态 | Luxafor | 纪律 |
| F51 | 呼吸灯空闲存活 | 慢节奏信号 | 树莓派 LED | 现状已有（idle 动画） |
| F52 | 悬停细读档 | 悬停浮迷你数据条 | Live Activity | 一期（②主动面） |
| F53 | 悬停浮现控件 | 靠近才浮现 | VPet DemoClock | 一期（同 F52） |
| F54 | 左右浮窗载体约束 | 不占卡区/不挡点击 | issue#3 | 一期（④⑤布局纪律） |
| F55 | 标签页标题闪烁 | document.title 闪烁 | whale-on-desk | 一期（③L3） |
| F56 | 思考摘要展示 | 思考一句话上屏 | liang-desktop-pet | 存档：现有取材链已覆盖（见 6.5） |
| F57 | TTS 朗读 | 朗读上屏文本 | liang-desktop-pet | 一期（⑤通道开关） |
| F58 | 会话清单 | 右键菜单各会话状态直达 | dsh-desk-pet | 一期（③会话一览） |
| F59 | 斜杠命令面板 | 快捷命令入口 | whale-on-desk | 存档·远期：WebUI 自有命令体系 |
| F60 | Roll-call 一句话汇报 | 全部项目状态拼一句 | vscode-pets | 二期（③会话一览文字形态；纯拼接不调 LLM） |
| F61 | 举牌 | 手持信息牌 | issue#3 A1 | 一期（⑤容器形态） |
| F80 | 宠物即审批门 | 气泡带允许/拒绝按钮 | workbuddy-buddy | 存档：盲批风险+复刻宿主审批 UI，用户否决 |

### 3.7 打扰治理纪律（横切）

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F62 | 严重度→形式映射 | 尾摇/气泡/前台+声三级 | Grafana、HIG | 存档：用户否决，现有情景映射够用 |
| F63 | 分组合并+原地更新 | 同类合成一条不堆叠 | Grafana、移动端 | 纪律 |
| F64 | 一次性静默 | 「这条 1 小时别再提」 | Grafana silence | 二期（体验打磨） |
| F65 | 周期静默/勿扰窗口 | 夜间/会议静默 | Grafana mute timings | 纪律 |
| F66 | 抑制 | 高级别压低级别 | Grafana inhibition | 一般原则（不单列） |
| F67 | 双速告警 | 快烧立即/慢烧进日报 | SLO burn rate | 一般原则（不单列） |
| F68 | 提醒必须可行动 | 每条带至少一个动作 | Apple HIG | 纪律 |
| F69 | 全局暂停不丢状态 | 勿扰总开关 | Roo Code | 现状已有（muted），随④扩展总结静默 |

### 3.8 隐私·信任

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F70 | 本地优先 | 数据不出机 | ccusage、ActivityWatch | 现状架构事实（胖宿主本地） |
| F71 | 诚实标注 | 口径/滞后明示 | ccusage、AWS | 纪律（纯 token 口径说明） |
| F72 | 安全气泡白名单 | 只显示静态文本，不显示 prompt 原文/密钥 | openpets | 纪律 |

### 3.9 皮肤与生态

| # | 功能点 | 说明 | 代表来源 | 状态 |
|---|---|---|---|---|
| F73 | 多宠物商店/导入/管理 | 四来源+热切换+守卫 | MAM、Petdex | v2（FR-10~17） |
| F74 | 16 方向看向图集 | 图集规范扩展 | awesome-codex-pet v2 | 存档·远期 |
| F75 | AI 生成形象 | 一句话画皮肤 | dsh-pet-sprite | 存档·远期 |
| F81 | 每项目专属宠物 | 不同项目不同宠物实例 | openpets pet-pack | 存档·远期 |

### 3.10 游戏化存档（明确不做）

| # | 功能点 | 代表来源 | 状态 |
|---|---|---|---|
| F76 | 养成/等级/经济/商店/图鉴 | VPet、Habitica | 存档：issue#3 非目标 |
| F77 | goals/streak/签到 | WakaTime、Forest | 存档：制造回访义务 |
| F78 | 投喂/摸头/扔球/WASD/陪聊 | vscode-pets、dsh-pet-sprite | 存档：娱乐向 |
| F79 | AI 看屏幕/roast/联机 | Open-LLM-VTuber、commitachi | 存档：娱乐向 |

### 3.11 本轮新增（调研后讨论产生）

| # | 功能点 | 说明 | 状态 |
|---|---|---|---|
| F82 | 审批直达 | 点击红卡/审批气泡直达该会话审批位置（锚点跳转，非按钮审批） | 一期（③末端） |
| F83 | 行尾年龄标注 | 卡片行末尾灰色「×s/×m」标注信息新鲜度 | 一期（⑤取材链增强） |

## 4. 筛选记录（两轮）

**轮 1（轴默认 + 大表）**：信息范围=仅 dsh 会话数据；成本口径=纯 token；交互边界=纯展示+直达；常驻信息=悬停/点开两档。
**轮 2（用户逐条意见）**：否决 F04/F07/F11/F12/F16（价格类）、F20/F21（预测式）、F23（分类够用）、F33（监控不必那么细）、F34（dsh 自带）、F39（复杂）、F62（映射够用）、F80（审批机制复杂）；纪律类定性为「优化性内容，可做但非核心」→ 压缩为实现约束。
**轮 3（重叠归并确认）**：悬停迷你条 ✅、审批纯直达 ✅、roll-call 记二期（不调 LLM）✅、年龄标注 ✅、F56 与 F23 同族一并存档 ✅。

## 5. 核心需求归纳：16 个一期功能点 → 5 类需求

重叠分析发现**三组高度同族**（同机制或同数据，只是触发时机/表达形式不同）：

| 同族组 | 成员 | 重叠本质 |
|---|---|---|
| 时间姿态族 | A3 高强度、A5 长任务、F29 摸鱼 | 同一把「事件时间轴→姿态」连续分阶尺的三个刻度 |
| Token 表现族 | A1 阈值、B3 里程碑、悬停迷你条 | 同读一个计数器（会话/日级三分账），被动喊/被动报/主动查三个面 |
| 审批升级族 | A4 滞留、F55 标题闪烁、审批直达、F58/F60 会话一览 | 同一条按等待时长逐级升压的召集链的不同环节 |

由同族关系 + 剩余独立点，归纳出 5 类核心需求（每类一句话）：

| # | 核心需求 | 吸收的功能点 | 组件 |
|---|---|---|---|
| ① | 不用切窗口就知道每个 agent 处在什么节奏 | F28、F27、F29 | 时长姿态引擎 |
| ② | token 烧到哪了——想查有处查，越线有分寸地喊 | F13/F14/F15、F37、F52/F53 | Token 计量器 |
| ③ | 需要我的地方逐级升级召集，最后一步直达 | F26、F55、F82、F58（+F60 二期） | 审批升级链 |
| ④ | 干完活有个交代 | F35/F36、F01、F08、F31、F06 | 小黑板 |
| ⑤ | 同样的信息用合适的容器和通道呈现 | F61、F57、F54、F83 | 表达容器 |

①②③是数据引擎（宿主半），④⑤是呈现层（客户端半）——与 v2.0.0 的胖宿主+薄客户端架构同构。

## 6. 需求 → 形态设计（每节四步：需求 → 组合 → 方案选择及理由 → 设计）

### 6.0 形态总览：所有组件在宠物上的空间展开

```
┌─ WebUI 标签页 ────────────────────────────────────────────────┐
│  document.title「🦊 审批等待中…」 ← ③ L3 标题闪烁（区域外信号）   │
│                                                               │
│         ┌────────────┐                                        │
│         │ 头顶状态卡区 │ ← 已有；+⑤行尾年龄标注；+③L0 红卡/直达   │
│         └────────────┘                                        │
│    ┌────────┐   ┌──────────────────┐                          │
│    │ 宠物本体 │   │ 左/右侧临时浮窗    │ ← ④小黑板 / ②阈值详情     │
│    │ 动画=①  │   │ 出现→停留→自动消失 │    （未显示时零遮挡）      │
│    └────────┘   └──────────────────┘                          │
│    悬停本体≈0.5s ─浮现→ 迷你条「今日 12.3万 · 本会话 2.1万 ·      │
│                          1 等审批 / 1 运行 / 1 完成」 ← ②主动面+③一览│
│    ┌────────────┐                                             │
│    │ 字幕格（已有）│ ← ⑤气泡台词 / 举牌（短数字） / TTS 可选朗读     │
│    └────────────┘                                             │
│    右键菜单：+③「会话一览」子菜单（各会话名+状态点+直达）            │
└───────────────────────────────────────────────────────────────┘
```

一图读懂：**宠物本体动画承载①，悬停迷你条是②③的主动查询面，头顶卡区（已有）叠加③的 L0 与⑤的年龄标注，左右浮窗是④②的明细层，字幕格是⑤的短文本层，标签页标题是③的最外层召集信号。** 新增的常驻 UI 元素数量：**零**（迷你条仅悬停期间存在）。

### 6.1 需求①：知道每个 agent 处在什么节奏 → 时长姿态引擎

- **组合**：F28 高强度 + F27 长任务 + F29 摸鱼阶梯——三个功能点是同一把尺子的三个区间。
- **方案选择**：
  - A. 独立 UI 元素（计时条/仪表贴宠物旁）——否：新增视觉占用，文字化，违反「不必说话也能沟通」。
  - B. 三个独立提醒功能（各弹各的气泡）——否：三次打扰、三份重复逻辑，正是用户批评的「多而杂」。
  - C. **连续姿态分阶尺（选定）**：一个引擎把「事件密度 × 距上次事件时长 × turn 开合」映射到宠物姿态档位，宠物动画本身就是显示层。
- **选 C 的理由**：RunCat 范式（动画速度=负载）被 30 年验证；calm technology 原则「信息先进余光」；零新增 UI 元素，完全复用宠物本体；实现上是一个函数（时间轴→档位），不是三个功能。
- **设计**：
  - 档位刻度（默认值，全部进设置卡可调）：
    | 档位 | 判定（初始值） | 姿态 |
    |---|---|---|
    | 高强度 | 近 5 分钟事件数 ≥ N | 狂奔/擦汗变体，动画加速 |
    | 活跃 | turn 开着，正常事件流 | 现有 running 姿态 |
    | 长跑 | turn 开着 + 静默 ≥ 3 分钟 | 看表姿态（防挂机干等） |
    | 空闲 | turn 关着 + 静默 < 15 分钟 | 现有 idle/环视 |
    | 摸鱼 | turn 关着 + 静默 ≥ 15/25/35/60 分钟（四阶） | 渐进咸鱼变体 |
  - 任何新事件立即重置到活跃/空闲；档位跃迁只播一次短动画（纪律 F50）。
  - 数据全部来自已有事件时间戳，无新采集。

### 6.2 需求②：token 烧到哪了 → Token 计量器（一个数据核 + 三个表现面）

- **组合**：F13/F14/F15 阈值 + F37 里程碑 + F52/F53 悬停迷你条（二期 F02/F43/F10 挂同一数据核）。
- **方案选择**：
  - A. 常驻 HUD 数字条——否：违反载体约束（区域零遮挡），高频跳动数字是 TrafficMonitor 式反面模式。
  - B. 全部主动查询（点开才看）——否：越线了用户不知道，失去警报价值。
  - C. **一个计数器 + 三表现面（选定）**：主动查（悬停迷你条）/ 被动喊（阈值举牌）/ 周期报（黑板一行）。
- **选 C 的理由**：这是 ClaudeUsageBar/ccusage 等菜单栏工具群的共识设计（常驻低打扰位+达阈值才出声）；三功能点同读一份数据，分开做=三份重复实现；B3 里程碑本质是「一次性整数关口阈值」，并入阈值规则后少一个功能点。
- **设计**：
  - 数据核（宿主半）：会话级+日级两档计数器，输入/输出/缓存三分账，随 `/state` 快照下发（token 数据源在 plan 阶段验证，见附录 A）。
  - 表现面 1·悬停迷你条：悬停宠物 ≈0.5s 浮现于宠物侧方，一行：「今日 12.3万 · 本会话 2.1万 · 1 等审批/1 运行/1 完成」；移开即消失；存在期间不拦截区域外点击。
  - 表现面 2·阈值举牌：会话/日两级阈值各自可配（默认关）；每级分 2-3 档阶梯（如 80%/100%）；触发时宠物举牌「本会话已消耗 X」+气泡+可选语音；同一档只报一次，不重复轰炸；档位完成使命后回稳态。
  - 表现面 3·里程碑：累计 token 过整数关口（100 万/500 万…）一句话气泡，一次性、不持久化、不成系统。

### 6.3 需求③：需要我时逐级召集 → 审批升级链

- **组合**：F26 滞留升级 + F55 标题闪烁 + F82 直达 + F58 会话清单（二期 F60 并入会话一览）。
- **方案选择**：
  - A. 各级提醒各自为政（气泡/弹窗/声音叠加）——否：Clippy 教训，通知轰炸。
  - B. 宠物内直接审批按钮（workbuddy-buddy 式）——否：盲批风险；复刻宿主审批 UI 双份维护；用户已否决。
  - C. **按等待时长逐级升压的一条链（选定）**：每升一级只增加一种新媒介，末端直达。
- **选 C 的理由**：Grafana 严重度分级与 Apple interruption levels 的成熟范式（逐级升压而非叠加轰炸）；标题闪烁是 whale-on-desk 验证过的「把信号带出宠物区域」的唯一 WebUI 手段（用户切去别的标签页时仍可见）；直达是最小改造量的行动闭环。
- **设计**：
  - 链条（每级可配/可关）：
    | 级 | 触发 | 形式 | 状态 |
    |---|---|---|---|
    | L0 | `approval/asked` | 红卡（已有） | 已有 |
    | L1 | 同上 | 审批语音（10s 限流，已有） | 已有 |
    | L2 | 等待 ≥ N 分钟（默认 5）未 `decided` | 姿态升级 + 气泡「等审批 N 分钟」 | 新增 |
    | L3 | L2 后仍无处理 | 标签页标题闪烁「🦊 审批等待中」 | 新增 |
    | 末端 | 点击红卡/气泡 | 直达该会话审批位置（锚点跳转） | 新增 |
  - `approval/decided` 即全链解除，标题恢复。
  - 会话一览：右键菜单子菜单（会话名+状态色点+点击直达）；二期加一句话形态（F60，纯拼接零 LLM）。

### 6.4 需求④：干完活有个交代 → 小黑板

- **组合**：F35 指标 + F36 时机（二期 F38 周报/F45 导出/F44/F41/F42/F09 全为本组件参数扩展）。
- **方案选择**：
  - A. 常驻侧栏图表面板——否：宠物不做常驻面板，Grafana 全家桶对个人是重型方案。
  - B. 逐条推送总结消息——否：打扰且不可回看。
  - C. **临时浮窗黑板（选定）**：出现 → 停留 → 自动消失，可手动唤回最近一期。
- **选 C 的理由**：issue #3 载体约束原文已定（左右浮窗、不占卡区、不挡点击、临时性）；TRMNL/e-ink 低频快照证明静态排版足以承担复盘；WakaTime 验证周期复盘的真实价值在「汇总」而非「实时」。
- **设计**：
  - 位置：宠物左侧或右侧（自动避让：优先与迷你条相反一侧）；未显示时该区域完全无遮挡（无透明蒙版占位）。
  - 一期指标集：会话数 / turn 数 / token 三分账（会话与日级）/ 工具调用 Top3（次数+耗时）/ 最长单 turn / 报错次数。
  - 触发：关闭桌宠时分发一次；不关闭时定时分发（每小时/每日日终，可配可关）。
  - 行为：自动消失（默认 15s 可配）；点击宠物或右键菜单可手动唤回最近一期。
  - 二期参数化扩展：周期维度（周报）、指标维度（缓存率/失败率/耗时漂移/等待分类/最贵任务）、输出维度（复制文本/导出图片）。

### 6.5 需求⑤：信息用合适的容器呈现 → 表达容器

- **组合**：F61 举牌 + 已有气泡/字幕 + F57 TTS + F54 布局约束 + F83 年龄标注。
- **现状发现（重要）**：v1.3.0 卡片行取材逻辑（`src/index.js:180-186`）**已经是一条「最新动态」回退链**：`assistant 正文 > tool/result 内容 > tool/call「运行 <工具名>」 > user 消息`，取最新 2 行、每行截断 24 字。因此 F23（当前动作展示）与 F56（思考摘要）**已被现状覆盖**，双双存档——这正是「同源功能点合并到单一组件」的最好例证。
- **方案选择**：每个功能自带提示样式（否，样式爆炸行为不一）→ **统一容器协议（选定）**：按内容类型选形态。
- **选它的理由**：VPet MessageBar/vscode-pets 等所有成熟桌宠都是「牌/泡/板」三级容器结构；容器统一后，新功能只生产内容，不发明呈现方式。
- **设计**：
  - 三形态规则：短数字类（≤8 字）→ **举牌**；一句话（台词/里程碑）→ **气泡**（现有）；明细（总结/阈值详情）→ **黑板浮窗**（组件④）。
  - 通道：视觉为主；TTS 为可选朗读通道（朗读任何上屏文本，复用现有语音组机制，无对应语音静默跳过）。
  - 取材链增强（本轮唯二新增之一）：卡片行沿用现有四档回退链，行尾追加灰色年龄标注「×s/×m」（来自事件时间戳，与①同源），解决「运行 bash 挂 5 分钟分不清是刚发生还是卡住」；思考文本兜底列为可选开关（默认关）。
  - 布局纪律（F54）：左右浮窗不占头顶卡区与下方字幕格；未显示时零遮挡；白名单（F72）：只显示静态文本与结构化摘要，不显示 prompt 原文与密钥。

## 7. 实现约束（纪律压缩版，随一期功能落地，不单独开发）

1. 进行中用缓动动画、终态定格（F49）；档位跃迁只播一次短动画，随后回稳态（F50）。
2. 同类提醒合并为一条、原地更新，不堆叠（F63）。
3. 勿扰窗口：夜间/会议时段静默（联动 muted 总开关，F65/F69）。
4. 每条提醒至少附带一个动作入口（跳转/唤回明细/关闭）（F68）。
5. 白名单：宠物只显示静态文本与结构化摘要，不显示 prompt 原文/代码内容/密钥（F72）；纯 token 口径在设置卡说明（F71）。

## 8. 与 v2.0.0 的关系

- 本设计为 **v2.1 方向**，前置依赖：v2.0.0 的 FR-1（snapshotEvents 迁移）与 FR-23（/state 扩展）是本设计的数据通道；FR-5（MAM 本体）是①的姿态变体基础。
- 不修改 v2.0.0 任何范围；一期功能全部为**增量**（新增聚合字段、新增姿态变体、新增浮窗组件）。
- 宿主半新增聚合（计数器/时间轴档位/审批等待时长）全部经 `/state` 快照下发，不新增轮询通道（维持 1.5s）。

## 9. 二期参数清单（全部为组件参数，不新增组件）

| 挂载组件 | 参数/指标 | 对应功能点 |
|---|---|---|
| ② 计量器 | 缓存命中率行、缓存率 vs 基线、sparkline | F02、F43、F10 |
| ④ 小黑板 | 周报周期、失败率、工具耗时漂移、等待时间分类、最贵任务归因、导出 | F38、F41、F42、F44、F09、F45 |
| ③ 会话一览 | 一句话形态（roll-call） | F60 |
| 体验打磨 | 一次性静默「这条 1 小时别再提」 | F64 |

## 10. 存档区索引（全部否决理由见第 3 章状态列）

价格类（F04/F07/F16/F21）、预测类（F20）、订阅窗口类（F11/F12）、断供类（F17/F18/F19）、状态细分类（F22/F23/F25/F30/F56）、监控深度类（F33/F34）、摘要类（F39）、映射细分类（F62）、命令面板（F59）、宠物内审批（F80）、远期形态（F24 数据源待定、F74/F75/F81）、游戏化全套（F76-F79）。

---

## 附录 A：数据源映射与验证状态

| 数据 | 来源（dsh） | 验证状态 |
|---|---|---|
| 审批事件 | `approval/asked` / `approval/decided`（含 id） | ✅ 已验证（v1.3.0 在用） |
| turn 边界与结果 | `turn/start` / `turn/end`（reason.kind: error/interrupted…） | ✅ 已验证 |
| 工具调用 | `tool/call`（name/arguments）/ `tool/result` | ✅ 已验证（取材链在用） |
| 消息文本 | `user/message` / `assistant/message` | ✅ 已验证（取材链在用） |
| 思考文本 | `reasoning-*` 事件族 | ✅ 存在（可选兜底开关用） |
| 事件时间戳 | 各事件 time 字段 | ✅ 已验证 |
| token 用量 | issue#3 称「dsh token-meter / 会话 usage 数据（web-ui usage 插件已读同类数据）」 | ⚠️ 待 plan 阶段逐行验证（唯一未验证项） |
| 审批锚点直达 | 会话内定位到审批位置的跳转机制 | ⚠️ 待 plan 阶段验证（WebUI 路由/滚动锚点） |

## 附录 B：本设计的功能点计数

- 功能点全集：83 条（调研 81 + 讨论中新增 F82 直达、F83 年龄标注；含 issue#3 全部条目）。
- 一期纳入：23 条 → 归并为 **5 个组件 + 2 个小增强**（年龄标注、直达）。
- 二期：12 条（全部为组件参数）。
- 纪律：7 条；现状已有：6 条；v2 已规划：2 条；一般原则：2 条；存档：31 条（每条有否决理由）。
- 净结论：**做精不做多——一期交付 5 个组件，新增常驻 UI 元素为零。**
